# PRD — نظام محاسبي متكامل للنقابة العامة ومشروع التكافل الاجتماعي

## بيان المنتج الأصلي
تطبيق محلي يعمل على Windows بنظام واجهة عربية كاملة لإدارة المحاسبة والودائع البنكية لجهتين منفصلتين بالكامل:
- النقابة العامة
- مشروع التكافل الاجتماعي

يجب أن يضمن النظام عزلاً كاملاً للبيانات بين الجهتين، وأن يعتمد على محرك قواعد محاسبية آلي بحيث يُدخل المستخدم أحداث العمل فقط، بينما يقوم النظام بتوليد القيود اليومية المتوازنة وتحديث دفتر الأستاذ دون إدخال يدوي للمدين والدائن. التطبيق موجه للعمل دون إنترنت داخل بيئة Windows محلية مع إمكانية الوصول عبر الشبكة المحلية LAN وحزمة تثبيت NSIS.

## المبادئ الحاكمة
- الواجهة والرسائل والتقارير باللغة العربية وباتجاه RTL.
- لا حذف محاسبي للقيود المرحلة؛ يتم استخدام القيود العكسية للحفاظ على الأثر الرقابي.
- دفتر الأستاذ والميزان والتسويات يجب أن تعتمد على journal_entries كمصدر الحقيقة.
- كل منظمة لها بياناتها ومستخدميها وقواعدها وقيودها بصورة مستقلة.
- محرك القواعد يحوّل الأحداث مثل الإيراد، المصروف، الرصيد الافتتاحي، إذن عضوية جماعي، ربط وديعة، وإهلاك أصل إلى قيود تلقائية متوازنة.
- تقارير PDF وExcel/Word يجب أن تكون عربية قابلة للطباعة والتصدير.

## المعمارية الحالية
- Backend: FastAPI + Motor/MongoDB في `/app/backend/server.py`.
- Frontend: React + Tailwind + Shadcn UI في `/app/frontend/src`.
- قاعدة البيانات: MongoDB عبر `MONGO_URL` و`DB_NAME` فقط.
- التغليف المحلي: NSIS عبر `/app/local_install/BankDepositSystem.nsi`.
- ملفات الإصدار المحلي داخل `/app/release/BankDepositSystem`، وملف التثبيت الناتج `/app/dist/BankDepositSystemSetup.exe`.

## أهم النماذج والكيانات
- `journal_entries`: مصدر الحقيقة للقيود ودفتر الأستاذ.
- `chart_accounts`: دليل الحسابات التلقائي لكل منظمة.
- `accounting_rules`: قواعد الترحيل الآلي.
- `banks` و`bank_settings`: البنوك وأرصدة الإدارة.
- `reconciliations`: التسويات البنكية.
- `memberships`: عضويات مشروع التكافل.
- `membership_batch_payments`: أذون التحصيل الجماعية وتوزيعات FIFO.
- `fixed_assets` و`fixed_asset_depreciations`: الأصول والإهلاكات.

## ما تم تنفيذه سابقاً
- محرك قواعد محاسبية آلي مع محاكاة Fail-Safe.
- دفتر أستاذ وميزان مراجعة من القيود اليومية.
- قيود عكسية بدلاً من حذف القيود المحاسبية.
- سجل تدقيق يتضمن قبل/بعد التعديل.
- تقارير PDF عربية محسّنة.
- إهلاك سنوي تلقائي للأصول الثابتة.
- تحسين تشغيل LAN وملفات التشغيل المحلية.

## تحديثات منفذة بتاريخ 2026-05-04
- تم تنفيذ محرك قيود الأرصدة الافتتاحية للبنوك: إدخال رصيد افتتاحي يولد قيداً تلقائياً من حساب البنك إلى حساب رصيد افتتاحي.
- تم جعل الرصيد الدفتري في التسوية البنكية مقروءاً من دفتر الأستاذ/القيود اليومية فقط عبر endpoint `/api/banks/{bank_id}/book-balance`، مع تجاهل أي رقم يدوي مرسل كرصيد دفتري في التسوية.
- تم إضافة حالات العضوية الأربع: فعال، معاش، متوفي، مستقيل، مع إيقاف الاشتراكات الجديدة لغير الفعالين والحفاظ على المديونيات القديمة.
- تم إضافة الاشتراك الشهري الافتراضي 3 جنيه، وحساب المستحق والمحصل والمتبقي لكل عضو.
- تم إضافة إذن جماعي للجان عبر `/api/memberships/batch-payments` يوزع المبلغ FIFO على أقدم المديونيات ويولد قيد يومية تلقائي.
- تم إضافة تقرير تحصيل العضويات حسب اللجنة أو المحافظة عبر `/api/memberships/collection-report`.
- تم توسيع التقرير السنوي للعضوية ليشمل المستحق والمحصل والمتبقي.
- تم تحديث صفحة العضوية لإضافة حالة العضو، الإذن الجماعي، تقرير المديونيات والتحصيل، وسجل الأذون.
- تم توحيد عرض التواريخ بدون وقت في الواجهة، وتعديل صفحة الودائع لاستخدام تاريخ فقط لإنشاء/استحقاق الوديعة.
- تم تحديث محرك القواعد ليدعم `OpeningBalance` و`MembershipBatchPayment` في القواعد والمحاكاة والواجهة.
- تم بناء نسخة الإنتاج للواجهة وتحديث حزمة Windows: `/app/dist/BankDepositSystemSetup.exe`.

## حالة الاختبارات
- اختبار ذاتي API ناجح لتسجيل الدخول، الرصيد الافتتاحي، الرصيد الدفتري من GL، العضوية، الإذن الجماعي، وتقرير العضوية.
- اختبار واجهة smoke ناجح: ظهور أقسام إذن جماعي وتقرير المديونيات في صفحة العضوية.
- اختبار مستقل iteration_57: نجحت تدفقات الأرصدة الافتتاحية، التسوية من GL، العضوية، الإذن الجماعي، تنسيق تاريخ الوديعة. كشف خطأ في Literals لمحرك القواعد وتم إصلاحه.
- إعادة اختبار ذاتي بعد الإصلاح: `/api/rules-engine/rules` يعمل، ومحاكاة `OpeningBalance` و`MembershipBatchPayment` تعمل بنجاح.
- فحص lint ناجح للباك إند والملفات الأمامية المعدلة.
- بناء `yarn craco build` ناجح، و`makensis` أنتج ملف التثبيت بنجاح.

## نقاط معروفة
- تكامل الفاتورة الإلكترونية ETA ما زال MOCKED ولم يتم ربطه فعلياً بسبب انتظار بيانات/SDK التكامل.
- ملف `/app/backend/server.py` ما زال كبيراً ويحتاج تفكيكاً لاحقاً إلى APIRouter modules لتقليل مخاطر الصيانة.

## الأولويات الحالية
### P0 — مكتمل
- الأرصدة الافتتاحية بقيود تلقائية وربطها بدفتر الأستاذ.
- التسوية البنكية من الرصيد الدفتري المحسوب من GL.
- حالات العضوية وإيقاف الاشتراكات الجديدة لغير الفعالين.
- الإذن الجماعي للجان وتوزيع FIFO وقيد اليومية.

### P1 — مكتمل
- تنسيق التاريخ بدون وقت في تدفقات الودائع والعرض العام.
- تقارير العضوية حسب اللجنة والمحافظة للمستحق/المحصل/المتبقي.

### P2 — متبقٍ
- تفكيك `/app/backend/server.py` إلى ملفات Routers وخدمات أصغر.
- استكمال تكامل ETA الحقيقي عند توفر مفاتيح/SDK من المستخدم.
- تحسين محرر محرك القواعد بخصائص بحث/نسخ/تعطيل جماعي.

## بيانات اختبار معروفة
- username: `admin`
- password: `Admin@123`
- organizations: `general-union`, `social-solidarity`


## تحديث إصلاح حرج بتاريخ 2026-05-04
- تم حل مشكلة سطور القيود غير المرتبطة بحسابات في القيود: 122، 123، 128، 129، 130، 131، 132، 133، 138، 139، 144، 145، 146، 147، 152، 153.
- تم إضافة حساب `الخزينة` إلى شجرة الحسابات كحساب أصل قابل للترحيل بكود `1150` و`system_key=cash_box`.
- تم ربط الاسم القديم `إيرادات الاشتراكات` تلقائياً بحساب `إيرادات اشتراكات العضوية` كود `4103` و`system_key=membership_subscription_revenue`.
- تم تشغيل مزامنة شجرة الحسابات وترميم روابط القيود الحالية؛ في منظمة مشروع التكافل تم ترميم 32 سطر/قيد مرتبط بهذه المشكلة.
- تم تشغيل فحص التدفق الكامل: `journal_entries → ledger → trial_balance → income_statement & cash_in_out → balance_sheet`.
- نتائج الفحص الذاتي: لا توجد سطور بلا حساب، ميزان المراجعة متوازن، دفتر الأستاذ مطابق للميزان، وفرق اتزان الميزانية = 0.
- نتائج Testing Agent iteration_58: جميع الفحوص المطلوبة نجحت 7/7، ولا توجد APIs/flows معطلة ضمن نطاق الاختبار.
- تم تحديث حزمة Windows النهائية بعد الإصلاح: `/app/dist/BankDepositSystemSetup.exe`.


## تحديث تقارير العضوية والطباعة بتاريخ 2026-05-04
- تم جعل تقارير العضوية الخمسة قابلة للطباعة كصفحات مستقلة: تقرير المديونيات والتحصيل، التقرير السنوي، الأذون الجماعية المحفوظة، بحث الاسم، وخروج المعاش.
- تم إضافة أزرار طباعة/تصدير لقسمي أذون جماعية محفوظة وبحث بالاسم، واستبدال طباعة خروج المعاش بطباعة تقرير مستقلة.
- تم إخفاء قسم العضويات المسجلة من عرض الصفحة مع استمرار تحميل بيانات العضويات لاستخدامها في القوائم والتقارير والبحث.
- تم إضافة حالة العضويات المستوردة وتاريخ الحالة إلى استيراد البيانات الخارجية، وربطها بالباك إند بحيث تحفظ الحالة وتاريخ الإيقاف تلقائياً لغير الفعالين.
- تم إضافة إعداد ESLint حديث للواجهة وسكربت `yarn lint` بعد ملاحظة الاختبار المستقل.
- اختبارات ذاتية: API preview/commit للاستيراد بحالة معاش، Screenshot للواجهة، Print media = 5 صفحات تقارير، lint/build ناجحين.
- Testing Agent iteration_59: كل اختبارات العضوية والطباعة والاستيراد نجحت؛ تمت معالجة ملاحظة lint بعدها بنجاح.
- تم تحديث حزمة Windows بعد تغييرات الواجهة والباك إند: `/app/dist/BankDepositSystemSetup.exe`.


## تحديث القيود العكسية وتدفقات البيانات بتاريخ 2026-05-04
- تم إلغاء عمود/ميزة `مقارن` نهائياً من صفحة الميزانية والحسابات الختامية، ولم تعد الصفحة تطلب تقرير السنة السابقة.
- تم تثبيت علم القيود العكسية `is_reversal=true/false` داخل نموذج القيود اليومية واستجابة API.
- القيود العكسية تبقى محفوظة في قاعدة البيانات لأغراض التدقيق، لكنها مخفية من دفتر اليومية، دفتر الأستاذ، ميزان المراجعة، القوائم المالية، وتقارير الفحص.
- تم إضافة endpoint شامل: `/api/admin/data-flow-validation` يعرض تدفقات البيانات المحاسبية والعضوية لكل جهة ويفحص الاتزان وعدم وجود missing/orphan entries.
- تم إضافة صفحة `تدفقات البيانات` داخل لوحة التحكم الآمنة لعرض مسار: journal_entries → ledger → trial_balance → income_statement/cash_in_out → balance_sheet، مع فحص العضوية والحالات والتحصيل.
- تم إضافة صفحة `تفريغ البيانات` داخل لوحة التحكم الآمنة للسوبر أدمن فقط، مع عبارة تأكيد إجبارية `تفريغ البيانات نهائيا` قبل أي حذف. لم يتم تنفيذ التفريغ على بيانات التطوير.
- تم تشغيل الفحص الكامل بنجاح: كل الجهات سليمة، ميزان المراجعة متوازن، فرق الميزانية = 0، ولا توجد قيود يتيمة أو سطور بلا حساب ضمن النطاق المطلوب.
- تم تشغيل فحص العضوية: الحالات والتحصيل والتوزيع سليمة وبدون issues.
- Testing Agent iteration_60: نجح 9/9، ونجحت واجهة القوائم ولوحة الأدمن وقيود التفريغ، ولا توجد تدفقات مكسورة ضمن النطاق.
- `yarn lint` و`yarn craco build` نجحا، وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe`.


## تحديث Data Flow & Rules Manager بتاريخ 2026-05-04
- تم إضافة صفحة جديدة داخل لوحة السوبر أدمن باسم `Data Flow & Rules Manager`، وهي خاصة بالسوبر أدمن فقط.
- تم إضافة Flow Monitor يعرض مراحل التدفق: إدخال العملية، الفحص الآلي، إنشاء القيد، دفتر اليومية، الأستاذ العام، ميزان المراجعة، القوائم المالية، ومحرك القواعد، مع الحالة وعدد العمليات وآخر تشغيل وعدد الأخطاء.
- تم إضافة Rules Engine إداري داخل الصفحة لإدارة القواعد فقط: الحدث، النوع، الحساب المدين، الحساب الدائن، وتشغيل/إيقاف القاعدة، بدون السماح بتعديل القيود المحاسبية نفسها.
- تم إضافة Validation سريع داخل الصفحة لفحص: اتزان القيود، الأستاذ العام، ميزان المراجعة، الميزانية، والعضوية.
- تم إضافة endpoints محمية بالسوبر أدمن: `/api/admin/data-flow-rules-manager` و`/api/admin/data-flow-rules-manager/rules/{rule_id}`.
- تم ربط تعديلات القواعد بسجل التدقيق مع `before_document` و`after_document`، ويظهر من قام بالتعديل ووقت التعديل والقيمة القديمة والجديدة.
- تم التحقق أن تعديل القاعدة لا يغير `journal_entries` ولا يسمح بتعديل القيود نفسها.
- الاختبار الذاتي نجح: GET للمدير، تعديل قاعدة ثم إرجاعها، وجود audit before/after، وظهور الواجهة.
- Testing Agent iteration_61 نجح بالكامل: لا توجد APIs أو تدفقات مكسورة ضمن النطاق.
- نجح `yarn lint` و`yarn craco build`، وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe`.


## تحديث تاريخ الرصيد الافتتاحي والتسوية البنكية بتاريخ 2026-05-04
- تم إضافة `opening_balance_date` لكل بنك، وإلزام السوبر أدمن بإدخال تاريخ الرصيد الافتتاحي بصيغة `YYYY/MM/DD` في إضافة البنك وتعديل الأرصدة الافتتاحية.
- تم منع أي عملية مالية مرتبطة ببنك قبل تاريخ الرصيد الافتتاحي برسالة: `No financial transaction is allowed before the Opening Balance Date.`
- تم جعل قيد الرصيد الافتتاحي يُنشأ بتاريخ الرصيد الافتتاحي نفسه، مع الحفاظ على تدفق القيود إلى اليومية والأستاذ والميزان والقوائم.
- تم تطبيق حماية التاريخ على الودائع، الإيرادات، المصروفات، الأصول الثابتة، العهد، مصروفات البنك اليدوية، أذون العضوية الجماعية، والتسويات البنكية.
- تم تعديل حقل `الإدارة` في التسوية البنكية ليعرض خياراً واحداً فقط حسب الجهة الحالية: مشروع التكافل عند الدخول عليه، والنقابة العامة عند الدخول عليها.
- تم تشغيل فحص ذاتي: رفض عملية قبل التاريخ، قبول عملية في نفس التاريخ، `/api/admin/data-flow-validation` سليم، واجهة التسوية تعرض خيار إدارة واحد للجهتين.
- Testing Agent iteration_62 نجح في نطاق الطلب، وتمت معالجة ملاحظتي الرسالة وصيغة التاريخ بعده ذاتياً.
- نجح `yarn lint` و`yarn craco build`، وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe`.


## تحديث معادلة رصيد التسوية البنكية بتاريخ 2026-05-06
- تم تعديل رصيد التسوية البنكية ليحسب أوتوماتيكياً من بنود تفصيلية، وليس من إدخال يدوي أو من رصيد الأستاذ الخام فقط.
- المعادلة المعتمدة: الرصيد الافتتاحي + إجمالي الإيرادات الشهرية المحصلة للبنك + ترصيد الودائع المستحقة في الفترة + شيكات تحت التحصيل - المصروفات المدفوعة - شيكات لم تقدم للصرف - المصروفات البنكية.
- تم إضافة endpoint: `/api/banks/{bank_id}/reconciliation-balance` يعيد breakdown كامل: opening_balance, monthly_revenues, deposit_settlements, checks_under_collection, gross_total, monthly_expenses, checks_not_presented, bank_expenses, reconciliation_balance.
- حفظ/تعديل التسوية الآن يتجاهل أي `book_balance` مرسل من الواجهة ويستخدم الرصيد المحسوب تلقائياً، ويحفظ `balance_breakdown` داخل مذكرة التسوية.
- تم منع الجمع/الطرح المزدوج داخل التسوية بجعل `calculated_balance = book_balance` المحسوب من المعادلة.
- واجهة التسوية تعرض تفصيل رصيد التسوية البنكية في بطاقات واضحة، وتعرض التفصيل داخل الطباعة/المعاينة.
- تم التحقق ذاتياً من المعادلة والحفظ وتدفق البيانات، ثم نجح Testing Agent iteration_63 بالكامل بدون تدفقات مكسورة.
- نجح `yarn lint` و`yarn craco build` وتم تحديث `/app/dist/BankDepositSystemSetup.exe`.


## تحديث الودائع القائمة عند بداية الفترة بتاريخ 2026-05-06
- تم إضافة خيار `وديعة قائمة عند بداية الفترة` عند تسجيل الودائع.
- إذا كانت الوديعة قائمة أول الفترة، يسمح بأن يكون تاريخ الربط أقدم من تاريخ الرصيد الافتتاحي للبنك.
- يتم احتساب الوديعة من تاريخ بداية الفترة/تاريخ الرصيد الافتتاحي عبر `accounting_start_datetime`، وليس من تاريخ الربط القديم.
- لا يتم إنشاء حركة بنك تاريخية قبل تاريخ الرصيد الافتتاحي؛ قيد الوديعة القائمة يكون: مدين `ودائع لأجل` ودائن `رصيد افتتاحي` بتاريخ الرصيد الافتتاحي.
- الودائع الموسومة `is_opening_balance_deposit=true` لا تمنع تعديل تاريخ الرصيد الافتتاحي بسبب تاريخ ربطها القديم.
- تقارير عوائد الودائع تبدأ من `accounting_start_datetime` للودائع القائمة أول الفترة.
- تم إضافة عرض `بداية الاحتساب` في ملخص الوديعة.
- الاختبارات الذاتية نجحت، ثم Testing Agent iteration_65 نفّذ اختبارات API/UI فعلية ونجحت 2/2 backend مع UI toggle، ولا توجد تدفقات مكسورة.
- ملف التثبيت `/app/dist/BankDepositSystemSetup.exe` موجود ورابط `/api/download/setup` يعمل.


## تحديث دفتر الأستاذ — عرض الكل داخل الفترة بتاريخ 2026-05-06
- تم إضافة خيار `عرض الكل ضمن الفترة المحددة` في قائمة الحساب داخل صفحة دفتر الأستاذ.
- endpoint `/api/ledger` يدعم الآن `account_id=all` ويعيد كل حركات الحسابات القابلة للترحيل داخل الفترة فقط.
- تم إضافة `account_scope=all/single` وحقول الحساب داخل كل صف: `account_id`, `account_code`, `account_name`.
- في وضع حساب واحد، لم يعد دفتر الأستاذ يضيف حركات قبل `from_date` إلى الرصيد الجاري؛ يتم عرض واحتساب الحركات داخل الفترة المحددة فقط.
- واجهة دفتر الأستاذ تعرض عمود `الحساب` عند اختيار عرض الكل، وتوضح أن التقرير للحركات المالية داخل الفترة فقط.
- تم اختبار API ذاتياً ثم Testing Agent iteration_66: backend pytest 3/3 ناجح، UI contract ناجح، data-flow valid=true، ولا توجد APIs مكسورة.
- نجح `yarn lint` و`yarn build` وتم تحديث `/app/dist/BankDepositSystemSetup.exe`.


## تحديث منطق الفترة المالية الشامل بتاريخ 2026-05-06
- تم ضبط منطق الفترة المالية مركزياً بحيث تعتمد اليوميات والأستاذ والميزان والتسويات والتقارير على الحركات داخل الفترة فقط.
- دفتر اليومية: يدعم `from_date/to_date` ويعرض القيود المرحلة داخل الفترة فقط.
- دفتر الأستاذ: رصيد أول المدة = صافي الحركات السابقة، حركة الفترة = داخل الفترة فقط، رصيد آخر المدة = أول المدة + صافي حركة الفترة، مع دعم عرض الكل.
- ميزان المراجعة: تم إصلاح endpoint `/api/trial-balance` ليستخدم الدالة المركزية، ويرحل السابق إلى opening_balance مع إبقاء total_debit/total_credit لحركة الفترة فقط.
- التسوية البنكية: الرصيد الدفتري = رصيد أول المدة للبنك + إيرادات الفترة + ترصيد ودائع الفترة - مصروفات الفترة - المصروفات البنكية، ورصيد التسوية = الرصيد الدفتري + شيكات لم تقدم - شيكات تحت التحصيل.
- تقارير الودائع تستخدم بداية الاحتساب وتستبعد ما انتهى قبل الفترة، وتحتسب العوائد داخل الفترة فقط.
- تم إصلاح خلل كشفه Testing Agent iteration_67 في ميزان المراجعة، ثم نجح pytest الخاص بالفترة 6/6 محلياً.
- Testing Agent iteration_68 نجح بالكامل: pytest 6/6، واجهة Ledger والتسوية تعمل، data-flow valid=true، ولا توجد تدفقات مكسورة.
- نجح `yarn lint` و`yarn build` وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe`.

## تحديث منطق فوائد الحساب الجاري واستحقاق/ربط الودائع بتاريخ 2026-05-06
- تم إيقاف الترحيل الآلي لفوائد الودائع المجدولة كقيود تشغيلية؛ الوديعة تظل أصلاً يظهر في الميزانية ولا تتكرر في تقارير التشغيل.
- تم إضافة طريقتي تحصيل في الإيرادات: `فوائد الحساب الجاري` و`استحقاق وديعة`، وكلاهما يرحل فوراً كمدين بنك ودائن إيراد مناسب داخل نفس الفترة.
- تم إضافة نوع مصروف/خروج: `ربط وديعة`، ويجبر الحركة على حالة مدفوعة فوراً، يصفر الاستقطاعات، ويرحل كمدين `ودائع لأجل` ودائن بنك حتى لا يظهر كExpense تشغيلي في حساب الإيرادات والمصروفات.
- تم توحيد فلترة التقارير وفحص تدفق البيانات لاستبعاد قيود `deposit_interest` القديمة من التقارير التشغيلية والميزان/الفحص الافتراضي، مع بقاء البيانات محفوظة رقابياً.
- تم تحديث واجهتي الإيرادات والمصروفات بخيارات عربية واضحة و`data-testid` للخيارات الجديدة.
- الاختبار الذاتي نجح لإنشاء فوائد حساب جاري وربط وديعة والتحقق من القيود والقوائم المالية ثم تنظيف بيانات الاختبار.
- Testing Agent iteration_69 كشف أولاً خللاً في `data-flow-validation` بسبب احتساب قيود فوائد الودائع القديمة في جانب الفحص فقط؛ تم إصلاحه ثم نجح إعادة اختبار pytest: 7/7، وأصبح `/api/admin/data-flow-validation` يعيد `is_valid=true` للجهتين.
- نجح `yarn craco build` وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe` بعد تثبيت NSIS داخل الحاوية.

## تحديث دفتر الأستاذ — الحسابات ذات الحركة الفعلية فقط بتاريخ 2026-05-06
- تم تعديل `/api/ledger` في وضع `عرض الكل ضمن الفترة المحددة` بحيث لا يظهر أي حساب لمجرد وجود رصيد سابق أو أصل قائم قبل الفترة.
- يتم تحديد الحسابات الظاهرة من سطور القيود التي لها حركة فعلية داخل `from_date/to_date` فقط، ثم يُحسب رصيد أول المدة لهذه الحسابات وحدها من الحركات السابقة.
- الحسابات التي لها رصيد سابق فقط دون حركة داخل الفترة لا تظهر في صفوف دفتر الأستاذ ولا تدخل ضمن رصيد أول المدة المجمع في عرض الكل.
- تم الحفاظ على وضع الحساب الواحد: عند اختيار حساب محدد يظل رصيد أول المدة محسوباً لهذا الحساب، مع عرض حركات الفترة فقط.
- تم استبعاد قيود `deposit_interest` القديمة من حسابات دفتر الأستاذ الافتراضية مثل باقي التقارير التشغيلية.
- تم إضافة اختبار regression: `/app/backend/tests/test_iteration70_ledger_actual_movement_scope.py`.
- الاختبارات نجحت: iteration70 + اختبارات الفترة المالية iteration67 = 7/7، وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe`.

## تحديث دفتر الأستاذ — قيود الودائع الافتتاحية وفوائد الفترة بتاريخ 2026-05-06
- تم تعديل دفتر الأستاذ بحيث لا تظهر قيود `وديعة قائمة عند بداية الفترة` كسطر حركة داخل الفترة حتى لو كان تاريخ قيدها داخل يناير أو داخل الفترة المعروضة.
- قيد الوديعة الافتتاحية الصحيح محاسبياً يظل محفوظاً في دفتر اليومية، لكنه عند عرض دفتر الأستاذ يُرحّل إلى `رصيد أول المدة` فقط ولا يظهر ضمن حركات الفترة.
- عند اختيار حساب واحد مثل `1250 - ودائع لأجل`، يتم احتساب قيد الوديعة الافتتاحية ضمن رصيد أول المدة للحساب ولا يظهر كسطر حركة.
- عند عرض `كل الحسابات`، لا يظهر حساب الوديعة لمجرد وجود قيد افتتاحي فقط؛ يظهر فقط إذا حدثت عليه حركة فعلية داخل الفترة.
- تم إضافة حركة دفتر أستاذ مجمعة لفوائد الودائع داخل الفترة فقط: `إجمالي فوائد الودائع عن الفترة من ... إلى ...`، وتظهر بتاريخ نهاية الفترة كسطر مجمع بدلاً من إظهار أصل الوديعة أو فوائد خارج الفترة.
- الفائدة المجمعة تُحسب يومياً من تداخل فترة الوديعة مع الفترة المختارة، وتتجاهل الودائع غير النشطة خارج الفترة.
- تم إضافة اختبار regression: `/app/backend/tests/test_iteration71_ledger_opening_deposit_interest.py`.
- الاختبارات نجحت: iteration71 + iteration70 + اختبارات الفترة المالية iteration67 = 9/9، وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe`.

## إصلاح تحميل دفتر الأستاذ عند تاريخ فارغ بتاريخ 2026-05-06
- سبب رسالة `تعذر تحميل دفتر الأستاذ من القيود اليومية` كان أن واجهة `LedgerPage` ترسل `to_date=` أو `from_date=` فارغة عند مسح حقل تاريخ، فيرفضها FastAPI كقيمة تاريخ غير صالحة 422.
- تم تعديل الواجهة بحيث لا تُرسل أي معلمة تاريخ إلا إذا كانت ممتلئة فعلاً، مع منع تحميل الفترة إذا كان تاريخ البداية بعد تاريخ النهاية.
- تم التحقق من `/api/ledger` بثلاث حالات: تاريخ بداية فقط، تاريخ نهاية فقط، وبدون تواريخ — كلها تعيد 200.
- تم اختبار الصفحة بالمتصفح بعد مسح تاريخ النهاية، ولم تظهر رسالة الخطأ.
- تم تنظيف آثار بيانات الاختبارات السابقة من دفتر الأستاذ حتى لا تظهر للمستخدم.
- نجح `yarn craco build` واختبارات دفتر الأستاذ iteration70/71 = 3/3، وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe`.

## تحديث معادلة رصيد التسوية البنكية التلقائي بتاريخ 2026-05-06
- تم تعديل `/api/banks/{bank_id}/reconciliation-balance` ليحسب رصيد نهاية الشهر حسب المعادلة المطلوبة: `رصيد أول الشهر + إجمالي المقبوضات والحركات المدينة - إجمالي المدفوعات والحركات الدائنة`.
- مكونات الزيادة الآن تشمل المقبوضات البنكية الفعلية داخل الشهر: نقداً، شيكات محصلة، أوامر دفع، فوائد الحساب الجاري، استحقاق وديعة، بالإضافة إلى فوائد الودائع الشهرية المحسوبة تلقائياً للفترة والبنك المحددين.
- مكونات الخصم الآن تشمل المدفوعات البنكية الفعلية داخل الشهر: المصروفات العمومية، إعانات الوفاة، ربط وديعة، بالإضافة إلى المصروفات البنكية الشهرية.
- تم إيقاف تعديل الرصيد النهائي بإضافات/خصومات الشيكات غير المقدمة أو تحت التحصيل؛ تظل هذه القوائم معروضة للمتابعة فقط ولا تدخل في معادلة رصيد التسوية البنكية التلقائي.
- تم إضافة حقول توضيحية في الـAPI: `monthly_deposit_interest`, `total_receipts`, `total_payments` مع الحفاظ على الحقول القديمة للتوافق.
- تم تحديث واجهة `BankReconciliationPage.jsx` لعرض بطاقات المعادلة الجديدة: رصيد أول الشهر، المقبوضات، فوائد الودائع الشهرية، إجمالي المقبوضات، المدفوعات، المصروفات البنكية، إجمالي المدفوعات، ورصيد نهاية الشهر.
- تم إضافة اختبار regression: `/app/backend/tests/test_iteration72_bank_reconciliation_monthly_formula.py`.
- الاختبارات نجحت: iteration72 + اختبارات الفترة المالية iteration67 = 7/7، ونجح Smoke API وواجهة التسوية، وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe`.

## تحديث تجديد الودائع كأصول مستقلة وفترة سريان الأصل بتاريخ 2026-05-07
- تم إضافة حقول للودائع: `renewed_from_deposit_id`, `renewal_notes`, `status` لدعم الحالات: نشطة، مستحقة، مجددة، مغلقة.
- عند إنشاء وديعة جديدة مرتبطة بوديعة سابقة يتم التعامل معها كوديعة مستقلة بالكامل: رقم جديد، مبلغ جديد، تواريخ جديدة، وفوائد مستقلة، مع عدم تعديل البيانات الجوهرية للوديعة القديمة.
- يتم تحديث الوديعة السابقة فقط كحالة `مجددة` وإضافة ملاحظة تتبع مشتركة تظهر في تقارير الودائع القديمة والجديدة، بدون أي أثر على القيود اليومية أو دفتر الأستاذ أو ميزان المراجعة أو القوائم أو التسويات.
- تم منع إعادة الربط قبل تاريخ استحقاق الوديعة السابقة؛ التجديد يبدأ من تاريخ ربط الوديعة الجديدة فقط.
- تم ضبط تقارير الودائع `detailed` و`volume` لتعرض فقط الودائع النشطة داخل الفترة المختارة، مع أعمدة الحالة والملاحظات.
- تم ضبط بند `ودائع لأجل` في ميزان المراجعة ليعتمد على أصل الودائع النشطة عند نهاية الفترة، بما يمنع استمرار ظهور أصل الوديعة القديمة بعد الاستحقاق أو بعد التجديد.
- تم تعزيز دفتر الأستاذ لحساب `ودائع لأجل` بحيث يحتسب الودائع النشطة فقط، مع استمرار نقل قيود الودائع الافتتاحية إلى رصيد أول المدة بدلاً من إظهارها كسطر حركة.
- تم تحديث واجهة `DepositRegistration.jsx` بخيار `تم تجديد هذه الوديعة من وديعة سابقة` وحقل `ملاحظات التجديد`، وتم إصلاح تحذير HTML في خيارات القائمة.
- تم تحديث `DepositSummary.jsx` و`StatementsPage.jsx` لإظهار حالة الوديعة وملاحظات التجديد داخل تقارير الودائع فقط.
- تم إضافة اختبارات regression: `/app/backend/tests/test_iteration73_deposit_renewal_independent_asset.py`، وأضيف بواسطة testing agent: `/app/backend/tests/test_iteration74_deposit_renewal_journal_scope.py`.
- نتائج الاختبار: pytest محلي iteration73/71/72 = 4/4، testing agent iteration_70 = backend 5/5 وواجهة 95% ثم تم إصلاح تحذير الواجهة وإعادة Playwright بنجاح، وبعدها pytest iteration74/73/71 = 5/5.
- نجح `yarn craco build`، وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe` والتحقق من رابط التحميل.

## تعديل تقرير المصروفات — إجمالي لكل بنك بتاريخ 2026-05-07
- تم تعديل جدول `ExpensesPage.jsx` بإزالة عمود `البنك` من صفوف المصروفات التفصيلية داخل تقرير المصروفات.
- تم إضافة صفوف إجمالي في نهاية الجدول، صف مستقل لكل بنك، تعرض: إجمالي المبلغ الكلي، إجمالي الاستقطاعات، وإجمالي الصافي بناءً على `bank_id` المختار عند إضافة المصروف.
- تم الحفاظ على اختيار البنك داخل نموذج إضافة المصروف لأنه مصدر التجميع المطلوب، مع إبقاء البنك داخل إذن الصرف التفصيلي والبحث عند الحاجة.
- تم إضافة `data-testid` لصفوف التجميع: `expenses-bank-total-row-{bank_id}` وقيم الإجماليات.
- تم اختبار الواجهة بإنشاء 3 مصروفات مؤقتة موزعة على بنكين، والتحقق من اختفاء رأس عمود البنك وظهور صف إجمالي بنك التنمية الصناعية وبنك مصر، ثم تم حذف بيانات الاختبار وقيودها.
- نجح `yarn craco build` وpytest regression = 3/3، وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe` والتحقق من رابط التحميل.

## تحديث معادلة رصيد التسوية البنكية التلقائي بتاريخ 2026-05-07
- تم تعديل معادلة `/api/banks/{bank_id}/reconciliation-balance` حسب طلب المستخدم لتصبح: `الرصيد الافتتاحي + فوائد الودائع الشهرية داخل الفترة + شيكات لم تقدم للصرف - المصروفات البنكية الخاصة بالبنك خلال الفترة - إجمالي مصروفات البنك من صفحة المصروفات خلال الفترة - شيكات تحت التحصيل`.
- تم تعديل `monthly_expenses` ليعبر عن إجمالي مصروفات البنك خلال الفترة من صفحة المصروفات، بدون قصرها على حالة `paid` فقط، حتى تعمل معادلة إضافة الشيكات غير المقدمة ثم خصم إجمالي المصروفات كما في التسوية اليدوية.
- تم إبقاء `monthly_revenues` في استجابة الـAPI للتوافق فقط، لكنه لم يعد يدخل في رصيد التسوية التلقائي وفق المعادلة الجديدة.
- تم تحديث بطاقات `BankReconciliationPage.jsx` ونسخة الطباعة لعرض: الرصيد الافتتاحي، فوائد الودائع، شيكات لم تقدم، الإجمالي قبل الخصم، المصروفات البنكية، إجمالي مصروفات البنك، شيكات تحت التحصيل، إجمالي الخصومات، ورصيد التسوية.
- تم تحديث اختبار regression `/app/backend/tests/test_iteration72_bank_reconciliation_monthly_formula.py` للمعادلة الجديدة، ونجحت الاختبارات مع iteration74 = 3/3.
- تم اختبار الواجهة بـPlaywright ونجح ظهور بطاقات `شيكات لم تقدم للصرف` و`شيكات تحت التحصيل` و`رصيد التسوية`.
- نجح Smoke API للمعادلة الفعلية و`yarn craco build`، وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe` والتحقق من رابط التحميل.

## فصل بنود ميزان المراجعة: شيكات تحت التحصيل/أوامر الدفع/فوائد الودائع بتاريخ 2026-05-07
- تم إضافة حساب مستقل في دليل الحسابات: `4105 - إيرادات أوامر الدفع` بنوع `revenue` وطبيعة دائنة، حتى لا تندمج أوامر الدفع داخل `الإيرادات` العامة.
- تم تعديل محرك قيود الإيرادات بحيث تُرحّل طريقة التحصيل `payment_order` إلى حساب `إيرادات أوامر الدفع` بدلاً من حساب `الإيرادات` العام.
- تم الحفاظ على `شيكات تحت التحصيل` كحساب أصل مستقل مدين `1200`، وتظهر الشيكات غير المحصلة في ميزان المراجعة كبند أصل منفصل.
- تم إدراج فوائد الودائع الشهرية داخل ميزان المراجعة كبند مستقل ومتوازن: مدين `عوائد ودائع مستحقة` ودائن `إيرادات فوائد ودائع`، بدلاً من دمجها داخل إجمالي الإيراد.
- تم إضافة تصحيح تلقائي لقيود الإيرادات القديمة عند احتساب ميزان المراجعة، بحيث يعاد تصنيف أوامر الدفع/فوائد الجاري/استحقاق الوديعة إلى حساباتها المستقلة إذا كانت مُرحّلة سابقاً على حساب الإيرادات العام.
- تم توحيد فحص `data-flow-validation` مع بنود فوائد الودائع المولدة للفترة حتى يظل الفحص صحيحاً.
- تم إضافة اختبار regression: `/app/backend/tests/test_iteration75_trial_balance_revenue_breakdown.py` للتأكد من فصل `شيكات تحت التحصيل` و`إيرادات أوامر الدفع` و`إيرادات فوائد ودائع`.
- الاختبارات نجحت: iteration75 + iteration67 + iteration72 = 8/8، وSmoke API أكد ظهور الحسابات الثلاثة واستمرار `data-flow-validation=true`، وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe` والتحقق من رابط التحميل.

## تحديث التسوية البنكية — معادلة على مرحلتين بتاريخ 2026-05-07
- تم ضبط رصيد التسوية البنكية ليُحسب على مرحلتين:
  1. `الرصيد الافتتاحي + الإيرادات + فوائد الودائع - المصروفات البنكية - إجمالي مصروفات البنك = الرصيد قبل تسويات الشيكات`.
  2. `الرصيد قبل تسويات الشيكات + شيكات لم تقدم للصرف - شيكات تحت التحصيل = رصيد التسوية البنكية النهائي`.
- تم تعديل `/api/banks/{bank_id}/reconciliation-balance` ليعيد `book_balance` كرصد المرحلة الأولى، و`reconciliation_balance` كرصد نهائي بعد تسويات الشيكات.
- تم تحديث واجهة `BankReconciliationPage.jsx` ونسخة الطباعة لإظهار المرحلة الأولى بوضوح: الإيرادات، فوائد الودائع، المصروفات البنكية، مصروفات البنك، ثم مرحلة تسويات الشيكات.
- تم تحديث اختبار `/app/backend/tests/test_iteration72_bank_reconciliation_monthly_formula.py` للمعادلة ذات المرحلتين.
- الاختبارات نجحت: iteration72 + iteration75 + iteration67 = 8/8، ونجح Playwright لواجهة التسوية وSmoke API للمعادلة، وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe` والتحقق من رابط التحميل.

## تصحيح عاجل لميزان المراجعة — إخفاء أصل الودائع وتصحيح 3100 بتاريخ 2026-05-07
- تم إزالة التعديل السابق الذي كان يحقن إجمالي أصل الودائع النشطة مباشرة داخل ميزان المراجعة على حساب `1250 - ودائع لأجل` بدون قيد مقابل، وهو سبب تضخم الأصول.
- تم تعديل ميزان المراجعة ليخفي قيود أصل الودائع لأجل من الفترات العادية، ولا يتم تحميل أصل الوديعة على `3100 - رصيد افتتاحي` لمجرد وجود أصل وديعة قائم.
- تم استبعاد قيود أصل الوديعة من احتساب ميزان المراجعة والتدفق في الفترات العادية، مع الإبقاء على فوائد الودائع كإيراد مستقل كما طلب المستخدم سابقاً.
- تم التحقق عملياً أن `1250 - ودائع لأجل` لا يظهر في ميزان مراجعة يناير مع `non_zero_only=true`، وأن `data-flow-validation=true` للجهتين.
- تم تحديث اختبار التجديد `test_iteration73_deposit_renewal_independent_asset.py` ليثبت أن أصل الودائع لا يظهر في ميزان المراجعة للفترات العادية.
- الاختبارات نجحت: iteration73 + iteration75 + iteration67 + iteration72 = 9/9، وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe` والتحقق من رابط التحميل.

## مراجعة شاملة للتدفقات وخاصة التسوية البنكية بتاريخ 2026-05-07
- تم اكتشاف وتصحيح نقطة مهمة في معادلة التسوية: كانت `monthly_revenues` تحتسب الإيرادات المحصلة فقط، وبالتالي كان الشيك تحت التحصيل يُخصم في المرحلة الثانية دون أن يكون مدرجاً أولاً ضمن الإيرادات. تم تعديلها لتشمل كل إيرادات البنك داخل الفترة أولاً، ثم يُخصم `checks_under_collection` في مرحلة تسويات الشيكات.
- تم تأكيد أن المصروفات تعمل بالطريقة المقابلة: كل مصروفات البنك داخل الفترة تدخل أولاً ضمن `monthly_expenses`، ثم تُضاف `checks_not_presented` في مرحلة تسويات الشيكات.
- تم تثبيت معادلة التسوية النهائية: `book_balance = opening_balance + monthly_revenues + monthly_deposit_interest - bank_expenses - monthly_expenses` ثم `reconciliation_balance = book_balance + checks_not_presented - checks_under_collection`.
- تم تصحيح عناوين نسخة الطباعة: `شيكات لم تقدم للصرف = يضاف` و`شيكات تحت التحصيل = يخصم`.
- تم تشغيل مراجعة ذاتية موسعة: اختبارات iteration72/67/75/73 = 9/9، وباقة التسوية البنكية الأوسع = 15 passed / 12 skipped، وPlaywright smoke لواجهة التسوية نجح.
- تم استدعاء testing agent لمراجعة مستقلة للتسوية وميزان المراجعة والتدفقات الأساسية؛ النتيجة iteration_71: Backend 100% وFrontend 100%، ولا توجد عيوب Blocking، مع تأكيد أن حفظ مذكرة التسوية لا يطبق الشيكات مرتين.
- أنشأ testing agent اختبارات إضافية للمراجعة: `test_iteration71_core_smoke_endpoints.py`, `test_iteration71_auth_playbook_checks.py`, `test_iteration71_reconciliation_save_integrity.py` وتقارير pytest الخاصة بها.
- تم بناء الواجهة بنجاح، وتحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe`، والتحقق من رابط التحميل بنجاح.

## تعديل التسوية البنكية — الرصيد التلقائي قبل تسويات الشيكات بتاريخ 2026-05-07
- بناءً على توضيح المستخدم، تم تعديل معنى حقل `رصيد التسوية البنكية التلقائي` ليكون هو نفس `الرصيد قبل تسويات الشيكات` وليس الرصيد النهائي بعد الشيكات.
- تم تعديل واجهة `BankReconciliationPage.jsx` بحيث تعرض في حقل الرصيد المقروء فقط قيمة `book_balance` من breakdown، مع تسمية واضحة: `رصيد التسوية البنكية التلقائي قبل تسويات الشيكات`.
- تم تعديل `calculate_reconciliation` بحيث يحسب الرصيد النهائي المحفوظ `calculated_balance = book_balance + total_outstanding_checks - total_collection_checks`، أي تطبيق الشيكات مرة واحدة بعد الرصيد التلقائي.
- تم تعديل إنشاء/تعديل مذكرة التسوية ليحفظ `book_balance = balance_breakdown.book_balance`، وليس `reconciliation_balance`، ثم يطبق الشيكات المسجلة في النموذج للوصول إلى الرصيد النهائي.
- تم تحديث ملخص الواجهة ونسخة الطباعة لتفرق بين `الرصيد التلقائي قبل الشيكات` و`الرصيد النهائي بعد تسويات الشيكات`.
- تم تحديث اختبارات `test_iteration71_reconciliation_save_integrity.py` و`test_iteration63_reconciliation_auto_balance.py` لتثبيت السلوك الجديد.
- الاختبارات نجحت: iteration72 + iteration71 + iteration63 + iteration67 = 15/15، ونجح Playwright للواجهة، وSmoke حفظ مذكرة تسوية مع شيكات ثم حذفها بنجاح.
- تم بناء الواجهة وتحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe` والتحقق من رابط التحميل.

## تعديل طباعة مذكرة التسوية البنكية بتاريخ 2026-05-07
- تم حذف سطر اسم الجهة/النظام من مكوّن طباعة مذكرة التسوية البنكية، وليس إخفاؤه فقط، حتى لا يظهر في PDF أو Word أو الطباعة.
- تم حذف بلوك تفصيل رصيد التسوية البنكية من تقرير الطباعة بالكامل: الرصيد الافتتاحي، الإيرادات، فوائد الودائع، إجمالي الزيادة، المصروفات البنكية، مصروفات البنك، الرصيد قبل الشيكات، الشيكات، ورصيد التسوية.
- بقيت البيانات التفصيلية ظاهرة في شاشة العمل فقط داخل قسم `تفصيل رصيد التسوية البنكية التلقائي`، لكنها لا تظهر في تقرير الطباعة/التصدير.
- تم التحقق بالكود من عدم بقاء `reconciliation-print-organization` و`print-balance-breakdown` داخل صفحة التسوية، ونجح lint وpytest التسوية 2/2 وPlaywright smoke.
- نجح `yarn craco build` وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe` والتحقق من رابط التحميل.

## احتواء طباعة مذكرة التسوية في صفحة واحدة بتاريخ 2026-05-07
- تم إضافة قواعد CSS خاصة بطباعة `reconciliation-print-report` باسم صفحة طباعة `reconciliation-single-page` بهوامش A4 صغيرة وضغط مخصص للخطوط والجداول والشعار.
- تم تقليل أحجام الخطوط والحشو داخل جداول الشيكات وحالة المطابقة، مع استخدام `zoom: 0.82` في وضع الطباعة لضمان احتواء مذكرة كل شهر في صفحة واحدة قدر الإمكان بدون حذف أقسام التقرير.
- تم تحديث CSS التصدير في `ExportReportButtons.jsx` حتى يستفيد تصدير Word/PDF من نفس الضغط الخاص بمذكرة التسوية.
- تم اختبار مذكرة تسوية فعلية مؤقتة لسنة 2029 ببيانات شيكات، ثم معاينتها في وضع `print` والتحقق أن التقرير يُعرض مضغوطاً داخل صفحة واحدة، ثم تم حذف مذكرة الاختبار.
- نجح lint وpytest التسوية 2/2 و`yarn craco build`، وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe` والتحقق من رابط التحميل.

## إضافة المخزون والدائنين والدراسات التحليلية وتصفية القيود بتاريخ 2026-05-08
- تم إضافة دفتر المخزون كدفتر حركة محاسبي مستقل داخل التدفق: `وارد/منصرف/رصيد لكل صنف` مع تحديث الكمية والقيمة ومتوسط التكلفة تلقائياً.
- كل حركة مخزون تنشئ قيداً تلقائياً: الوارد يرحل `مدين المخزون / دائن دائنون متنوعون`، والمنصرف يرحل `مدين منصرف مخزون / دائن المخزون`، مع منع صرف كمية أكبر من الرصيد.
- تم إضافة حسابات دليل الحسابات: `1700 - المخزون` ضمن الأصول، `2200 - دائنون متنوعون` ضمن الخصوم، و`5104 - منصرف مخزون` ضمن المصروفات.
- تم إضافة دفتر الدائنين المتنوعين: إثبات الالتزام يرحل `مدين المصروفات / دائن دائنون متنوعون`، والسداد يرحل `مدين دائنون متنوعون / دائن البنك` مع اشتراط اختيار البنك ومنع سداد أكبر من الرصيد.
- تم إضافة صفحات الواجهة: `/inventory` و`/misc-creditors` وربطها بالصفحة الرئيسية، مع نماذج إدخال وحركة تفصيلية وفلاتر تاريخ/صنف/دائن.
- تم تطوير صفحة القيود اليومية بإضافة فلاتر مرئية: `صنف القيد` و`نوع القيد`، ودعم الفلترة مع الفترة المالية والمصدر، وعرض أعمدة: التاريخ، رقم القيد، البيان، الحساب المدين، الحساب الدائن، المبلغ، حالة الترحيل. الشاشة للعرض فقط ولا تحتوي تعديل.
- تم إضافة API للتصنيفات `/api/journal-entry-classifications`، وتم توسيع `/api/journal-entries` ليدعم `entry_category` و`entry_item` مع عرض القيود المرحلة فقط.
- تم إضافة صفحة `دراسة جدوى` مستقلة `/feasibility-study` لا تنشئ قيوداً، وتحسب صافي الربح المتوقع ونقطة التعادل وفترة الاسترداد وتدعم الطباعة.
- تم إضافة صفحة `دراسة اكتوارية` مستقلة `/actuarial-study` لا تنشئ قيوداً، وتحسب التدفقات والالتزامات ومعدل كفاية الصندوق وتدعم الطباعة.
- تم تحديث الصفحة الرئيسية والمسارات وتعريفات الوحدات لإظهار: دفتر المخزون، الدائنون المتنوعون، دراسة جدوى، دراسة اكتوارية.
- تم ضبط ميزان المراجعة بحيث لا يظهر `1250 - ودائع لأجل` حتى كصف صفري في الاستجابة الخام للفترات العادية، استجابة لملاحظة الاختبار والمستخدم.
- الاختبار الذاتي نجح: إنشاء وارد/منصرف مخزون وإثبات/سداد دائن، التحقق من القيود والتصفية وميزان المراجعة ثم تنظيف البيانات. ونجح lint كامل + build + Playwright smoke للصفحات الجديدة.
- Testing agent iteration_72 نجح Backend/Frontend، وأنشأ اختبار regression `/app/backend/tests/test_iteration72_inventory_misc_creditors_flow.py` بنتيجة 8/8، مع ملاحظتين تم إصلاحهما: إضافة تسميات `صنف القيد/نوع القيد` وإخفاء 1250 من API الخام.
- بعد الإصلاحات نجحت اختبارات: inventory/misc creditors + trial balance + deposit renewal = 10/10، وتم التحقق من `data-flow-validation=true`.
- تم بناء الواجهة وتحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe` والتحقق من رابط التحميل.

## نقل الدراسات إلى بوابة مستقلة في الصفحة الرئيسية بتاريخ 2026-05-08
- تم إضافة كارت جديد في الصفحة الرئيسية باسم `الدراسات والتحليلات` بجانب `الحسابات` و`العضوية`.
- تم نقل صفحتي `دراسة جدوى` و`دراسة اكتوارية` من داخل قائمة الحسابات إلى صفحة مستقلة `/studies`.
- أصبحت صفحة `/studies` تعرض فقط كارتَي: `دراسة جدوى` و`دراسة اكتوارية`، مع استمرار الروابط المباشرة `/feasibility-study` و`/actuarial-study` للعمل كما هي.
- تم تحديث عنوان الصفحة في المتصفح إلى `الدراسات والتحليلات` عند فتح `/studies`.
- تم اختبار الواجهة بـPlaywright: الصفحة الرئيسية تعرض الحسابات/العضوية/الدراسات، ولا تعرض دراسة جدوى مباشرة، وصفحة الدراسات تعرض دراسة جدوى ودراسة اكتوارية فقط.
- نجح lint و`yarn craco build`، وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe` والتحقق من رابط التحميل.

## إضافة صفحة الخزينة والبنوك للعرض فقط بتاريخ 2026-05-08
- تم إضافة صفحة جديدة داخل بوابة الحسابات باسم `الخزينة والبنوك` على المسار `/treasury-banks`، وهي صفحة مراقبة وتحليل فقط بدون أي أزرار إضافة أو حفظ أو تعديل أو حذف.
- تم إضافة endpoint قراءة فقط `/api/treasury-banks` يعتمد حصراً على `journal_entries` المرحلة وغير العكسية، ولا يكتب في `journal_entries` أو `reconciliations` أو `bank_settings` أو أي جداول أرصدة.
- التقرير يحصر الحسابات في حسابات البنوك `system_key=bank:*` وحساب الخزينة `system_key=cash_box` فقط، ويعرض بطاقات: إجمالي الرصيد، إجمالي الإيرادات، إجمالي المصروفات، وصافي الحركة.
- جدول الحركات يعرض: التاريخ، رقم القيد، البيان، نوع الحركة، الحساب، المبلغ، والرصيد التراكمي، مع فلاتر للفترة ونوع الحساب والحساب ونوع الحركة، وبحث نصي، ومعاينة وطباعة PDF.
- تم ربط كارت `الخزينة والبنوك` داخل بوابة `الحسابات`، وتحديث تعريفات الوحدات والمسارات وعنوان الصفحة.
- تم الحفاظ على استقلال الصفحة عن التسوية البنكية؛ لا يتم استدعاء منطق حفظ التسويات ولا تعديل معادلات التسوية الحالية.
- الاختبار الذاتي نجح: API أعاد 200، الواجهة فتحت من بوابة الحسابات، القوائم والمعاينة تعمل، و`yarn craco build` ناجح.
- Testing agent iteration_73 نجح Backend 100% وFrontend 95% ثم تم إصلاح تحذير القوائم المنسدلة وإعادة اختبار واجهة مستهدف بنجاح دون تحذيرات مؤثرة.
- تم تحديث ملف التثبيت Windows في `/app/dist/BankDepositSystemSetup.exe` والتحقق من رابط `/api/download/setup` بطلب GET ناجح.

## إضافة بوابة مطبوعات بنكية محدثة لحظياً بتاريخ 2026-05-08
- تم إضافة بوابة جديدة في الصفحة الرئيسية باسم `مطبوعات بنكية` على المسار `/bank-prints`، وتعرض البنوك الموجودة بالبرنامج مع تفعيل صفحة بنك مصر حالياً.
- تم إضافة صفحة `مطبوعات بنك مصر` على المسار `/bank-prints/banque-misr` بتدفق: `External Data Fetch → Pre-Print Form → Merge Engine → Print Generator → Requests Archive`.
- تم إضافة endpoint `/api/bank-prints/banque-misr/external-data` لجلب بيانات حقيقية مباشرة من `https://www.banquemisr.ae/#tab-2` وتحويلها إلى جدول داخلي، بدون MOCK وبدون تخزين تلقائي.
- تم إضافة أرشيف طلبات الطباعة في `bank_print_requests` مع endpoints للحفظ قبل الطباعة، عرض الأرشيف، عرض طلب منفرد، وتحديث الحالة إلى `printed/cancelled`؛ الأرشيف يحفظ البيانات الخارجية واليدوية ووقت الإنشاء والحالة.
- تم التأكيد أن المطبوعات البنكية لا تكتب في `journal_entries` أو `reconciliations` أو `bank_settings` ولا تؤثر على الأرصدة أو التسويات البنكية.
- تم إضافة نموذج ما قبل الطباعة: ملاحظات البنك، أرقام الشيكات أو التسويات، تعديلات وصفية، اختيار الفترة، اسم الاعتماد الداخلي، التوقيع/الكود، وكود الاعتماد.
- تم إضافة مولد طباعة PDF من نافذة مستقلة يدمج البيانات الخارجية واليدوية، مع Timestamp ومصدر البيانات ورقم الطلب وحالة الطلب.
- تم تحصين نموذج الطباعة ضد حقن HTML داخل التقرير، وإضافة إعادة محاولة لجلب المصدر الخارجي لتقليل فشل المواقع البطيئة.
- الاختبار الذاتي نجح: external-data أعاد 18 بنداً، حفظ طلب طباعة تجريبي تم بنجاح ثم تنظيفه، الواجهة فتحت البوابة وصفحة بنك مصر وأظهرت البيانات الخارجية.
- Testing agent iteration_74 نجح Backend/Frontend 100%، وتأكد من read-only safety وعدم وجود تأثير على القيود أو التسويات أو أرصدة البنوك، ثم تم تنظيف طلبات الاختبار.
- تم بناء الواجهة وتحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe` والتحقق من رابط `/api/download/setup` بطلب GET ناجح.

## إلغاء مطبوعات بنكية وإضافة ERP Health Report بتاريخ 2026-05-08
- تم إلغاء بوابة `مطبوعات بنكية` من الصفحة الرئيسية، وإزالة مسارات الواجهة `/bank-prints` و`/bank-prints/banque-misr` وتعطيل API الخاص بها ليعيد 404.
- تم تنظيف أرشيف `bank_print_requests` من أي طلبات اختبارية، وإزالة `bank_print_requests` من قوائم النسخ الاحتياطي والتفريغ بعد الإلغاء.
- تم إضافة بوابة جديدة في الصفحة الرئيسية باسم `تقرير تقييم النظام` على المسار `/erp-health-report`.
- تم إضافة endpoint قراءة فقط `/api/erp-health-report` يولد تقرير ERP Health ويعيد: `overall_score` من 100، مؤشرات التقييم، التوصيات، ورابط تحميل مباشر PDF.
- تم إضافة `/api/erp-health-report/pdf` لتحميل PDF مباشر بتوكن مستخدم، و`/api/erp-health-report/files/{report_id}` كرابط تحميل مباشر عام للتقرير المُنشأ فقط.
- التقرير يحلل من قواعد البيانات فقط بدون أي كتابة في: `journal_entries` أو `reconciliations` أو `bank_settings` أو أرصدة البنوك.
- المؤشرات المضافة: دقة القيود، اتساق ميزان المراجعة، كفاءة التسويات البنكية، سرعة معالجة العمليات، تكامل الموديولات، الاعتماد الخارجي، استقرار البيانات، Audit Completeness، وزمن إنشاء التقرير.
- PDF يتضمن درجة إجمالية، جدول مؤشرات، توصيات تلقائية، ونص تأكيدي أن العملية Read Only ولا تؤثر على القيود أو التسويات.
- تم تثبيت `reportlab` لتوليد PDF من الباك إند وتحديث `requirements.txt` عبر `pip freeze`.
- الاختبار الذاتي نجح: توليد تقرير بدرجة 88/100 ورابط مباشر يعمل 200، واختفاء كارت مطبوعات بنكية وظهور كارت تقرير تقييم النظام.
- Testing agent iteration_75 نجح: Backend 100% وFrontend 95%، وتحقق من أن توليد التقرير لا يغير القيود أو التسويات أو ميزان المراجعة أو أرصدة البنوك.
- تم بناء الواجهة وتحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe` والتحقق من رابط `/api/download/setup` بطلب GET ناجح.

## تحسين PDF تقرير ERP Health للغة العربية الكاملة بتاريخ 2026-05-08
- تم إعادة تصميم مولد PDF الخاص بتقرير ERP Health ليستخدم تخطيط عربي RTL واضح بالكامل دون تغيير بيانات التقييم أو درجات التقرير.
- تم تثبيت وإرفاق خط عربي `Amiri-Regular.ttf` داخل `app_assets` وتسجيله في ReportLab ليكون مدمجاً داخل ملف PDF نفسه، مع fallback لخطوط النظام عند الحاجة.
- تم عكس ترتيب أعمدة جدول المؤشرات ليتوافق بصرياً مع اتجاه القراءة العربي: البند في اليمين، ثم Score، ثم الحالة، ثم التفاصيل.
- تم ضبط أنماط العنوان والدرجة والجداول والتوصيات بمحاذاة عربية وwordWrap RTL وتقليل احتمالات ظهور رموز غير مفهومة.
- تم التحقق برمجياً أن ملف PDF يحتوي خطاً مدمجاً: `/AAAAAA+Amiri-Regular` مع `FontFile` مرفق.
- تم تحليل PDF الناتج وتأكيد أن النص العربي مقروء، بدون مربعات أو رموز غريبة، والجداول والعناوين منظمة RTL.
- الاختبار الذاتي نجح: التقرير احتفظ بنفس الدرجة 88/100 ونفس عدد المؤشرات 9، والرابط المباشر للتحميل يعمل 200.
- تم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe` مع تضمين خط Amiri داخل حزمة التثبيت، والتحقق من رابط `/api/download/setup`.

## Reset Test Environment وSeed Clean Dataset وإصلاح ERP تدريجي بتاريخ 2026-05-08
- تم تنفيذ تحليل Read Only أولاً قبل أي تعديل، ووجد: 2 حساب غير معروف داخل قيود اختبارية، 4 روابط مصدر مفقودة، 0 قيود غير متوازنة، و0 اعتماد خارجي مؤثر.
- تم حفظ نسخة احتياطية للبيانات قبل reset في `/app/generated_reports/erp-reset-backup-ed3814c7-e536-4a4f-98eb-154e49d167fe.json`.
- تم تنفيذ Reset Test Environment للبيانات التشغيلية الاختبارية القديمة، مع تسجيل كل خطوة تعديل داخل `audit_logs` على المسار `/system/erp-reset-correction`.
- تم Seed Dataset نظيفة تحتوي على 5 قيود نشطة ومترابطة: افتتاحي، وديعة بنك مصر، عائد وديعة، إيراد، ومصروف.
- تم إنشاء مستندات تشغيل نظيفة مرتبطة مباشرة بالقيود: `deposits=1`، `revenues=1`، `expenses=1`.
- تم إصلاح استرجاع المصروفات تاريخياً بإضافة معالجة دفاعية في `hydrate_expense` لحساب `total_deductions` و`net_amount` عند غيابها، وتم backfill للمصروف النظيف.
- تم تعديل عرض `/api/journal-entries` ليشمل `deposit_interest` عند عرض كل القيود، مع بقاء استبعاده من التقارير الحسابية التي تستخدم `REPORT_EXCLUDED_SOURCE_TYPES` لحماية الميزان/الأستاذ من ازدواجية العرض.
- تم ربط الوديعة النظيفة ببنك مصر والتحقق من ظهورها داخل ودائع بنك مصر.
- الاختبار الذاتي بعد الإصلاح: `/api/journal-entries` يعرض 5 قيود بالأنواع المطلوبة، ميزان المراجعة 2026 متوازن، وERP Health Score أصبح 93.
- Testing agent iteration_76 وجد مشكلتين بعد reset؛ تم إصلاحهما، ثم نجح regression test `/app/backend/tests/test_iteration82_erp_reset_seed_health_report.py` بنتيجة 9/9 passed.
- تقرير الأخطاء النهائي المباشر: `https://interest-calculator-12.preview.emergentagent.com/api/erp-health-report/files/341f5ddb-b473-4882-bf0f-3b9833f03914`.
- تم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe` والتحقق من رابط التقرير النهائي 200.

## بناء Tax Engine ديناميكي متوافق مع ETA Ready بتاريخ 2026-05-08
- تم تحويل الفاتورة الإلكترونية إلى Tax Engine هجين Config-Driven يعمل حالياً بدون ETA API حي، مع تصميم قابل لإضافة API Layer والتوقيع الإلكتروني والإرسال لاحقاً دون إعادة هيكلة.
- تم إضافة Tenant Tax Profile مستقل لكل جهة في `tax_profiles` يشمل: رقم التسجيل الضريبي، اسم الجهة، العملة، بيئة ETA، قواعد الضرائب، أكواد أنواع المستندات، حسابات القيود، وETA payload schema.
- تم منع الاعتماد على نسب ضريبية ثابتة داخل منطق الحساب؛ الضريبة تُحسب من `tax_rules` المخزنة في قاعدة البيانات لكل Tenant، وتم حفظ Profile التكافل الحالي كنموذج تشغيل: VAT 14% من الإعدادات وليس من الكود.
- تم تفعيل موديول `electronic_invoice` لجهة `social-solidarity` على مستوى `default_modules_for_organization` وقاعدة `organizations/users` مع Audit Log، حتى تظهر الفاتورة الإلكترونية/Tax Engine داخل بوابة الحسابات.
- تم إضافة endpoints: `GET/PUT /api/tax-engine/profile`، `POST /api/tax-engine/invoices`، `POST /api/tax-engine/invoices/{id}/approve`، و`POST /api/tax-engine/invoices/generate-from-operations` لدعم فواتير البيع والشراء.
- عند اعتماد الفاتورة يتم إنشاء قيد يومية تلقائي `source_type=tax_invoice`، وتحديث الفاتورة بـ `journal_entry_id`، وإنشاء Tax Invoice Entity مستقلة في `tax_invoices` مرتبطة بالفاتورة والقيد.
- تم إضافة `tax_engine_snapshot` داخل الفاتورة لحفظ القواعد المستخدمة والأكواد وdocument type وETA schema، بما يدعم مراجعة الحساب لاحقاً وربط ETA المستقبلي.
- تم تحديث واجهة `ElectronicInvoicePage` بإعدادات Tax Profile، قاعدة ضريبية افتراضية، حسابات القيود من الإعدادات، أكواد المستندات، Schema مستقبلي لـ ETA، وأزرار توليد فواتير بيع/شراء واعتماد وربط.
- تم عزل ETA Integration Settings بالجهة باستخدام `with_organization` بدلاً من إعدادات عامة، وتحويل payload preview ليقرأ أكواد المستندات والأصناف من snapshot/config.
- تم تسجيل عمليات الحفظ والإنشاء والاعتماد والتنظيف في `audit_logs` على المسار `/system/tax-engine`.
- Testing agent iteration_77 كشف أن الموديول غير مفعل للتكافل؛ تم إصلاح مصدر التفعيل، ثم نجح regression test `/app/backend/tests/test_iteration83_tax_engine_profile_and_invoices.py` بنتيجة 7/7 passed.
- تم تنظيف كل بيانات الاختبار المعلّمة `TEST_TAX_ENGINE_AUTOMATION_DELETE_ME` من `electronic_invoices` و`tax_invoices` و`journal_entries` حتى لا تؤثر على بيانات التشغيل.
- الاختبار الذاتي النهائي: `GET /api/tax-engine/profile` يعيد `is_configured=true` وبدون أخطاء، وملف التثبيت `/app/dist/BankDepositSystemSetup.exe` تم تحديثه والتحقق من رابط `/api/download/setup` 200.
- ملاحظة: Tax Engine الداخلي ليس MOCKED، لكن إرسال ETA API الحي ما زال **MOCKED/CONFIGURATION_REQUIRED** لحين إدخال بيانات التكامل والتوقيع الإلكتروني.

## ضبط ظهور الفاتورة الإلكترونية حسب جهة السوبر أدمن بتاريخ 2026-05-08
- تم تعديل `default_modules_for_organization` بحيث لا تكون `electronic_invoice` مفعلة تلقائياً للجهات الجديدة؛ يجب تفعيلها صراحة من إعدادات الخواص.
- تم تحديث endpoints `GET/PUT /api/admin/organization/modules` لقبول `organization_id` كسؤال اختياري للسوبر أدمن فقط، مع منع أي أدمن عادي من تعديل جهة أخرى.
- تم إضافة سجل تدقيق `ORGANIZATION_MODULES_UPDATED` عند حفظ إعدادات الخواص، ويشمل before/after modules للجهة المستهدفة.
- تم تعديل صفحة الأدمن `إعدادات الخواص` بإضافة اختيار جهة `feature-settings-organization-select` للسوبر أدمن، بحيث يمكن تفعيل الفاتورة الإلكترونية للنقابة العامة وتعطيلها لمشروع التكافل أو العكس.
- الحالة الحالية وفق مثال المستخدم: `general-union.electronic_invoice=true` و`social-solidarity.electronic_invoice=false`.
- تم التحقق ذاتياً: النقابة العامة تعرض كارت الفاتورة الإلكترونية و`/api/tax-engine/profile` يعيد 200، بينما مشروع التكافل لا يعرض الكارت و`/api/tax-engine/profile` يعيد 404.
- Testing agent iteration_78 نجح بنسبة 100%: backend 8/8، وتحقق من الواجهة، عزل الجهات، منع cross-org لغير السوبر أدمن، وعدم تفعيل الفاتورة تلقائياً للجهات الجديدة.
- تم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe` والتحقق من رابط `/api/download/setup` بطلب GET ناجح.

## تقييد تقرير تقييم النظام وتحسين معاينة طباعة التسوية البنكية بتاريخ 2026-05-08
- تم تقييد `تقرير تقييم النظام` للسوبر أدمن فقط على الواجهة: لا يظهر كارت `module-erp-health-report-card` إلا إذا كان `user.role === super_admin`.
- تم تقييد Route `/erp-health-report` عبر `SuperAdminRoute`، وتقييد APIs `/api/erp-health-report` و`/api/erp-health-report/pdf` و`/api/erp-health-report/files/{id}` باستخدام `require_super_admin`.
- تم تحديث تصميم معاينة/طباعة التسوية البنكية إلى مذكرة شهرية احترافية في صفحة واحدة A4، تشمل: اسم الجهة، اسم البنك، الشهر، السنة، تاريخ الإعداد، عنوان `مذكرة التسوية الشهرية`، جدول معادلة التسوية، وجزء توقيعات.
- تم إضافة زر `طباعة هذه المعاينة` داخل معاينة التسوية حتى يتمكن المستخدم من رؤية الشكل أولاً قبل اعتماد/استخدام الطباعة النهائية.
- تم تحديث CSS الطباعة لضمان صفحة واحدة `reconciliation-single-page` وإخفاء عناصر الواجهة غير المطلوبة مثل النموذج والتاريخ والأزرار أثناء الطباعة.
- تم اختبار ذاتي: السوبر أدمن يحصل على تقرير ERP Health 200، والرابط المباشر بدون توكن يعيد 401، ومعاينة التسوية تعرض الجهة/البنك/الشهر/السنة.
- Testing agent iteration_79 نجح بنسبة 100%: backend 5/5 وfrontend 100%، وتحقق من تقييد التقرير للسوبر أدمن ومن عناصر معاينة التسوية وجدول المعادلة وCSS الطباعة.
- تم حذف مذكرة التسوية الاختبارية التي أنشأها اختبار المعاينة وتسجيل cleanup audit، ثم التحقق من عدم بقاء بيانات اختبارية.
- تم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe` والتحقق من رابط `/api/download/setup` بطلب GET ناجح.

## إصلاح طباعة مذكرة التسوية إلى ورقة واحدة بنفس نموذج الصورة بتاريخ 2026-05-08
- تم تعديل شكل مذكرة التسوية ليحاكي الصورة المرسلة: شعار البنك أعلى اليسار، اسم الجهة في الأعلى، البنك والشهر/السنة في المنتصف، الرصيد في اليمين، أقسام `يضاف` و`يخصم` بجداول بسيطة، وتذييل حالة الرصيد.
- تم استبدال طباعة زر `طباعة هذه المعاينة` بنافذة طباعة مستقلة تحتوي على المذكرة فقط، بحجم A4 ثابت وارتفاع مضبوط و`overflow:hidden` لضمان عدم خروجها إلى 3 أوراق.
- تم ربط زر PDF العلوي `print-reconciliation-pdf-button` بنفس محتوى المذكرة عبر `printSelectors` بدلاً من طباعة الصفحة كاملة.
- تم إضافة CSS print override يخفي كل عناصر صفحة التسوية ما عدا `reconciliation-print-report` عند استخدام طباعة المتصفح، ويثبت المذكرة على صفحة واحدة A4.
- تم اختبار المعاينة ذاتياً باستخدام مذكرة مؤقتة، ثم حذف كل بيانات الاختبار المؤقتة والتحقق من عدم بقاء أي reconciliation test records.
- تم تشغيل lint و`yarn craco build` بنجاح، وتحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe` والتحقق من رابط `/api/download/setup`.

## إصلاح ظهور لوجو البنك في طباعة مذكرة التسوية بتاريخ 2026-05-08
- تم تعديل نافذة الطباعة المستقلة لمذكرة التسوية بحيث تستخدم شعار البنك من `bank.logo_url` أو من الشعارات الافتراضية `bankPalette` عند عدم وجود شعار محفوظ في قاعدة البيانات.
- تم تحويل مسار الشعار إلى رابط مطلق داخل نافذة الطباعة، لأن نافذة `about:blank` لا تعرض المسارات النسبية أحياناً أثناء الطباعة.
- تم إضافة انتظار تحميل الصور قبل استدعاء `print()` لضمان ظهور الشعار داخل الطباعة، مع fallback يعرض اسم البنك داخل مربع إذا فشل تحميل الصورة.
- تم تحديث `ExportReportButtons` لتحويل كل صور التقرير إلى روابط مطلقة عند استخدام زر PDF/Word/Excel، حتى يظهر اللوجو أيضاً من زر الطباعة العلوي.
- الاختبار الذاتي نجح: شعار بنك مصر ظهر في معاينة التسوية، ومصدر الصورة `/assets/bank-logos/banque-misr.svg` تم تحميله، ثم تم حذف تسوية اختبار اللوجو والتحقق من عدم بقاء بيانات اختبارية.
- تم تشغيل lint و`yarn craco build` بنجاح، وتحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe` والتحقق من رابط `/api/download/setup`.

## تشغيل صامت للبرنامج مع Splash Screen والشعار الرسمي بتاريخ 2026-05-08
- تم اعتماد الصورة المرفوعة `ERP.png` كشعار رسمي للنظام، ونسخها إلى `/app/app_assets/erp-official-logo.png` و`/app/frontend/public/assets/branding/erp-official-logo.png` دون تعديل التصميم.
- تم توليد أيقونة الاختصار `accounting_app.ico` من نفس الشعار الرسمي لأنه مربع 1024x1024، لاستخدامه في اختصارات Windows.
- تم إضافة مشغّل صامت `launch_bank_deposit_system.vbs` يفتح `splash.hta` بدلاً من تشغيل ملف Batch مباشرة.
- تم إضافة `splash.hta` بشاشة تشغيل احترافية تعرض الشعار الرسمي، Progress Bar، ورسائل حالة: تهيئة النظام، فحص قاعدة البيانات، التحقق من الترخيص، تشغيل الموديولات، وفتح الواجهة الرئيسية.
- تم إضافة `start_system_worker.bat` لتنفيذ تهيئة البيئة وتشغيل الخدمات في الخلفية، مع إخفائه عبر HTA/VBS وكتابة السجلات فقط إلى `%LOCALAPPDATA%\BankDepositSystem\logs`.
- تم إضافة `run_backend_hidden.vbs` وتشغيل `run_backend_server.bat` بشكل مخفي، وتعديل ملفات البات لإزالة `pause` وعدم عرض CMD عند التشغيل من الاختصار.
- تم تحديث NSIS بحيث تختصرات سطح المكتب وقائمة Start تشغّل `wscript.exe launch_bank_deposit_system.vbs` بدلاً من `.bat`، لمنع ظهور Command Prompt للمستخدم.
- تم استخدام الشعار الرسمي داخل واجهة البرنامج أيضاً في شاشة الدخول `login-official-logo-image` والصفحة الرئيسية `module-selection-official-logo`.
- تم بناء الواجهة والمثبت بنجاح، والتحقق من وجود كل ملفات التشغيل الصامت داخل `/app/release/BankDepositSystem` ومن رابط `/api/download/setup` 200 وحجم الملف مطابق.
- Testing agent iteration_80 نجح: regression suite `/app/backend/tests/test_iteration86_windows_silent_packaging.py` بنتيجة 9/9، وفحص الواجهة والشعار وملفات NSIS والتشغيل الصامت بنجاح.
- ملاحظة اختبارية: عدم ظهور نافذة CMD فعلياً يحتاج تأكيد نهائي على جهاز Windows حقيقي، لأن بيئة الاختبار الحالية Linux؛ تم التحقق static/integration من سلسلة التشغيل الصامت.

## توسيط عناصر Splash Screen بتاريخ 2026-05-08
- تم تعديل `splash.hta` ليستخدم تخطيط Flex مركزي بالكامل داخل النافذة، بحيث يتم توسيط الشعار والعنوان ورسائل الحالة وشريط التحميل أفقياً ورأسياً.
- تم ضبط حجم بطاقة الشعار إلى 300x300 وتثبيت `object-position:center center` و`margin:auto` للحفاظ على الصورة الرسمية كما هي مع عرضها في المنتصف.
- تم ضبط منطقة الحالة `status-area` بعرض ثابت متوسط ومركزي، وجعل رسالة الحالة والنسبة وشريط التحميل في منتصف الشاشة.
- تم إعادة بناء ملف التثبيت `/app/dist/BankDepositSystemSetup.exe` والتحقق من أن `splash.hta` داخل release يحتوي على توسيط Flex وأن رابط `/api/download/setup` يعمل 200.
