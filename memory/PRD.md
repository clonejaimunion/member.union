# PRD — نظام حساب عوائد الودائع البنكية

## أصل الطلب
المستخدم طلب برنامج عربي احترافي خاص بمعاملات حسابية لفوائد الودائع البنكية، يشبه برامج البنوك، ويشمل: اختيار بنك من ثلاثة بنوك، تسجيل بيانات الوديعة، استخراج تقرير العائد الشهري للسنة الحالية، واستخراج تقرير العائد الشهري للسنة السابقة، مع فصل بيانات كل بنك عن الآخر.

## قرارات المعمارية
- Frontend: React بواجهة عربية RTL، صفحات منفصلة لاختيار البنك، تسجيل الوديعة، تقرير السنة الحالية، وتقرير السنة السابقة.
- Backend: FastAPI endpoints تحت `/api` لإدارة البنوك والودائع والتقارير.
- Database: MongoDB لتخزين الودائع مع فصلها باستخدام `bank_id` لكل بنك.
- الحسابات: الفائدة الشهرية = مبلغ الوديعة × نسبة الفائدة الشهرية ÷ 100، مع احتساب الاستحقاق داخل كل شهر وفق فترة الوديعة.

## المستخدمون المستهدفون
- موظف أو مسؤول مالي يحتاج إدخال بيانات ودائع ومراجعة عوائدها.
- مستخدم يريد فصل تقارير كل بنك عن الآخر بطريقة منظمة وواضحة.

## المتطلبات الأساسية
- صفحة اختيار البنك: بنك التنمية الصناعية، بنك مصر، البنك الزراعي.
- صفحة تسجيل بيانات الوديعة: رقم الحساب، رقم الوديعة، مبلغ الوديعة، تاريخ الإنشاء، تاريخ الاستحقاق، نسبة الفائدة الشهرية.
- صفحة تقرير السنة الحالية: بيانات الوديعة أعلى الجدول + جدول شهري.
- صفحة تقرير السنة السابقة: بيانات الوديعة أعلى الجدول + جدول شهري.
- فصل كامل للبيانات والتقارير بين البنوك.

## ما تم تنفيذه — 2026-04-26
- بناء واجهة عربية احترافية RTL بتصميم بنكي، تنقل علوي، وبطاقات بيانات واضحة.
- تنفيذ API كامل للبنوك والودائع والتقارير مع MongoDB وبدون أي APIs وهمية.
- تنفيذ حساب العائد الشهري للسنة الحالية والسابقة مع جدول 12 شهر وإجمالي العائد.
- إضافة اختبار backend regression suite ونجاح الاختبارات، مع اختبار واجهة عبر Playwright.
- إصلاح تحذير واجهة في قائمة اختيار الوديعة واستبدالها بقائمة مخصصة آمنة.

## المتبقي حسب الأولوية
### P0
- لا توجد مهام حرجة متبقية للنسخة الحالية.

### P1
- إضافة تعديل/حذف وديعة من داخل الواجهة.
- إضافة طباعة أو تصدير التقرير PDF/Excel.
- إضافة بحث وتصفية داخل الودائع لكل بنك.

### P2
- إضافة صلاحيات مستخدمين وتسجيل دخول إذا احتاج المستخدم لاحقًا.
- إضافة لوحة ملخصات ورسوم بيانية للعوائد السنوية.
- إضافة أرشفة تقارير ومراجعة تاريخية لكل وديعة.

## المهام القادمة المقترحة
1. إضافة زر طباعة وتصدير للتقارير.
2. إضافة صفحة قائمة بكل الودائع المسجلة لكل بنك.
3. إضافة إمكانية تعديل بيانات الوديعة مع إعادة حساب التقارير تلقائيًا.

## ما تم تنفيذه — 2026-04-26 — المرحلة الثانية
- إضافة نظام تسجيل دخول آمن باستخدام username/password وجلسات Bearer Token.
- إضافة حساب أدمن افتراضي حسب طلب المستخدم: `admin` مع صلاحية لوحة تحكم خاصة.
- إضافة رابط أدمن خاص: `/secure-admin-control-panel` لإدارة المستخدمين والصلاحيات.
- إضافة إنشاء مستخدمين بصلاحيات منفصلة: إدخال ودائع، مشاهدة تقارير، تعديل ودائع، وإدارة مستخدمين.
- إضافة تغيير كلمة مرور الأدمن من لوحة الأدمن بعد إدخال كلمة المرور الحالية.
- إضافة إعداد Google Authenticator للأدمن فقط مع QR Code و manual secret، ولا يتم التفعيل إلا بعد إدخال كود صحيح.
- حماية واجهات البنوك والودائع والتقارير بالصلاحيات المناسبة.
- إضافة كشوف تفريغية مفصلة لكل بنك: عائد السنة الحالية، مستحق السنوات السابقة، الإجمالي المستحق لكل وديعة.
- إضافة كشف إجمالي حجم الودائع: رقم الوديعة، رقم الحساب، مبلغ الوديعة، نسبة الفائدة، والفائدة الشهرية.
- إضافة ملفات تشغيل محلي: `LOCAL_INSTALL_GUIDE_AR.md` و scripts لنظام Windows و Linux/macOS.
- توثيق بيانات الاختبار في `/app/memory/test_credentials.md` وإضافة `/app/auth_testing.md`.

## تحديث المتبقي حسب الأولوية
### P0
- لا توجد مشاكل حرجة متبقية بعد اختبار المرحلة الثانية.

### P1
- تحويل ملفات التشغيل المحلي إلى Windows Installer كامل بملف Setup.exe.
- إضافة تصدير PDF/Excel للكشوف والتقارير.
- إضافة شاشة تعديل وديعة من الواجهة بدل API فقط.

### P2
- إضافة سجل عمليات Audit Log لكل مستخدم.
- إضافة نسخ احتياطي واسترجاع محلي لقاعدة البيانات.

## ما تم تنفيذه — 2026-04-26 — المرحلة الثالثة
- إضافة شعارات البنوك محليًا داخل المشروع وظهورها في صفحة اختيار البنك وكل صفحات البنك.
- إضافة اعتماد واضح داخل الواجهة: "تم إنشاء البرنامج بواسطة يوسف عبدالغني احمد".
- بناء نسخة Frontend إنتاجية تعمل محليًا على `http://localhost:8001` من خلال FastAPI.
- إضافة خدمة Static Serving في الخلفية لتشغيل الواجهة محليًا من نفس السيرفر.
- إنشاء ملف تثبيت Windows بصيغة EXE: `/app/dist/BankDepositSystemSetup.exe`.
- تحديث دليل التشغيل المحلي وملفات التثبيت.

## ملاحظة تشغيل المثبت
ملف التثبيت يثبت ملفات البرنامج ويضع اختصار تشغيل على سطح المكتب. عند التشغيل لأول مرة يتم تجهيز بيئة Python وتشغيل النظام محليًا. يجب توفر Python وMongoDB Community Server على جهاز Windows.

## تحديث — 2026-04-26 — رابط تنزيل المثبت
- إضافة endpoint مباشر لتنزيل ملف التثبيت: `/api/download/setup`.
- التحقق من تنزيل الملف بنجاح وأنه Windows EXE صالح بتوقيع `MZ`.

## إصلاح — 2026-04-26 — ملف تشغيل Windows
- إصلاح مشكلة ترميز ملف BAT التي كانت تجعل CMD يقرأ النص العربي كأوامر.
- تحويل ملف التشغيل والاختصارات إلى أسماء ونصوص إنجليزية آمنة على Windows CMD.
- تغيير مسار التثبيت إلى LocalAppData لتقليل مشاكل صلاحيات الكتابة عند تجهيز بيئة Python.
- إعادة إنشاء `BankDepositSystemSetup.exe` والتحقق من صحة التحميل.

## إصلاح — 2026-04-26 — Uvicorn على Windows
- إصلاح خطأ: `'uvicorn' is not recognized` على Windows.
- تعديل التشغيل لاستخدام `python -m uvicorn` من داخل البيئة الافتراضية بدل استدعاء `uvicorn` كأمر مباشر.
- إضافة ملف `run_backend_server.bat` منفصل لتجنب مشاكل علامات الاقتباس ومسارات Windows.
- إعادة إنشاء ملف التثبيت والتحقق من تنزيله.

## إصلاح — 2026-04-26 — متطلبات Python المحلية
- إزالة مكتبة `emergentintegrations==0.1.0` من متطلبات النسخة المحلية لأنها مكتبة داخلية غير مستخدمة في البرنامج وتسبب فشل pip على أجهزة المستخدمين.
- تقليل `requirements.txt` إلى المكتبات المطلوبة فعليًا لتشغيل البرنامج فقط.
- تعديل ملف التشغيل ليفضل Python 3.11 عبر `py -3.11` عند وجوده، مع رسالة واضحة عند فشل تثبيت المتطلبات.
- إعادة إنشاء `BankDepositSystemSetup.exe` والتحقق من الرابط.

## إصلاح — 2026-04-26 — حساب العائد الشهري من الفائدة السنوية
- تعديل حساب العائد بحيث يتم التعامل مع النسبة كنسبة فائدة سنوية.
- المعادلة الجديدة: العائد السنوي = مبلغ الوديعة × نسبة الفائدة ÷ 100.
- العائد اليومي = قطع ناتج (العائد السنوي ÷ 365) إلى رقمين عشريين.
- عائد الشهر = العائد اليومي × أيام الشهر المستحقة، بحد أقصى 30 يومًا للشهر الكامل.
- تحقق المثال: 1,000,000 جنيه × 20% = 200,000 سنويًا، اليومي 547.94، شهر 30 يوم = 16,438.20 جنيه.
- تحديث تسميات الواجهة من فائدة شهرية إلى فائدة/عائد سنوي، وإعادة بناء ملف التثبيت.

## إضافة — 2026-04-26 — تقرير فوائد الودائع المستحقة
- إضافة تقرير مستقل باسم "تقرير المستحقات" لكل بنك.
- حساب المستحقات بناءً على يوم ربط الوديعة: آخر صرف شهري يكون في نفس يوم الربط، ثم يتم حساب الأيام المتبقية حتى نهاية السنة المختارة.
- مثال تم اختباره: وديعة مربوطة 18/01/2024 بمبلغ 1,000,000 ونسبة 20%؛ آخر صرف 18/12/2024، الأيام المستحقة 13، العائد اليومي 547.94، المستحق 7,123.22.
- إضافة اختيار السنة من سنة أول وديعة حتى السنة الحالية.
- إضافة زر "طباعة PDF" باستخدام طباعة المتصفح لتصدير التقرير كملف PDF.
- إعادة بناء ملف التثبيت `BankDepositSystemSetup.exe` والتحقق من صلاحيته.

## إضافة — 2026-04-26 — زر طباعة PDF لتقرير السنة الحالية
- إضافة زر "طباعة PDF" داخل تقرير العائد الشهري عن السنة الحالية.
- الزر يستدعي طباعة المتصفح ليتمكن المستخدم من اختيار Save as PDF.
- إخفاء عناصر الاختيار والتنقل أثناء الطباعة حتى يظهر التقرير بصورة أنظف.
- إعادة بناء ملف التثبيت والتحقق من صلاحيته.

## إضافة — 2026-04-26 — حذف وديعة بالكامل للأدمن فقط
- إضافة endpoint آمن لحذف الوديعة بالكامل: `DELETE /api/banks/{bank_id}/deposits/{deposit_id}`.
- الحذف متاح للأدمن فقط، وأي مستخدم عادي يحصل على رفض صلاحية 403.
- إضافة قسم في صفحة تسجيل الوديعة يظهر للأدمن فقط لعرض الودائع وحذف أي وديعة بعد تأكيد.
- التحقق من أن الوديعة المحذوفة تختفي من قائمة الودائع والتقارير.
- إعادة بناء ملف التثبيت والتحقق من رابط التنزيل.

## إضافة — 2026-04-26 — صفحات التسوية البنكية
- إضافة صفحة "التسوية البنكية" لكل بنك بترويسة: مذكرة تسوية حساب بنك - اسم البنك.
- إضافة حقول الرصيد الدفتري ورصيد كشف حساب البنك.
- إضافة جداول إدخال شيكات لم تقدم للصرف وشيكات تحت التحصيل، بكل من رقم الشيك والمبلغ والتاريخ.
- تطبيق معادلة التسوية: الرصيد الدفتري + شيكات لم تقدم للصرف - شيكات تحت التحصيل = الإجمالي.
- عرض حالة "الرصيد مطابق" عندما يساوي الإجمالي رصيد كشف حساب البنك، أو "الرصيد غير مطابق" عند وجود فرق.
- حفظ مذكرات التسوية لكل بنك واستعراض المذكرات المحفوظة واختيارها بالماوس.
- إضافة زر طباعة PDF للتسوية البنكية مع تنسيق طباعة أنظف.
- إصلاح صلاحيات صفحة التسوية بحيث يستطيع مستخدم إدخال البيانات الوصول والحفظ، بينما مستخدم العرض يستطيع مشاهدة المذكرات ولا يستطيع الحفظ.
- إعادة بناء ملف التثبيت والتحقق من رابط التنزيل.

## تعديل — 2026-04-26 — فصل الودائع عن التسويات البنكية
- إضافة صفحة اختيار رئيسية بعد تسجيل الدخول تحتوي على قسمين: فوائد الودائع، والتسويات البنكية.
- قسم فوائد الودائع يفتح اختيار البنك الخاص بالودائع فقط، ويخفي صفحات التسوية من تنقل الودائع.
- قسم التسويات البنكية يفتح اختيار البنك الخاص بالتسويات فقط ثم صفحة مذكرة التسوية.
- إضافة زر "القائمة الرئيسية" للرجوع إلى صفحة الاختيارات الرئيسية.
- تعديل عنوان التسوية إلى: "مذكرة تسوية حساب - اسم البنك".
- حذف الجملة التوضيحية الطويلة الخاصة بمعادلة التسوية من واجهة الصفحة.
- إزالة عبارة الترويسة "تسوية بنكية" واستبدالها بعبارة "مذكرة تسوية".
- إعادة بناء ملف التثبيت والتحقق من نجاح الاختبارات.

## تعديل — 2026-04-26 — نص رصيد كشف الحساب البنكي
- تعديل تسمية حقل التسوية من "الرصيد الشهري من واقع كشف حساب البنك" إلى "الرصيد - كشف الحساب البنكي".
- إعادة بناء ملف التثبيت والتحقق من رابط التنزيل.

## تعديل — 2026-04-26 — حذف مذكرة التسوية وتنسيق الشيكات
- إضافة حذف مذكرة تسوية بالكامل للأدمن فقط من واجهة المذكرات المحفوظة ومن API.
- منع المستخدم غير الأدمن من ظهور زر حذف مذكرة التسوية.
- تعديل إدخال وعرض تاريخ الشيك إلى صيغة `MM/DD` فقط داخل صفحة التسوية.
- تعديل عرض مبالغ التسوية لتظهر بعبارة `جنيه مصري` صراحة مثل: `١٬٠٠٠٫٠٠ جنيه مصري`.
- اختبار الحذف والصلاحيات والتنسيق وملف التثبيت بنجاح.

## تعديل — 2026-04-26 — قواعد طباعة مذكرة التسوية حسب الإدارة
- إضافة حقل "الإدارة" في نموذج التسوية باختيارات: النقابة العامة للعاملين بالزراعة والري، ومشروع التكافل الاجتماعي.
- حفظ الإدارة داخل مذكرة التسوية واستخدامها في الطباعة.
- إذا كانت الإدارة "النقابة العامة للعاملين بالزراعة والري" تظهر كما هي في الطباعة.
- إذا كانت الإدارة "مشروع التكافل الاجتماعي" يظهر النص: "النقابة العامة للزراعة والري - مشروع التكافل االجتماعي".
- إخفاء عنوان "مذكرة تسوية" و"مذكرة تسوية حساب - اسم البنك" من أعلى منطقة الطباعة.
- وضع لوجو البنك أعلى يسار منطقة الطباعة.
- إخفاء رصيد كشف البنك من الطباعة، وتغيير تسمية الرصيد الدفتري إلى "الرصيد" فقط في الطباعة.
- إظهار إجمالي شيكات لم تقدم للصرف أسفل آخر شيك في جدول الطباعة.
- إخفاء جدول شيكات تحت التحصيل أو شيكات لم تقدم للصرف إذا لم توجد بيانات.
- وضع الحالة في أسفل الورقة جهة اليسار بعد آخر بيانات مدخلة.
- اختبار جميع قواعد الطباعة وإعادة بناء ملف التثبيت.

## إضافة — 2026-04-26 — إجراءات المذكرات المحفوظة
- إضافة أزرار لكل مذكرة محفوظة: معاينة، طباعة، تعديل، مع استمرار حذف مذكرة التسوية للأدمن فقط.
- زر المعاينة يغير المذكرة المعروضة في منطقة الطباعة/المعاينة.
- زر الطباعة يحدد المذكرة ثم يستدعي طباعة المتصفح.
- زر التعديل يملأ نموذج التسوية ببيانات المذكرة المختارة ويظهر وضع تعديل مع زر إلغاء.
- إضافة API لتعديل مذكرة محفوظة عبر PUT بدل إنشاء مذكرة جديدة.
- إخفاء نموذج الإدخال وزر التعديل عن مستخدم مشاهدة التقارير فقط، مع إبقاء المعاينة والطباعة متاحتين له.
- اختبار الأدوار والتعديل والطباعة وملف التثبيت بنجاح.

## تعديل — 2026-04-26 — تجميع المذكرات المحفوظة حسب السنة
- إضافة اختيار سنة داخل قسم "مذكرات محفوظة".
- يتم استخراج السنة من نص الفترة/الشهر إذا كان يحتوي على سنة مثل 2025، وإلا يتم استخدام سنة إنشاء المذكرة.
- عند اختيار سنة معينة تظهر مذكرات هذه السنة فقط، مع استمرار خيارات المعاينة والطباعة والتعديل والحذف لكل مذكرة.
- إعادة بناء ملف التثبيت والتحقق من رابط التنزيل.

## إضافة — 2026-04-26 — حذف بنك بالكامل للأدمن فقط
- إضافة حذف بنك بالكامل من صفحة اختيار البنوك في قسم فوائد الودائع وقسم التسويات البنكية.
- الحذف متاح للأدمن فقط، ويظهر زر "حذف البنك بالكامل" للأدمن دون المستخدمين العاديين.
- إضافة API محمي: `DELETE /api/admin/banks/{bank_id}`.
- حذف البنك يزيله من قائمة البنوك ويحذف كل الودائع والتسويات المرتبطة به.
- منع الوصول إلى البنك المحذوف من جميع endpoints بعد الحذف.
- اختبار الصلاحيات والحذف المتتابع للودائع والتسويات وملف التثبيت بنجاح.

## تعديل — 2026-04-26 — نصوص النظام وحذف المستخدمين وصلاحية التسويات
- تعديل نص شاشة الدخول ليعرض اسم النقابة والنظام البنكي المحلي لإدارة الودائع البنكية وفوائدها والتسويات البنكية.
- حذف جملة الوصف القديمة الخاصة بالدخول المنفصل ولوحة الأدمن والصلاحيات.
- تعديل وصف قسم فوائد الودائع بإضافة الفوائد المستحقة.
- حذف مؤشر "تقارير منفصلة / 3 بنوك" من صفحة اختيار البنك.
- إضافة صلاحية "إدارة التسويات البنكية" ضمن صلاحيات المستخدمين.
- إضافة تحديث صلاحيات المستخدمين المسجلين من لوحة الأدمن، بما يشمل صلاحية التسويات البنكية.
- إضافة حذف المستخدمين المسجلين للأدمن فقط مع منع حذف حساب الأدمن.
- اختبار النصوص والصلاحيات وحذف المستخدمين والوصول للتسويات وملف التثبيت بنجاح.

## تعديل — 2026-04-26 — تحسينات التسويات والأرقام وملف التثبيت
- إضافة زر "رجوع" في القائمة الرئيسية، صفحة اختيار البنك، لوحة الأدمن، وداخل صفحات البنك قبل زر الخروج أو بجواره.
- إضافة تحذير مخصص عند محاولة مغادرة صفحة التسوية البنكية قبل الحفظ، بخياري: "موافق على عدم الحفظ" و"تراجع للحفظ".
- إخفاء بطاقات المذكرات المحفوظة في صفحة التسوية إلى أن يختار المستخدم سنة من قائمة "اختيار السنة".
- تطبيق تحويل عام للأرقام المعروضة في نصوص الواجهة إلى الأرقام العربية الشرقية ٠١٢٣٤٥٦٧٨٩، مع الحفاظ على حقول الإدخال الرقمية قابلة للتحرير.
- إعادة بناء واجهة الإنتاج المحلية باستخدام `REACT_APP_BACKEND_URL=http://localhost:8001` ثم إعادة إنشاء ملف التثبيت `/app/dist/BankDepositSystemSetup.exe`.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `942936` بايت، و SHA256: `aeeb9464a84a0f2e9c254105be1b8165ff666d4b98151a96758204e0a6737c03`.
- تشغيل اختبار مستقل شامل في `/app/test_reports/iteration_13.json` ونجاح اختبارات الواجهة والـ API بنسبة 100%، مع حذف سجل الاختبار المؤقت `TEST` بعد الانتهاء.

## تحديث المتبقي حسب الأولوية — بعد تحسينات 2026-04-26
### P0
- لا توجد مشاكل حرجة متبقية ضمن النطاق الحالي.

### P1
- مراجعة صلاحية `manage_reconciliations` الافتراضية للمستخدمين الجدد إذا أراد المستخدم تطبيق أقل صلاحية افتراضية.

### P2
- تقسيم `/app/backend/server.py` إلى ملفات routers منفصلة: auth، banks، deposits، reconciliations؛ لتقليل مخاطر التعديلات المستقبلية.

## إصلاح — 2026-04-26 — إدخال بيانات الشيكات في التسويات البنكية
- إصلاح حقول "شيكات لم تقدم للصرف" و"شيكات تحت التحصيل" بحيث تقبل رقم الشيك كأرقام، ومبلغ الشيك كرقم عشري بالجنيه المصري، وتاريخ الشيك بصيغة يوم/شهر فقط.
- تغيير حقل مبلغ الشيك من `number` إلى إدخال نصي رقمي مرن يدعم الأرقام الإنجليزية والعربية الشرقية والفاصل العشري العربي/الإنجليزي، ثم يتم تنظيفه قبل الحساب والحفظ.
- تغيير تاريخ الشيك إلى صيغة `يوم/شهر` بدلاً من `MM/DD`، مع منع إعادة تنسيقه أثناء الكتابة حتى لا يمنع المستخدم من إدخال التاريخ.
- اختبار إدخال الحقول الستة في قسمي الشيكات بنجاح من الواجهة، ثم اختبار حفظ مذكرة تسوية مؤقتة بهذه البيانات وحذف سجل الاختبار بعد التحقق.
- إعادة بناء ملف التثبيت `/app/dist/BankDepositSystemSetup.exe` والتحقق من رابط `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `944131` بايت، و SHA256: `a5e256a880a129c9e02090c212e9d4d11ea950947af6a3e262af127e6d8d4598`.

## إصلاح — 2026-04-26 — فقدان التركيز بعد أول رقم في حقول الشيكات
- سبب المشكلة: محرر الشيكات كان معرفًا كمكوّن React داخل صفحة التسوية نفسها، فكان يتم إعادة إنشاء/تركيب قسم الشيكات بعد كل رقم، مما يجعل المؤشر يخرج من الحقل كأن المستخدم ضغط Tab.
- تم تحويل محرر الشيكات إلى دالة رسم مستقرة داخل نفس الصفحة حتى لا يتم remount لحقول الإدخال عند كل ضغطة مفتاح.
- تم اختبار الكتابة حرفًا/رقمًا برقم باستخدام `keyboard.type` وليس `fill` فقط، والتأكد أن `document.activeElement` يظل نفس الحقل بعد كل رقم في جميع الحقول الستة.
- الاختبار المستقل في `/app/test_reports/iteration_14.json` نجح بنسبة 100% للواجهة والـ API، وتم تنظيف سجلات TEST المؤقتة.
- إعادة بناء ملف التثبيت والتحقق من رابط `/api/download/setup`: الحجم `944131` بايت، و SHA256: `08926c46efe944061bb484bd70b5eb7509cd1280822d2696be6ccb092f1ee646`.

## تعديل — 2026-04-26 — تنسيق PDF للتسويات البنكية
- حذف بطاقة "الإجمالي" وقيمتها من منطقة طباعة PDF الخاصة بمذكرة التسوية البنكية.
- حذف كلمة "الحالة" من الطباعة، مع إبقاء نص النتيجة فقط: "الرصيد مطابق" أو "الرصيد غير مطابق".
- نقل نص نتيجة المطابقة إلى يسار وأسفل صفحة الطباعة.
- إخفاء تنبيهات النظام من وضع الطباعة حتى لا تظهر فوق ملف PDF.
- اختبار معاينة الطباعة بوضع `print media` بنجاح: لا توجد بطاقة إجمالي، لا تظهر كلمة الحالة، ونص المطابقة يظهر أسفل اليسار.
- إعادة بناء ملف التثبيت والتحقق من رابط `/api/download/setup`: الحجم `944379` بايت، و SHA256: `f269830a4433d7084da560f4543ade2d23956a1ace7d3a4dc149d7d19233a709`.

## إضافة — 2026-04-26 — صفحة الإيرادات
- إضافة صفحة جديدة باسم "الإيرادات" بجانب "فوائد الودائع" و"التسويات البنكية" في القائمة الرئيسية.
- إضافة قاعدة بيانات وإندبوينتات للإيرادات: إنشاء، عرض، بحث، تعديل، حذف، مع منع تكرار رقم الإذن نهائيًا، ومنع تكرار رقم الشيك، ومنع تكرار رقم أمر الدفع.
- حقول الإيراد: رقم الإذن، المبلغ بالجنيه المصري، طريقة التحصيل نقداً/شيك/أمر دفع، اسم المورد أو رقم الشيك أو رقم أمر الدفع حسب الاختيار، البنك، بتاريخ، قيمة، تحريراً في، الموظف المختص.
- إضافة تقرير الإيرادات مع فلاتر البنك وطريقة التحصيل والتاريخ، وإمكانية طباعة PDF للتقرير.
- إضافة بحث برقم الشيك أو رقم أمر الدفع أو اسم الشخص الذي قام بالتوريد، مع عرض النتائج في نافذة منبثقة بكافة بيانات الإيراد.
- صلاحيات الإيرادات: الأدمن ومدخل البيانات يمكنهم الإضافة والتعديل والحذف، ومستخدم المشاهدة يمكنه عرض التقرير والبحث والطباعة فقط.
- إضافة صلاحية `manage_revenues` ضمن صلاحيات المستخدمين بلوحة الأدمن.
- الاختبارات: فحص backend والواجهة، إنشاء/بحث/تعديل/حذف، منع التكرار، الصلاحيات، وطباعة التقرير. تقرير الاختبار المستقل: `/app/test_reports/iteration_15.json`، مع تنظيف سجلات TEST المؤقتة.
- إعادة بناء ملف التثبيت والتحقق من رابط `/api/download/setup`: الحجم `959464` بايت، و SHA256: `7d9543ba944deed90d8d6c3412deaefc62b765e976158ec60b2ac59c73501096`.

## ملاحظات أمان مستقبلية — بعد اختبار الإيرادات
- أوصى الاختبار المستقل بتحسينات مستقبلية في تسجيل الدخول: حد محاولات خاطئة، دعم Cookie HttpOnly، ضبط CORS على نطاقات محددة، وتحديث seed الأدمن عند تغيير كلمة المرور من البيئة.
- هذه التحسينات ليست ضمن طلب صفحة الإيرادات الحالي، لكنها مناسبة كأولوية أمان لاحقة قبل التشغيل الموسع على شبكة داخلية.

## إضافة — 2026-04-26 — صفحة المصروفات
- إضافة صفحة جديدة باسم "المصروفات" بجانب الودائع والتسويات والإيرادات بعد تسجيل الدخول.
- إضافة قاعدة بيانات وإندبوينتات للمصروفات: إنشاء، عرض، بحث، تعديل، حذف، مع منع تكرار رقم الإذن داخل المصروفات، ومنع تكرار رقم الشيك، ومنع تكرار رقم عملية التحويل.
- حقول المصروف: رقم الإذن، طريقة الصرف نقداً/شيك/تحويل بنكي، يصرف للسيد أو تم التحويل إلى حسب الطريقة، رقم الشيك أو رقم التحويل عند الحاجة، البنك، المبلغ الكلي، بيان المبلغ، الاستقطاعات المتعددة، إجمالي الاستقطاعات، الصافي، تحريراً في، الموظف المختص.
- الصافي يتم حسابه تلقائياً: الصافي = المبلغ الكلي - إجمالي الاستقطاعات، ويظهر في النموذج والتقرير وواجهة البحث.
- إضافة زر "إضافة بيان" للاستقطاعات، وكل صف يحتوي مبلغ الاستقطاع وبيان المبلغ.
- إضافة تقرير المصروفات مع فلاتر البنك وطريقة الصرف والتاريخ، وإمكانية طباعة PDF للتقرير.
- إضافة بحث برقم الإذن أو رقم الشيك أو رقم عملية التحويل أو اسم الشخص/الجهة، مع عرض النتيجة في نافذة منبثقة بكافة بيانات المصروف.
- صلاحيات المصروفات: الأدمن ومدخل البيانات يمكنهم الإضافة والتعديل والحذف، ومستخدم المشاهدة يمكنه عرض التقرير والبحث والطباعة فقط.
- إضافة صلاحية `manage_expenses` ضمن صلاحيات المستخدمين بلوحة الأدمن.
- الاختبارات: فحص backend والواجهة، إنشاء نقداً/شيك/تحويل، منع التكرار، تعدد الاستقطاعات وحساب الصافي، البحث، التعديل، الحذف، الصلاحيات، وطباعة التقرير. تقرير الاختبار المستقل: `/app/test_reports/iteration_16.json`، مع تنظيف سجلات TEST المؤقتة.
- إعادة بناء ملف التثبيت والتحقق من رابط `/api/download/setup`: الحجم `969780` بايت، و SHA256: `45588013877d950943a83309ce68d1cccc3eebe967fbc8ab7fb447b530039225`.

## ملاحظات أمان مستقبلية — بعد اختبار المصروفات
- أعاد الاختبار المستقل نفس توصيات الأمان السابقة: إضافة حد لمحاولات تسجيل الدخول الخاطئة، دعم Cookie HttpOnly، ضبط CORS على نطاقات محددة، وتحديث seed الأدمن عند تغيير كلمة المرور من البيئة.
- هذه ملاحظات أمان قائمة كـ P1 قبل التشغيل الموسع، بينما صفحة المصروفات نفسها تعمل بنجاح حسب الاختبار المستقل.

## تعديل — 2026-04-28 — عرض إذن صرف تفصيلي للمصروفات
- إضافة قسم "عرض إذن الصرف التفصيلي" داخل صفحة المصروفات، يعرض زر "عرض" لكل مصروف مسجل.
- عند الضغط على "عرض" يتم فتح نافذة تفصيلية شبيهة بصورة إذن الصرف المرفقة: العنوان، رقم الإذن، يصرف للسيد/تم التحويل إلى، رقم الشيك أو التحويل، البنك، جدول البيان، المبلغ الكلي، المبلغ الجزئي، الاستقطاعات، الصافي مبلغ وقدره، تحريراً في، الموظف المختص، وتوقيعات الاعتماد.
- العنوان يتغير حسب طريقة الصرف: "إذن صرف نقدي"، "إذن صرف شيك"، أو "إذن تحويل بنكي".
- إضافة زر "طباعة PDF" داخل نافذة العرض لطباعة إذن الصرف فقط، مع إخفاء صفحة التقرير العامة أثناء الطباعة.
- الاختبارات: فحص ذاتي لإنشاء مصروف، فتح العرض، التحقق من حقول القالب، التحقق من مسار التحويل البنكي، والتأكد أن الطباعة تعزل إذن الصرف فقط. تقرير الاختبار المستقل الأولي: `/app/test_reports/iteration_17.json`، ثم تم إصلاح عزل الطباعة والتحقق ذاتياً.
- إعادة بناء ملف التثبيت والتحقق من رابط `/api/download/setup`: الحجم `978496` بايت، و SHA256: `81d8345e41fe67e6e9abd0601d4e9bb049371031e47b05a0e8b4fe1d41d5bafa`.

## تعديل — 2026-04-28 — تحسين عرض إذن الصرف التفصيلي
- تجميع أزرار "عرض إذن الصرف التفصيلي" حسب سنة تاريخ التحرير `issued_at`، مع عنوان لكل سنة وعدد الأذون داخلها.
- حذف النصوص المطلوبة من نموذج إذن الصرف: "رئيس الحسابات"، "رئيس الصندوق"، و"يعتمد الصرف".
- تغيير اتجاه جدول إذن الصرف إلى من اليسار لليمين، مع الحفاظ على النص العربي داخل خلايا البيان والاستقطاعات.
- الاختبارات: إنشاء مصروفات اختبارية في سنتي 2025 و2026، التحقق من ظهور مجموعات السنوات، فتح إذن صرف، التأكد من حذف النصوص الثلاثة، والتأكد أن اتجاه الجدول `ltr`، ثم تنظيف بيانات الاختبار.
- إعادة بناء ملف التثبيت والتحقق من رابط `/api/download/setup`: الحجم `979404` بايت، و SHA256: `907c36afe04cd8cbc8b5e8c4b4e1c88ffc0e4b01b6f516cbdfd4f6e55648fad6`.

## إصلاح — 2026-04-28 — إذن الصرف A4 واختيار السنة
- في جدول إذن الصرف التفصيلي تم تبديل خانات "المبلغ الجزئي" بحيث تظهر خانة القرش قبل خانة الجنيه، وتنتقل قيمة الجنيه إلى مكانها المطلوب حسب طلب المستخدم.
- ضبط طباعة إذن الصرف على ورق A4 Portrait، بحيث يكون كل إذن صرف على ورقة A4 مستقلة قدر الإمكان عند الطباعة.
- في قسم "عرض إذن الصرف التفصيلي" لا تظهر الأذون المحفوظة مباشرة؛ يجب أولاً اختيار سنة التحرير من القائمة، وفي حالة عدم اختيار السنة تظهر رسالة "اختر السنة أولاً" فقط.
- الاختبارات: إنشاء مصروف اختبار، التأكد من عدم ظهور أزرار العرض قبل اختيار السنة، ظهور الإذن بعد اختيار 2026، التحقق من ترتيب جنيه/قرش في المبلغ الجزئي، والتحقق من اتجاه الجدول والطباعة A4، ثم تنظيف بيانات الاختبار.
- إعادة بناء ملف التثبيت والتحقق من رابط `/api/download/setup`: الحجم `980793` بايت، و SHA256: `33b6c935d127481b6ac44935ff63a30245261ac7474d247853c93ead440209a7`.

## إضافة — 2026-04-28 — جهة إذن الصرف ونوع المصروف وإعانات الوفاة
- إضافة اختيار "الجهة" قبل تسجيل إذن الصرف: "النقابة العامة" أو "مشروع التكافل الاجتماعي".
- في إذن الصرف والتقارير تظهر جملة "النقابة العامة للعاملين بالزراعة والري والصيد واستصلاح الأراضي" دائماً، ويظهر "صندوق التكافل الاجتماعي" فقط عند اختيار مشروع التكافل الاجتماعي.
- إضافة اختيار "نوع المصروف": "مصروفات عمومية" أو "إعانات وفاة".
- عند اختيار مصروفات عمومية يتم إخفاء حقول رقم العضوية/لجنة/محافظة من النموذج والتقارير والطباعة.
- عند اختيار إعانات وفاة تظهر حقول رقم العضوية/لجنة/محافظة وتصبح مطلوبة، وتظهر في التقرير وإذن الصرف التفصيلي.
- فصل قسم "عرض إذن الصرف التفصيلي" إلى لوحتين: "مصروفات عمومية" و"إعانات وفاة"، وكل لوحة لا تعرض الأذون إلا بعد اختيار سنة التحرير.
- تجميع إعانات الوفاة وحدها حسب سنة تاريخ التحرير، منفصلة عن المصروفات العمومية.
- الاختبارات: فحص API والواجهة، رفض إعانة وفاة بدون بيانات العضوية/اللجنة/المحافظة، التحقق من إخفاء/إظهار الحقول حسب النوع، التحقق من طباعة الجهة، وتجميع إعانات الوفاة منفصلة حسب السنة. الاختبار المستقل: `/app/test_reports/iteration_18.json` بنجاح 100%، وتم تنظيف بيانات TEST.
- إعادة بناء ملف التثبيت والتحقق من رابط `/api/download/setup`: الحجم `984035` بايت، و SHA256: `37f15501f66e007444fc563a0f1564bc0fda91ac787fe9286f64dbe42fbbe961`.

## تعديل — 2026-04-28 — مشروع التكافل والشهر والصافي كتابة
- تعديل نص "صندوق التكافل الاجتماعي" إلى "مشروع التكافل الاجتماعي" في إذن الصرف التفصيلي.
- في قسم "عرض إذن الصرف التفصيلي" لا تظهر أذون المصروفات العمومية أو إعانات الوفاة إلا بعد اختيار السنة ثم اختيار الشهر.
- إضافة اختيار الشهر لكل من لوحة "مصروفات عمومية" ولوحة "إعانات وفاة"، مع تعطيل اختيار الشهر إلى أن يتم اختيار السنة.
- إضافة تحويل الصافي إلى نص عربي تلقائياً بصيغة: "فقط ... جنيهاً مصرياً ... لا غير" داخل خانة "الصافي مبلغ وقدره"، وإظهار "الصافي كتابة" في نافذة تفاصيل البحث/العرض.
- الاختبارات: إنشاء إعانة وفاة اختبارية في شهر 04/2026، التأكد من عدم ظهورها قبل اختيار السنة والشهر، التحقق من النص "مشروع التكافل الاجتماعي"، والتحقق من ظهور الصافي كتابة باللغة العربية، ثم تنظيف بيانات الاختبار.
- إعادة بناء ملف التثبيت والتحقق من رابط `/api/download/setup`: الحجم `988146` بايت، و SHA256: `979cb0495aa4e44523eaba0772720f9381ddfac739cc1abac8473d1cbbab74be`.

## تعديل — 2026-04-28 — إلغاء الصافي كتابة
- إلغاء تحويل الصافي إلى نص عربي في خانة إذن الصرف التفصيلي حسب طلب المستخدم.
- تغيير عنوان خانة "الصافي مبلغ وقدره" إلى "الصافي" فقط.
- إزالة "الصافي كتابة" من نافذة تفاصيل المصروفات والبحث، والإبقاء على الصافي كرقم/عملة.
- الاختبارات: إنشاء مصروف اختبار، فتح إذن الصرف، التأكد أن العنوان أصبح "الصافي" وأن النصوص "فقط" و"لا غير" لا تظهر، ثم تنظيف بيانات الاختبار.
- إعادة بناء ملف التثبيت والتحقق من رابط `/api/download/setup`: الحجم `986598` بايت، و SHA256: `9d910a0eb86305d1a58041fa5c6daa2a4bbba02274ad83dfc6f3e5a0e8e62bf8`.

## إضافة — 2026-04-28 — صفحة تحليل المصروفات
- إضافة صفحة جديدة باسم "تحليل المصروفات" في القائمة الرئيسية، وهي صفحة تقرير فقط بدون أي إدخال بيانات من المستخدم.
- الصفحة تعتمد على بيانات المصروفات المسجلة وتعرض تحليلاً شهرياً أو سنوياً حسب اختيار المستخدم.
- إضافة فلاتر: الجهة المسجلة في المصروفات "النقابة العامة" أو "مشروع التكافل الاجتماعي"، نوع العرض شهري/سنوي، السنة، والشهر عند العرض الشهري.
- إضافة بنود التحليل المطلوبة: أجور ومرتبات، بدل حضور جلسات، بدل انتقال، بدل جهد، قطع غيار وصيانة، إستشارات فنية.
- التصنيف يتم بالبحث داخل بيان المصروفات وبيانات الاستقطاعات عن الكلمات المفتاحية التي حددها المستخدم: منحة/مرتبات عاملين/مكافاة، مجلس الإدارة، بدل انتقال، بدل أعباء، قطع غيار/صيانة، أتعاب/أحمد بدران/مراجعة ميزانية.
- التقرير يعرض البيان بالكامل من المصروفات، كل خانات التحليل، إجمالي كل خانة، وعدد المصروفات والإجمالي العام للخانات للفترة المختارة.
- إضافة طباعة PDF للتقرير مع إخفاء أدوات الاختيار أثناء الطباعة.
- الاختبارات: إنشاء مصروفات اختبارية تغطي كل الكلمات المفتاحية، التحقق من التحليل الشهري والسنوي، فلترة الجهة، وطباعة التقرير فقط. الاختبار المستقل `/app/test_reports/iteration_19.json` تحقق من التدفق الأساسي، ثم تم استكمال التحقق ذاتياً للعرض السنوي والجهة والطباعة، وتم تنظيف كل بيانات TEST.
- إعادة بناء ملف التثبيت والتحقق من رابط `/api/download/setup`: الحجم `995574` بايت، و SHA256: `7613a11dd71f27f57e1855a3b094d0767d82e184e562fc55b6d129b7ab1facaa`.

## تعديل — 2026-04-28 — معاينة وإجماليات تحليل المصروفات
- تعديل تقرير تحليل المصروفات ليعرض جدول إجماليات مستقل لكل صف/بند: أجور ومرتبات، بدل حضور جلسات، بدل انتقال، بدل جهد، قطع غيار وصيانة، إستشارات فنية، وإجمالي الصف.
- إخفاء أي بيان تفصيلي يكون "إجمالي الصف" الخاص به صفراً، بحيث لا تظهر المصروفات غير المطابقة لأي كلمة مفتاحية داخل جدول البيان بالكامل.
- إضافة زر "معاينة التقرير" يفتح نافذة معاينة للتقرير، مع زر واضح "خروج من المعاينة" لإغلاقها.
- إضافة طباعة من داخل نافذة المعاينة، مع عزل التقرير أثناء الطباعة.
- الاختبارات: إنشاء مصروف مطابق ومصروف غير مطابق، التحقق من ظهور جدول الإجماليات، إخفاء الصف الصفري، فتح المعاينة، وجود زر الخروج وإغلاق النافذة، ثم تنظيف بيانات TEST.
- إعادة بناء ملف التثبيت والتحقق من رابط `/api/download/setup`: الحجم `997208` بايت، و SHA256: `7b90e32ba4084675df6a86e58b71ee845d2ea2067eac43cd92424e6df6c0d6fd`.

## تعديل — 2026-04-28 — بند إعانات الوفاة في تحليل المصروفات
- إضافة صف/بند جديد في تقرير تحليل المصروفات باسم "إعانات الوفاة" بعد إستشارات فنية وقبل إجمالي الصف.
- بند إعانات الوفاة لا يعتمد فقط على الكلمات المفتاحية؛ بل يأخذ كل مصروف مسجل بنوع "إعانات وفاة" من صفحة المصروفات ويضع مبلغ كل إعانة على حدة في جدول البيان.
- إجمالي بند إعانات الوفاة يظهر ضمن جدول الإجماليات المستقل، ويدخل في إجمالي الصف والإجمالي العام.
- المصروفات غير المصنفة ما زالت لا تظهر في جدول البيان إذا كان إجمالي الصف صفراً.
- الاختبارات: إنشاء إعانتين وفاة ومصروف غير مصنف، التحقق من ظهور بند إعانات الوفاة بإجمالي مستقل، ظهور كل إعانة على حدة، وإخفاء الصف غير المصنف، ثم تنظيف بيانات TEST.
- إعادة بناء ملف التثبيت والتحقق من رابط `/api/download/setup`: الحجم `997538` بايت، و SHA256: `7cfbd59223d062f4d4615d17d9f3b6736151037e20d8020f62d354c3d6ae9853`.

## تعديل — 2026-04-28 — صف الإجمالي أسفل جدول تحليل المصروفات
- نقل إجماليات بنود تحليل المصروفات إلى صف أخير داخل جدول البيان نفسه بدلاً من جدول إجماليات منفصل.
- ترتيب الجدول أصبح: التاريخ، البيان بالكامل من المصروفات، بنود التحليل، ثم إجمالي الصف.
- صف "الإجمالي العام" يظهر في أسفل الجدول، وتحت كل بند يظهر مبلغ إجمالي ذلك البند فقط بدون نصوص إضافية، وتحت عمود "إجمالي الصف" يظهر الإجمالي العام النهائي.
- استمرار إخفاء أي بيان يكون إجمالي الصف فيه صفراً.
- الاختبارات: إنشاء مصروف مطابق ومصروف غير مطابق، التحقق من ظهور صف الإجمالي أسفل جدول البيان، التحقق من إجماليات الأجور وبدل الانتقال وإجمالي الصف، والتأكد من إخفاء الصف غير المطابق، ثم تنظيف بيانات TEST.
- إعادة بناء ملف التثبيت والتحقق من رابط `/api/download/setup`: الحجم `997251` بايت، و SHA256: `2ef7606eb0347f57cdf0ac95240bf6407bb19fa62ef193b6a91856150a94731c`.

## تعديل — 2026-04-29 — طباعة تحليل المصروفات A4 عرضي
- ضبط طباعة تقرير تحليل المصروفات على ورق A4 بالاتجاه العرضي Landscape.
- تصغير وتثبيت عرض جدول التحليل أثناء الطباعة حتى تظهر كل الأعمدة داخل ورقة الطباعة بدون شريط تمرير أفقي.
- إخفاء أدوات الاختيار أثناء الطباعة، مع بقاء التقرير فقط.
- الاختبارات: إنشاء بيانات اختبارية تغطي بنود التحليل، تفعيل وضع الطباعة، التحقق من استخدام صفحة `expenses-analysis-landscape`، وأن عرض الجدول لا يتجاوز عرض التقرير، وأن أدوات الاختيار مخفية، ثم تنظيف بيانات TEST.
- إعادة بناء ملف التثبيت والتحقق من رابط `/api/download/setup`: الحجم `998099` بايت، و SHA256: `bc1af2d5867db0f473cdb171efd15985aa28f5a5c64a98bd5a354b9612b4faac`.

## تعديل — 2026-04-29 — إظهار ج.م في إجماليات تقارير المصروفات
- تعديل خانات الإجمالي العام في تقرير تحليل المصروفات لتعرض المبلغ متبوعاً بـ "ج.م" صراحة.
- تعديل إجماليات تقرير المصروفات العام: إجمالي الاستحقاقات، إجمالي الاستقطاعات، وإجمالي الصافي لتعرض المبلغ متبوعاً بـ "ج.م" تلقائياً.
- الاختبارات: إنشاء بيانات مصروفات اختبارية، التحقق من ظهور "ج.م" في إجمالي بنود تحليل المصروفات وإجمالي الصف أثناء وضع الطباعة، والتحقق من إجمالي الصافي في تقرير المصروفات، ثم تنظيف بيانات TEST.
- إعادة بناء ملف التثبيت والتحقق من رابط `/api/download/setup`: الحجم `998320` بايت، و SHA256: `7b08f255807cbc2ec6efffd4a1fdc044db6dfaafd94b3979d2e67aa2c742e693`.

## تعديل — 2026-04-28 — تحسين بطاقات الصفحة الرئيسية
- تعديل الصفحة الرئيسية بعد تسجيل الدخول بحيث تظهر أقسام النظام في بطاقات مربعة متساوية الحجم.
- إزالة الوصف الطويل من العرض المرئي داخل البطاقات لتجنب تزاحم النصوص، مع الإبقاء على العنوان والأيقونة وزر اختيار القسم.
- جعل شبكة البطاقات تستخدم كامل عرض الصفحة بدلاً من مساحة ضيقة بجانب المقدمة، مما يحسن احتواء أسماء الأقسام.
- الاختبارات: تسجيل الدخول، التحقق من ظهور 5 بطاقات، قياس كل بطاقة والتأكد أنها مربعة ومتساوية تقريباً، والتأكد من ظهور كل العناوين بدون تزاحم.
- إعادة بناء ملف التثبيت والتحقق من رابط `/api/download/setup`: الحجم `997321` بايت، و SHA256: `c9f11c5685bd9a24f32812ac598d5c5637a5a626b9e28d4b6473f2cb2269b30d`.

## تعديل — 2026-04-29 — تصدير تحليل المصروفات وحجم خط الطباعة
- تعديل حجم خط جدول تقرير تحليل المصروفات في وضع الطباعة إلى `5.5px`، بما يشمل خلايا الإجمالي العام، لضمان الالتزام بالمقاس المطلوب.
- إضافة أزرار تصدير مباشرة بجوار أدوات التقرير: PDF، Excel، Word، مع استمرار زر طباعة PDF الحالي.
- تصدير Excel يتم كملف `.xls` قابل للفتح في Excel، وتصدير Word يتم كملف `.doc` يحتوي عنوان التقرير والجهة وجدول التحليل والإجماليات.
- الاختبارات: تسجيل الدخول، فتح صفحة تحليل المصروفات، إنشاء مصروف اختبار مؤقت، اختيار سنة 2026 وشهر أبريل، التحقق من تفعيل أزرار PDF/Excel/Word، التحقق من أن حجم خط الطباعة `5.5px`، وتنزيل ملفي Excel وWord بنجاح، ثم حذف سجل الاختبار المؤقت.
- إعادة بناء واجهة الإنتاج المحلية باستخدام `REACT_APP_BACKEND_URL=http://localhost:8001`، نسخ build الجديد إلى مجلد release، وإعادة إنشاء ملف التثبيت.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1003429` بايت، و SHA256: `e555f83b620958f3b580f831e1ecaab89f1b018dcba863f62a3a1e8c0f063f36`.

## تعديل — 2026-04-29 — أزرار PDF/Excel/Word لجميع التقارير
- إضافة مكوّن تصدير موحد `ExportReportButtons` لإخراج أي تقرير إلى PDF عبر الطباعة، وExcel بصيغة `.xls`، وWord بصيغة `.doc`.
- إضافة أزرار PDF/Excel/Word إلى تقارير: العائد الشهري للسنة الحالية، العائد الشهري للسنة السابقة، تقرير المستحقات السنوية، الكشوف التفريغية، تقرير الإيرادات، تقرير المصروفات، إذن الصرف التفصيلي، ومذكرة التسوية البنكية.
- صفحة تحليل المصروفات كانت محدثة مسبقاً بنفس أزرار التصدير، وتم التأكد من استمرار ظهورها ضمن الاختبار.
- الاختبارات: تسجيل الدخول، زيارة صفحات التقارير الرئيسية، التحقق من ظهور أزرار PDF/Excel/Word في كل تقرير، وتجربة تنزيل Excel وWord من تقرير الإيرادات بنجاح.
- إعادة بناء واجهة الإنتاج المحلية، نسخ build الجديد إلى مجلد release، وإعادة إنشاء ملف التثبيت.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1008385` بايت، و SHA256: `a36ccf6ddd7c1f0bda7230d1a97ff8e56be0fed4d4c553fc8b7ae9d13d233a45`.

## إضافة — 2026-04-29 — صفحة المصروفات البنكية
- إضافة صفحة جديدة باسم "المصروفات البنكية" تظهر في الصفحة الرئيسية بعد تسجيل الدخول.
- الصفحة تحسب رسوم/عمولات البنك المتوقعة من الإيرادات والمصروفات المسجلة، وليس من مبلغ الحركة نفسه، وتعرض جدولاً مختصراً بالأعمدة: بيان، العدد، مصروف البنك.
- تطبيق تعريفة بنك التنمية الصناعية لحسابات الشركات من ملف التعريفة المرسل: كشف حساب شهري 100 جنيه، أمر دفع/ACH 10 جنيه، تحصيل شيك 20 جنيه، تحويل بنكي صادر 0.3% بحد أدنى 50 وحد أقصى 500، ونقدي أقل من 20 ألف = 20 جنيه.
- إضافة حالات بنكية للإيرادات من نوع شيك أو أمر دفع: تم التحصيل / تحت التحصيل، مع API لتحديث الحالة.
- إضافة حالات بنكية لشيكات المصروفات: تم الصرف / لم يقدم للصرف، مع API لتحديث الحالة.
- إضافة بنود يدوية شهرية قابلة للحفظ لكل بنك/سنة/شهر: دمغة، مراسلات بنكية، حفظ مراسلات، رسوم تحويل داخلي، رسوم تحويل خارجي.
- إضافة تصدير تقرير المصروفات البنكية إلى PDF/Excel/Word.
- الاختبارات الذاتية: تسجيل الدخول، ظهور كارت الصفحة، فتح `/banking-expenses`، ظهور الجدول وأزرار التصدير، تنزيل Excel وWord، واختبار API لحالات الإيرادات/المصروفات والبنود اليدوية مع تنظيف بيانات TEST.
- الاختبار المستقل: `/app/test_reports/iteration_20.json` نجح بنسبة 100% للـ backend والواجهة للميزة الجديدة، ولا توجد APIs وهمية.
- ملاحظات الاختبار المتبقية ليست مرتبطة بالميزة الجديدة: تحسينات أمان تسجيل الدخول وCORS وseed الأدمن ما زالت ضمن backlog الأمني.
- إعادة بناء واجهة الإنتاج المحلية، نسخ build الجديد إلى مجلد release، وإعادة إنشاء ملف التثبيت.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1022023` بايت، و SHA256: `af9fa1bf086e7def97f19d9415196bf1c543b58fe44529f3c02343a19d26eefd`.

## تعديل — 2026-04-29 — نوع الشيك في الإيرادات داخلي/خارجي
- إضافة اختيار "نوع الشيك" داخل نموذج الإيرادات عند اختيار طريقة التحصيل "شيك": داخلي أو خارجي.
- حفظ نوع الشيك داخل بيانات الإيراد وإظهاره في بيان التحصيل، مع افتراض "داخلي" للسجلات القديمة التي لا تحتوي على النوع.
- تعديل تقرير المصروفات البنكية ليقسم الشيكات المحصلة إلى صفين: "شيكات محصلة داخلي" و"شيكات محصلة خارجي".
- تطبيق تعريفة بنك التنمية الصناعية للشركات: الشيك الداخلي المحصل = 20 جنيه لكل شيك، والشيك الخارجي المحصل = 0.3% من قيمة الشيك بحد أدنى 50 جنيه وحد أقصى 500 جنيه.
- الاختبارات: إنشاء شيك خارجي مؤقت بقيمة 100,000 جنيه وحالة "تم التحصيل"، والتأكد من أن تقرير المصروفات البنكية يعرض عدد الشيكات الخارجية 1 ومصروف البنك 300 جنيه، ثم حذف سجل الاختبار.
- إعادة بناء واجهة الإنتاج، تحديث backend داخل مجلد release، وإعادة إنشاء ملف التثبيت.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1023846` بايت، و SHA256: `4ad28df2bb1ffa97ec287abc1cf04085cb53e8b30473a8b14dcac19a2894c26e`.

## تعديل — 2026-04-29 — طباعة وفترة تقرير المصروفات البنكية
- توضيح زر طباعة تقرير المصروفات البنكية بتسمية صريحة "طباعة PDF" بدلاً من الاكتفاء بكلمة PDF.
- إضافة اختيار "عرض التقرير" داخل صفحة المصروفات البنكية: شهري / سنوي، على نفس نمط التقارير السابقة.
- في العرض الشهري: يعمل التقرير على الشهر المختار، ويمكن تعديل البنود اليدوية لذلك الشهر.
- في العرض السنوي: يعمل التقرير على السنة كاملة، ويعطل اختيار الشهر، وتظهر رسوم كشف الحساب الشهري بعدد 12 شهر، كما يتم تجميع البنود اليدوية من الشهور المحفوظة خلال السنة.
- الاختبارات: فتح صفحة المصروفات البنكية، التأكد من ظهور زر "طباعة PDF"، اختيار العرض السنوي، التأكد من تعطيل الشهر، وظهور عنوان "سنة 2026" وعدد رسوم كشف الحساب 12، ثم الرجوع للعرض الشهري.
- إعادة بناء واجهة الإنتاج وإعادة إنشاء ملف التثبيت.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1024802` بايت، و SHA256: `9749de3596505267205a36805f36ebe1a7f39cb2433371f8878626d93c6e440d`.

## تعديل — 2026-04-29 — ملء شيكات مذكرة التسوية تلقائياً
- تعديل صفحة مذكرة التسوية بحيث يتم جلب "شيكات لم تقدم للصرف" تلقائياً من المصروفات المسجلة للبنك عندما تكون طريقة الصرف شيك وحالة الشيك البنكية "لم يقدم للصرف".
- تعديل صفحة مذكرة التسوية بحيث يتم جلب "شيكات تحت التحصيل" تلقائياً من الإيرادات المسجلة للبنك عندما تكون طريقة التحصيل شيك وحالة التحصيل "تحت التحصيل".
- البيانات التي يتم ملؤها تلقائياً لكل شيك: رقم الشيك، مبلغ الشيك، تاريخ الشيك. تاريخ شيكات المصروفات يؤخذ من تاريخ المصروف، وتاريخ شيكات الإيرادات يؤخذ من تاريخ الإيراد/الشيك.
- إضافة زرين داخل مذكرة التسوية: "تحديث من المصروفات" لقسم الشيكات التي لم تقدم للصرف، و"تحديث من الإيرادات" لقسم الشيكات تحت التحصيل، مع استمرار إمكانية إضافة شيك يدوياً عند الحاجة.
- الاختبارات: إنشاء إيراد شيك مؤقت تحت التحصيل ومصروف شيك مؤقت لم يقدم للصرف، فتح مذكرة تسوية بنك التنمية الصناعية، والتأكد من ظهور رقم الشيك والمبلغ والتاريخ تلقائياً في القسمين، ثم حذف بيانات الاختبار.
- إعادة بناء واجهة الإنتاج وإعادة إنشاء ملف التثبيت بدون تحذيرات lint/build.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1026841` بايت، و SHA256: `5ba0a38c2cc7e1bfb48b8161c6709cc35791d9a456f80769ca4cc0baa56775b9`.

## تعديل — 2026-04-29 — حماية شيكات مذكرة التسوية من التعديل والحذف
- جعل بيانات "شيكات لم تقدم للصرف" داخل مذكرة التسوية للقراءة فقط، وتؤخذ تلقائياً من المصروفات فقط عند كون طريقة الصرف شيك وحالة الشيك "لم يقدم للصرف".
- جعل بيانات "شيكات تحت التحصيل" داخل مذكرة التسوية للقراءة فقط، وتؤخذ تلقائياً من الإيرادات فقط عند كون طريقة التحصيل شيك وحالة التحصيل "تحت التحصيل".
- إزالة أزرار إضافة الشيكات وحذف الشيكات من القسمين؛ المتاح فقط هو زر تحديث البيانات من المصدر الصحيح.
- عند عدم وجود بيانات في المصدر، يظهر تنبيه فارغ بدلاً من صف يدوي فارغ قابل للتعديل.
- الاختبارات: إنشاء إيراد شيك مؤقت تحت التحصيل ومصروف شيك مؤقت لم يقدم للصرف، التأكد من ظهور البيانات تلقائياً، والتأكد من أن الحقول readOnly وأن أزرار الإضافة والحذف غير موجودة، ثم حذف بيانات الاختبار.
- إعادة بناء واجهة الإنتاج وإعادة إنشاء ملف التثبيت.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1026327` بايت، و SHA256: `5cc69196128486ff00d7dc31462c5df1198992c1bb51c5ef1e6d58ceb23277f2`.

## تعديل — 2026-04-29 — رسوم صرف الشيكات وعمولة الإيداع النقدي
- إضافة اختيار "نوع الشيك" داخل نموذج المصروفات عند اختيار طريقة الصرف "شيك": داخلي أو خارجي، مثل الإيرادات.
- تعديل تقرير المصروفات البنكية ليحسب رسوم الشيكات المصروفة فعلياً فقط، مع إخفاء بند "شيكات لم تقدم للصرف" نهائياً من التقرير لأنه لا يترتب عليه رسوم بنك.
- إضافة صفين بدلاً من بند الشيكات المصروفة العام: "شيكات تم الصرف داخلي" و"شيكات تم الصرف خارجي".
- قواعد بنك التنمية الصناعية للشركات المطبقة حالياً: الشيك المصروف داخلي = 20 جنيه لكل شيك، والشيك المصروف خارجي = 0.3% من قيمة الشيك بحد أدنى 50 جنيه وحد أقصى 500 جنيه.
- تعديل بند "نقدي" إلى "عمولة إيداع نقدي"، ويُحسب من الإيرادات النقدية وليس من المصروفات، وفق قاعدة 0.2% بحد أدنى 20 جنيه لكل عملية إيداع نقدي.
- تعديل قاعدة "تحويل بنكي لمستفيد" إلى 0.2% بحد أدنى 50 جنيه وحد أقصى 500 جنيه وفق تعريفة التحويلات المحلية للشركات.
- الاختبارات: إنشاء بيانات مؤقتة لشيك صرف داخلي، شيك صرف خارجي، تحويل بنكي لمستفيد، وإيراد نقدي، والتأكد من ظهور الرسوم في تقرير المصروفات البنكية وإخفاء صف شيكات لم تقدم للصرف، ثم حذف بيانات الاختبار.
- إعادة بناء واجهة الإنتاج وتحديث backend داخل مجلد release وإعادة إنشاء ملف التثبيت.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1026798` بايت، و SHA256: `2ebb64246717f48adf5911a1efbe4e4e8f15a5d27dea801edd82f16508833237`.

## تعديل — 2026-04-29 — إخفاء أوامر الدفع تحت التحصيل من تقرير العمولات
- حذف صف "أوامر دفع إلكتروني تحت التحصيل" من جدول تقرير رسوم وعمولات البنك حتى لا يظهر ضمن التقرير المالي.
- الإبقاء على أوامر الدفع تحت التحصيل داخل قسم "تحديد حالة إيرادات الشيكات وأوامر الدفع" بحيث يستطيع المستخدم تغيير الحالة إلى تم التحصيل أو تحت التحصيل.
- الاختبارات: إنشاء أمر دفع مؤقت تحت التحصيل، التأكد من عدم وجود صفه داخل التقرير، والتأكد من ظهوره داخل قسم تحديد الحالة، ثم حذف بيانات الاختبار.
- إعادة بناء واجهة الإنتاج وإعادة إنشاء ملف التثبيت.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1026730` بايت، و SHA256: `e56d3d9764607ddd1921a24fdb7ae7029da8ef7b0d441fe5522528dc1d3df244`.

## تعديل — 2026-04-29 — أزرار حالة التحصيل داخل تقارير الإيرادات والمصروفات
- إضافة أزرار "تم التحصيل" و"تحت التحصيل" أسفل زر تعديل كل سجل في بيان الإيرادات المسجلة، لجميع طرق التحصيل: شيك، أمر دفع، نقدي.
- توسيع API حالة الإيرادات ليقبل تحديث حالة التحصيل لكل الطرق وليس الشيكات وأوامر الدفع فقط.
- إضافة أزرار "تم التحصيل" و"تحت التحصيل" أسفل زر تعديل كل سجل في بيان المصروفات المسجلة، لجميع طرق الصرف: شيك، تحويل بنكي، نقدي، مع استخدام نفس الحالة المتصلة بتقرير الرسوم ومذكرة التسوية.
- توسيع API حالة المصروفات ليقبل تحديث الحالة لكل الطرق وليس الشيكات فقط.
- تحديث صفحة المصروفات البنكية: قسم تحديد حالة الإيرادات يعرض الشيكات وأوامر الدفع والنقدي، وقسم تحديد حالة المصروفات يعرض الشيكات والتحويل البنكي والنقدي، بنفس أزرار الحالة.
- تعديل حساب تقرير رسوم وعمولات البنك بحيث يعتمد على الحالة: الإيراد النقدي يحسب عمولة الإيداع عند "تم التحصيل" فقط، والتحويل البنكي للمستفيد يحسب عند "تم التحصيل" فقط، والشيكات المصروفة تحسب عند الحالة المتصلة بـ"تم التحصيل".
- الاختبارات: إنشاء إيراد نقدي ومصروف تحويل مؤقتين، تحديث حالتهما عبر API، التأكد من ظهور أزرار الحالة في تقرير الإيرادات وتقرير المصروفات، والتأكد من ظهورهما في أقسام الحالة داخل المصروفات البنكية، ثم حذف بيانات الاختبار.
- إعادة بناء واجهة الإنتاج وتحديث backend داخل مجلد release وإعادة إنشاء ملف التثبيت.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1027945` بايت، و SHA256: `1d5a3b381dc26748a8d19511caa5af5e1dd88d320ae51a65dc56e6f2acfa88f3`.

## تعديل — 2026-04-29 — عنوان تبويب البرنامج ونصوص الصفحة الرئيسية
- تغيير عنوان تبويب المتصفح الافتراضي إلى "النقابة العامة للعاملين بالزراعة والري".
- إضافة تحديث ديناميكي لعنوان تبويب المتصفح حسب القسم المفتوح: الإيرادات، المصروفات، المصروفات البنكية، تحليل المصروفات، التسويات البنكية، فوائد الودائع، وغيرها.
- تغيير تسمية "القائمة الرئيسية" إلى "الصفحة الرئيسية" في الصفحة الرئيسية وأزرار العودة للصفحة الرئيسية داخل الصفحات.
- إزالة الجمل المطلوبة من الصفحة الرئيسية: "اختر نوع العمل"، "نظام بنكي منفصل ومنظم"، "كل قسم مستقل وواضح للوصول السريع بدون تزاحم في العرض."، و"اختيار القسم ←".
- تغيير عبارة الحقوق من "تم إنشاء البرنامج بواسطة يوسف عبدالغني احمد" إلى `powered py ® Youssef abdelghane`.
- الاختبارات: تسجيل الدخول، التحقق من عنوان التبويب في الصفحة الرئيسية، فتح الإيرادات والمصروفات البنكية والتأكد من تغيير عنوان التبويب، والتأكد من حذف الجمل المطلوبة وظهور عبارة الحقوق الجديدة.
- إعادة بناء واجهة الإنتاج وإعادة إنشاء ملف التثبيت.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1028592` بايت، و SHA256: `058bd02cb07c90b0f699e1e19e157fe6881192ee1c966af8b4d43fc470055244`.

## تعديل — 2026-04-29 — إخفاء البنود الصفرية في تقرير رسوم وعمولات البنك
- تعديل جدول تقرير المصروفات البنكية/رسوم وعمولات البنك بحيث لا يظهر أي بند إذا كان العدد الخاص به صفر أو مصروف البنك الخاص به صفر.
- إضافة رسالة فارغة داخل الجدول عند عدم وجود أي بند برسوم بنكية خلال الفترة المختارة.
- الإجمالي العام أصبح يُحسب من البنود الظاهرة فقط بعد فلترة البنود الصفرية.
- الاختبارات: فتح تقرير المصروفات البنكية لشهر أبريل 2026، والتأكد من اختفاء صفوف مثل شيكات تحت التحصيل وأوامر الدفع تحت التحصيل أو أي صف صفري، مع استمرار ظهور البنود ذات العدد والمبلغ.
- إعادة بناء واجهة الإنتاج وإعادة إنشاء ملف التثبيت.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1028894` بايت، و SHA256: `63143ff7778a2803f376a1b396a990f2dff629a1b459854a9083385de1043f25`.

## إضافة — 2026-04-29 — إدارة تعريفة الخدمات المصرفية من صفحة الأدمن
- إضافة قسم جديد في صفحة الأدمن بعنوان "تعريفة الخدمات المصرفية" مخصص للأدمن فقط، لإدارة تعريفة كل بنك لحسابات الشركات.
- يدعم القسم اختيار البنك، إدخال رابط ملف PDF، رفع ملف PDF، ثم الضغط على "تحديث تلقائي من PDF/رابط" لاستخراج التعريفة وتحديث قواعد الحساب.
- إضافة حقول مراجعة واعتماد القيم التي يستخدمها تقرير رسوم وعمولات البنك: رسوم كشف الحساب، أوامر الدفع، الشيكات الداخلية/الخارجية، التحويل البنكي، الإيداع النقدي، وربط الوديعة.
- عند إضافة بنك جديد، يتم إنشاء تعريفة افتراضية له تلقائياً بقيم صفرية لحين رفع/اعتماد تعريفة البنك.
- تعديل تقرير المصروفات البنكية ليقرأ قواعد التعريفة من backend عبر `/api/banking-tariffs/{bank_id}` بدلاً من الاعتماد على قيم ثابتة داخل الواجهة.
- إضافة endpoints للأدمن: `GET /api/admin/banking-tariffs`، `PUT /api/admin/banking-tariffs/{bank_id}`، `POST /api/admin/banking-tariffs/{bank_id}/extract`، مع endpoint قراءة للتقرير `GET /api/banking-tariffs/{bank_id}`.
- إضافة قراءة PDF في backend باستخدام `pypdf` ورفع الملفات باستخدام `python-multipart`، وتحديث `requirements.txt` ونسخة release.
- تم اختبار استخراج تعريفة بنك التنمية الصناعية من الرابط المرسل، والتأكد من القيم المعتمدة: كشف حساب 100، أمر دفع 10، شيك داخلي 20، شيك خارجي 0.3%، تحويل 0.2%، إيداع نقدي 0.2%.
- تم اختبار ظهور قسم التعريفة في صفحة الأدمن، وظهور القيم، وقراءة تقرير رسوم وعمولات البنك للتعريفة المحفوظة.
- إعادة تشغيل backend بسبب إضافة الاعتماديات، ثم إعادة بناء واجهة الإنتاج وتحديث backend/requirements داخل release وإعادة إنشاء ملف التثبيت بدون تحذيرات.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1040554` بايت، و SHA256: `bd09e17c7746e399e040177cfc5bbeabf47e47c290cdab798565de3a904ab632`.

## تعديل — 2026-04-29 — تثبيت أساس حساب فوائد الودائع على 365 يوم
- تعديل دالة حساب أيام السنة في backend بحيث تستخدم 365 يوم دائماً في حساب الفائدة اليومية، بما في ذلك السنوات الكبيسة.
- لم يتم تغيير عدد الأيام الفعلية النشطة داخل كل شهر؛ التغيير فقط في مقام معادلة الفائدة اليومية ليصبح ثابتاً 365.
- الاختبار: استدعاء حساب فائدة وديعة اختبارية لسنة 2024 الكبيسة بقيمة 100,000 ومعدل 10%، والتأكد أن `days_in_year(2024)=365` وأن الفائدة اليومية = `27.39` بدلاً من حسابها على 366.
- إعادة بناء واجهة الإنتاج وتحديث backend داخل release وإعادة إنشاء ملف التثبيت.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1040544` بايت، و SHA256: `d4c1e8c8e63c9c5124e1c4b6d648dcb4c0df8c3fce026ff7e1d258564af3ae41`.

## إصلاح — 2026-04-29 — تنظيف اعتماديات التثبيت
- إصلاح خطأ تثبيت `emergentintegrations==0.1.0` الذي ظهر أثناء تشغيل ملف التثبيت؛ السبب أن `requirements.txt` كان قد تم تحديثه سابقاً عبر `pip freeze` فدخلت حزم بيئة التطوير غير المطلوبة.
- استبدال ملف `backend/requirements.txt` ونسخة release بقائمة اعتماديات نظيفة ومحدودة لما يستخدمه البرنامج فعلياً: FastAPI, Uvicorn, Motor, Pydantic, bcrypt, PyJWT, PyOTP, qrcode, pypdf, python-multipart وغيرها من الاعتماديات اللازمة فقط.
- اختبار تثبيت الاعتماديات داخل virtualenv مؤقت جديد بنجاح، والتأكد من عدم وجود `emergentintegrations` في ملفات requirements.
- إعادة بناء واجهة الإنتاج وتحديث requirements داخل release وإعادة إنشاء ملف التثبيت.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1039544` بايت، و SHA256: `300489dc9538074f770f7eb2de45f0ba5297e0147822138da069bb2b898412f4`.

## تعديل — 2026-04-29 — اختيارات السنة والشهور في تقرير رسوم وعمولات البنك
- تعديل فلتر السنة في صفحة "المصروفات البنكية / تقرير رسوم وعمولات البنك" من خانة كتابة إلى قائمة اختيار.
- قائمة السنة تبدأ من 2025 وتستمر لسنوات مستقبلية، وتم اختبار اختيار سنة 2025 بنجاح.
- تثبيت ترتيب قائمة الشهور ليبدأ من يناير وينتهي في ديسمبر بالترتيب الصحيح، بعد استبدال الاعتماد على ترتيب مفاتيح الكائن بمصفوفة أشهر ثابتة.
- الاختبارات: فتح تقرير رسوم وعمولات البنك، التأكد أن أول سنة هي 2025، والتأكد أن ترتيب الشهور: يناير، فبراير، مارس، أبريل، مايو، يونيو، يوليو، أغسطس، سبتمبر، أكتوبر، نوفمبر، ديسمبر.
- إعادة بناء واجهة الإنتاج وإعادة إنشاء ملف التثبيت.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1039983` بايت، و SHA256: `6e2e3206a82753c2c773564d0a82872ffdfae370046bf3aa74d823c983351c15`.

## إضافة — 2026-04-29 — صفحة دفتر الأستاذ
- إضافة صفحة جديدة باسم "دفتر الأستاذ" تظهر في الصفحة الرئيسية، مع تحديث عنوان تبويب المتصفح إلى "دفتر الأستاذ" عند فتحها.
- دفتر الأستاذ يجمع الحركات من الإيرادات والمصروفات والمصروفات البنكية حسب البنك والفترة، ويعرض جدولاً محاسبياً: م، التاريخ، المصدر، المرجع، البيان، مدين، دائن، الرصيد.
- إضافة فلاتر: البنك، الحساب، من تاريخ، إلى تاريخ. الحسابات المتاحة: حساب البنك، الإيرادات، المصروفات، المصروفات البنكية.
- حساب البنك يعرض الإيرادات المحصلة كمدين، والمصروفات المنفذة ورسوم البنك كدائن. حساب الإيرادات يعرض الإيرادات المحصلة كدائن. حساب المصروفات والمصروفات البنكية يعرضان القيم كمدين.
- ربط دفتر الأستاذ بتعريفة الخدمات المصرفية المحفوظة لكل بنك، وبالحالات الحالية للسجلات: يتم إدراج الإيرادات/المصروفات فقط عند حالة "تم التحصيل".
- إضافة KPIs للإجمالي: إجمالي مدين، إجمالي دائن، الرصيد، مع تصدير PDF/Excel/Word.
- الاختبارات: تسجيل الدخول، التأكد من ظهور كارت دفتر الأستاذ، فتح `/ledger`، التحقق من الفلاتر والجدول وأزرار التصدير، تغيير الحساب إلى المصروفات البنكية، ونجاح تنزيل Excel.
- إعادة بناء واجهة الإنتاج وإعادة إنشاء ملف التثبيت.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1049087` بايت، و SHA256: `09179a94045a2bb9fe1ab707b784b15ba3b09c613ad5a9755138c8475a17712b`.

## إضافة — 2026-04-29 — رصيد افتتاحي لكل بنك
- إضافة حقل `opening_balance` إلى نموذج البنك، مع تخزين إعدادات الأرصدة الافتتاحية لكل بنك في backend.
- إضافة endpoint أدمن: `PUT /api/admin/banks/{bank_id}/opening-balance` لحفظ الرصيد الافتتاحي لأي بنك، بما في ذلك البنوك الأساسية أو أي بنك جديد.
- تعديل `/api/banks` وقراءة البنك بحيث تعيد الرصيد الافتتاحي مع بيانات كل بنك.
- إضافة قسم جديد في صفحة الأدمن بعنوان "الأرصدة الافتتاحية" يعرض كل بنك وحقل الرصيد الافتتاحي وزر حفظ لكل بنك.
- تعديل دفتر الأستاذ: عند اختيار حساب البنك، يظهر قيد "الرصيد الافتتاحي" كأول قيد إذا كان رصيد البنك الافتتاحي غير صفر، ويؤثر في الرصيد المتراكم.
- الاختبارات: حفظ رصيد افتتاحي مؤقت لبنك التنمية الصناعية، التحقق من ظهوره في صفحة الأدمن، فتح دفتر الأستاذ والتأكد من ظهور قيد الرصيد الافتتاحي ورصيده، ثم إعادة قيمة الاختبار إلى صفر.
- إعادة بناء واجهة الإنتاج وتحديث backend داخل release وإعادة إنشاء ملف التثبيت.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1051406` بايت، و SHA256: `5b5a407587aaf04f723e85b68da85ad526d1d854afbfc2ea3911e7b5f890d7ec`.

## إضافة — 2026-04-29 — صفحة الفاتورة الإلكترونية
- إضافة صفحة جديدة باسم "الفاتورة الإلكترونية" تظهر في الصفحة الرئيسية، مع تحديث عنوان تبويب المتصفح عند فتحها.
- إضافة إعدادات الجهة الضريبية: اسم الجهة، الرقم الضريبي، كود النشاط، العنوان، المحافظة، ونسبة الضريبة الافتراضية.
- إضافة سجل داخلي للعملاء/الجهات وسجل أكواد الخدمات، مع كود خدمة افتراضي `EGS-SERVICE-001`.
- إضافة توليد تلقائي للفواتير من الإيرادات المحصلة فقط عبر زر "توليد من الإيرادات"، مع عدم تكرار الفواتير لنفس الإيراد.
- كل فاتورة تسحب تلقائياً من الإيراد: رقم الإيصال، التاريخ، اسم العميل/الجهة، البيان، المبلغ، طريقة التحصيل، البنك، وتضيف كود الخدمة والضريبة والإجمالي.
- إضافة حالات داخلية للفواتير: مسودة، جاهزة، تحتاج مراجعة، مرسلة، مقبولة، مرفوضة. الفاتورة تظهر "تحتاج مراجعة" إذا كانت بيانات الجهة/العميل الضريبية ناقصة.
- إضافة تصدير الفواتير إلى JSON وPDF/Excel/Word من الصفحة.
- إضافة endpoints: إعدادات الفاتورة الإلكترونية، العملاء، أكواد الخدمات، قائمة الفواتير، توليد الفواتير من الإيرادات، وتحديث حالة الفاتورة.
- الاختبارات: إنشاء إيراد نقدي محصل مؤقت، توليد فاتورة إلكترونية منه، التأكد من ظهورها في الصفحة، تنزيل JSON، ثم حذف الإيراد والفاتورة والعميل التجريبيين من قاعدة البيانات.
- إعادة بناء واجهة الإنتاج وتحديث backend داخل release، وإعادة تثبيت NSIS في البيئة بعد غيابه، وإعادة إنشاء ملف التثبيت.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1064186` بايت، و SHA256: `a28cad8e300e5240fbeb44e0f248cf8bda6c566da72d1020f5e8d13b1f21a2fa`.

## إضافة — 2026-04-29 — صفحة المراجعة الأمنية داخل الأدمن
- إضافة قسم جديد داخل صفحة الأدمن باسم "المراجعة الأمنية" لتنفيذ ضوابط الاقتراب من الاعتماد.
- إضافة سجل تدقيق Audit Log عام لكل طلب API مؤثر غير GET، مع تسجيل المستخدم، المسار، العملية، الحالة، الوقت، ونسخة من جسم الطلب بعد إخفاء كلمات المرور والرموز السرية.
- إضافة إقفال الفترات المالية شهرياً وسنوياً، وفتح الفترة بسبب مكتوب، مع منع إضافة/تعديل/حذف الإيرادات والمصروفات والودائع داخل فترة مقفلة.
- إضافة اعتماد التقارير بحالات: غير معتمد، معتمد، تم إلغاء الاعتماد. يسجل النظام اسم المعتمد الحقيقي حسب المستخدم، رقم الاعتماد الثابت للأدمن `0103535` والاسم `يوسف عبدالغني`، وللمستخدم `entryuser` الرقم `028060` والاسم `دعاء علي`، وأرقام مولدة للمستخدمين الجدد، مع الصفة والملاحظات وعبارة الاعتماد وبيانات QR الداخلية.
- إضافة النسخ الاحتياطي والاستعادة للأدمن فقط: إنشاء نسخة احتياطية مشفرة بكلمة مرور باستخدام Fernet/PBKDF2، تحميل النسخة، واستعادة نسخة مشفرة بكلمة مرور، مع تسجيل العملية في Audit Log.
- إضافة صلاحيات أدق في إدارة المستخدمين: إضافة/تعديل/حذف الإيرادات والمصروفات، اعتماد تقرير، اعتماد إيراد، إقفال/فتح فترة، إدارة الفاتورة الإلكترونية، إدارة التعريفات البنكية، إدارة النسخ الاحتياطي.
- إضافة مؤشرات ودليل إجراءات داخل صفحة المراجعة الأمنية يوضح من يدخل البيانات، من يراجع، من يعتمد، متى يتم إقفال الشهر، كيف تفتح فترة مقفلة، كيف يصدر التقرير، وكيف تنشأ النسخ الاحتياطية.
- ملاحظة صريحة: مراجعة محاسب قانوني ومراجعة أمنية خارجية لا تتم آلياً ويجب تنفيذها بواسطة مختص، وتم عرضها داخل الصفحة.
- تحديث `requirements.txt` لإضافة `cryptography==46.0.7` وتحديث نسخة release.
- الاختبارات: إنشاء/فتح فترة اختبارية، إنشاء اعتماد تقرير اختباري، إنشاء نسخة احتياطية مشفرة، التحقق من سجل التدقيق، وفتح واجهة المراجعة الأمنية في صفحة الأدمن.
- إعادة بناء واجهة الإنتاج وتحديث backend/requirements داخل release، وإعادة تثبيت NSIS عند غيابه، وإعادة إنشاء ملف التثبيت.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1076242` بايت، و SHA256: `25c337c85117b0eb665458dfbf06ba6ae6c2efdc69b389ab09fd0c29fca4e1d0`.

## تعديل — 2026-04-29 — طباعة وتصدير كشف إجمالي حجم الودائع
- إضافة أزرار مباشرة داخل قسم "كشف إجمالي حجم الودائع" لطباعة PDF وتصدير Excel وWord، مستقلة عن أزرار تصدير صفحة الكشوف التفريغية العامة.
- تعديل مكوّن `ExportReportButtons` ليدعم طباعة جزء محدد من الصفحة عند تمرير `printSelectors`، مع الحفاظ على السلوك القديم لباقي التقارير.
- أزرار الكشف الجديدة: `print-volume-statement-pdf-button`، `export-volume-statement-excel-button`، `export-volume-statement-word-button`.
- الاختبارات: فتح صفحة الكشوف التفريغية لبنك التنمية الصناعية، التأكد من ظهور أزرار الطباعة والتصدير داخل كشف حجم الودائع، ونجاح تنزيل Excel وWord للكشف.
- إعادة بناء واجهة الإنتاج وإعادة إنشاء ملف التثبيت.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1077081` بايت، و SHA256: `10ab29a77a12c9bfec222bf72e01d267b5e987caf14db8aa9339b2c85b874ee1`.

## تعديل — 2026-04-29 — تصفية كشف العوائد التفريغي بالشهر/السنة والتصدير
- إكمال إضافة فلتر "عرض الكشف" داخل كشف العوائد التفريغي: سنوي أو شهري، مع اختيار السنة والشهر عند العرض الشهري.
- تحديث endpoint `/api/banks/{bank_id}/statements/detailed` لدعم `period_type` و`year` و`month`، بحيث يحسب عائد الشهر المحدد أو عائد السنة المحددة مع الحفاظ على أساس 365 يوم.
- إضافة أزرار مباشرة داخل كشف العوائد التفريغي: طباعة PDF، تصدير Excel، تصدير Word، مع عزل قسم الكشف عند الطباعة/التصدير.
- الاختبارات: فحص lint للواجهة والخلفية، إنشاء وديعة اختبارية مؤقتة لسنة 2030 والتحقق من عائد يناير 3100.00 وعائد السنة 36500.00 ثم حذفها، واختبار الواجهة بتسجيل الدخول واختيار يناير/2030 وتنزيل Excel وWord بنجاح.
- إعادة بناء واجهة الإنتاج المحلية باستخدام `REACT_APP_BACKEND_URL=http://localhost:8001`، وتحديث نسخة release، وإعادة إنشاء ملف التثبيت NSIS.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1079479` بايت، و SHA256: `09ebaa2fb8f69878890965eada4cd5d5a82cbe5d05d222334bcf3c56a0680550`.

## تحديث المتبقي حسب الأولوية — بعد تصفية كشف العوائد التفريغي
### P0
- لا توجد مهام حرجة متبقية حالياً بعد اختبار فلترة كشف العوائد وإعادة بناء ملف التثبيت.

### P1
- تقسيم `/app/backend/server.py` إلى Routers منفصلة لتقليل حجم الملف ومخاطر التعديلات المستقبلية.
- مراجعة تحسينات أمان تسجيل الدخول المقترحة سابقاً: حد المحاولات، Cookie HttpOnly، وضبط CORS.

### P2
- التكامل الرسمي للفواتير الإلكترونية مع مصلحة الضرائب المصرية عند توفر بيانات الاعتماد والتوقيع الرقمي وبيئة الاختبار.

## تعديل — 2026-04-29 — فلاتر مستقلة لعائد الفترة ومستحق سنوات سابقة
- تعديل كشف العوائد التفريغي بحيث أصبح عائد الفترة المختارة يعتمد على السنة/الشهر المختارين من فلتر "تصفية عائد الفترة" مع زر تطبيق مستقل.
- إضافة فلتر مستقل لقيمة "مستحق سنوات سابقة" باسم "تصفية مستحق سنوات سابقة"، يدعم اختيار سنوي أو شهري مع سنة وشهر وزر تطبيق مستقل.
- تعديل الإجمالي المستحق ليصبح مجموع عائد الفترة المختارة + مستحق الفترة السابقة المختارة فقط، وليس مجموعاً عاماً غير محدد.
- تحديث عناوين الأعمدة وبطاقات الإجماليات لتوضح الفترة المختارة لكل من العائد والمستحق.
- تعديل نص شاشة الدخول من "نظام بنكي محلي لإدارة الودائع البنكية وفوائدها - والتسويات البنكية" إلى "نظام محاسبي متكامل".
- إضافة إجمالي "أيام الاستحقاق" في آخر صف داخل جدول العائد الشهري، وتحديث وصف التقرير ليؤكد أن الحساب على أساس 365 يوم.
- الاختبارات: فحص lint للواجهة والخلفية، اختبار API بوديعة مؤقتة للتأكد من عائد 2030 ومستحق 2029 والإجمالي، واختبار واجهة كامل لتغيير الفلاتر وتنزيل القيم، والتحقق من نص الدخول وإجمالي أيام الاستحقاق 365، مع تنظيف بيانات الاختبار.
- إعادة بناء واجهة الإنتاج، تحديث نسخة release، وإعادة إنشاء ملف التثبيت NSIS.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1081292` بايت، و SHA256: `a1e20bb3e4b61260c6a0cb4a3aa72ba536da9df0f33a591b0f28525220cc3e49`.

## تحديث المتبقي حسب الأولوية — بعد فلاتر العوائد المستقلة
### P0
- لا توجد مهام حرجة متبقية في كشف العوائد التفريغي بعد الاختبار والبناء.

### P1
- تقسيم `/app/backend/server.py` إلى Routers منفصلة لتسهيل الصيانة وتقليل حجم الملف.
- مراجعة تحسينات أمان تسجيل الدخول وCORS قبل التشغيل الموسع.

### P2
- التكامل الرسمي مع مصلحة الضرائب المصرية للفواتير الإلكترونية عند توفر بيانات الاعتماد والتوقيع الرقمي.

## تعديل — 2026-04-29 — حفظ فلاتر كشف العوائد وأيقونة محاسبية للاختصار
- إضافة حفظ تلقائي لإعدادات فلاتر كشف العوائد التفريغي لكل مستخدم ولكل بنك داخل المتصفح المحلي، بحيث عند فتح الكشف مرة أخرى تظهر آخر سنة/شهر استخدمها المستخدم لعائد الفترة ومستحق سنوات سابقة.
- تصحيح كل الأوصاف القديمة التي تشير إلى أيام السنة الفعلية أو 366 يوم، وتثبيت الصياغة والاختبارات على أساس 365 يوم.
- تعديل اسم الجهة في شاشة الدخول من "استصلاح الارادي" إلى "استصلاح الاراضي".
- إنشاء أيقونة محاسبية بصيغة `.ico` وإضافتها إلى نسخة التثبيت، وربط اختصار سطح المكتب وStart Menu بها بدلاً من الأيقونة الافتراضية.
- تحديث NSIS ليستخدم `accounting_app.ico` كأيقونة للمثبت والاختصارات وإلغاء التثبيت.
- الاختبارات: فحص lint للواجهة والخلفية والاختبار المعدل، اختبار واجهة لحفظ الفلاتر بعد إعادة تحميل الصفحة، التحقق من نص الجهة، ثم بناء production وNSIS بنجاح.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1100012` بايت، و SHA256: `4de1d84794aa5161fd74a98aaa488f07bde15488f6675461a3f4163c8573b453`.

## تحديث المتبقي حسب الأولوية — بعد حفظ الفلاتر وأيقونة الاختصار
### P0
- لا توجد مهام حرجة متبقية حالياً.

### P1
- تقسيم `/app/backend/server.py` إلى Routers منفصلة.
- مراجعة تحسينات أمان تسجيل الدخول وCORS.

### P2
- التكامل الرسمي مع مصلحة الضرائب المصرية للفواتير الإلكترونية عند توفر بيانات الاعتماد والتوقيع الرقمي.

## تعديل — 2026-04-29 — شاشة إعدادات عامة للأدمن
- إضافة شاشة "الإعدادات العامة" داخل لوحة الأدمن لتعديل اسم النظام من داخل البرنامج بدون تعديل ملفات التثبيت.
- اسم النظام أصبح يظهر ديناميكياً في شاشة الدخول، رأس الصفحة الرئيسية، وعنوان تبويب المتصفح.
- إضافة رفع شعار/أيقونة اختصار بصيغ PNG أو JPG أو ICO، مع تحويل PNG/JPG إلى ICO في الخلفية وحفظها محلياً.
- عند تشغيل النسخة المثبتة على Windows، يحاول النظام تحديث أيقونة اختصار سطح المكتب وStart Menu تلقائياً بعد رفع الأيقونة.
- إضافة endpoint عام `/api/app-settings/public` لجلب اسم النظام والأيقونة قبل تسجيل الدخول، وendpoints أدمن لإدارة الاسم والأيقونة.
- إضافة أيقونة النظام المخصصة ضمن نسخ الاحتياطي عبر مجموعة `app_settings`.
- الاختبارات: فحص lint للواجهة والخلفية، اختبار API لتعديل الاسم واستعادته ورفع أيقونة ICO، واختبار واجهة كامل من لوحة الأدمن للتحقق من ظهور الاسم في شاشة الدخول ورأس الصفحة وعنوان التبويب وتحديث حالة الأيقونة.
- إعادة بناء واجهة الإنتاج، تحديث نسخة release، وإعادة إنشاء ملف التثبيت NSIS.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1106145` بايت، و SHA256: `edeaabe4d0a94d74615707b2c4aac69138358d84be87b17940bed4dc5fb2fb0a`.

## تحديث المتبقي حسب الأولوية — بعد شاشة الإعدادات العامة
### P0
- لا توجد مهام حرجة متبقية حالياً.

### P1
- تقسيم `/app/backend/server.py` إلى Routers منفصلة لتقليل حجم الملف وتسهيل الصيانة.
- تحسين آلية تحديث الاختصارات على Windows بإظهار عدد الاختصارات التي تم تحديثها فعلياً داخل الشاشة.

### P2
- دعم شعار مؤسسة منفصل داخل التقارير المطبوعة، بجانب أيقونة الاختصار.
- التكامل الرسمي مع مصلحة الضرائب المصرية للفواتير الإلكترونية عند توفر بيانات الاعتماد والتوقيع الرقمي.

## تعديل — 2026-04-29 — تقسيم البرنامج إلى جهتين منفصلتين
- إضافة اختيار الجهة قبل تسجيل الدخول عبر زرين كبيرين: "مشروع التكافل الاجتماعي" و"النقابة العامة".
- إنشاء فصل كامل للبيانات حسب الجهة `organization_id`: المستخدمون، البنوك المخصصة، أرصدة البنوك، الودائع، الإيرادات، المصروفات، التسويات، المصروفات البنكية، الفاتورة الإلكترونية، الفترات المالية، الاعتمادات، وسجلات التدقيق.
- البيانات القديمة/القائمة تم نسبها إلى `social-solidarity` = مشروع التكافل الاجتماعي كما طلب المستخدم.
- إنشاء أدمن مستقل لكل جهة:
  - مشروع التكافل الاجتماعي: `admin_takaful` / `Admin@123`، مع استمرار `admin` كحساب قديم تابع لنفس الجهة.
  - النقابة العامة: `admin_union` / `Admin@123`.
- تسجيل الدخول لا ينجح إلا إذا كان اسم المستخدم تابعاً للجهة المختارة؛ نفس كلمة المرور على جهة أخرى تفشل.
- شاشة إعدادات الأدمن أصبحت تحتوي على "اسم الجهة/النقابة"، ويظهر اسم الجهة في شاشة الدخول، رأس الصفحة الرئيسية، ترويسة صفحات البنك، وعناوين الطباعة/التصدير.
- إضافة ترويسة طباعة عامة باسم الجهة والنظام عند استخدام الطباعة من صفحات البرنامج.
- تحسينات أمان المصادقة بعد فحص مستقل: قفل مؤقت بعد 5 محاولات دخول خاطئة لمدة دقيقة، إصدار Cookie باسم `access_token` مع `HttpOnly`، وتحديث seed للأدمن ليعيد ضبط كلمة المرور عند تغير `ADMIN_INITIAL_PASSWORD`.
- ضبط CORS في التطبيق من wildcard إلى قائمة صريحة في البيئة المحلية. ملاحظة فنية: استجابة OPTIONS الخارجية قد تُدار من طبقة الـ preview قبل الوصول للتطبيق، لكن تدفق المتصفح والتطبيق نفسه يعملان.
- الاختبارات: self-test API لاختيار الجهة، مصفوفة الدخول الصحيح/الخاطئ، نطاق مستخدمي الأدمن، عزل وديعة بين الجهتين، اختبار واجهة لاختيار الجهة وظهور الاسم في لوحة الأدمن والصفحة الرئيسية، وفحص مستقل عبر testing agent iteration 21 أكد نجاح التدفقات الأساسية.
- إعادة بناء واجهة الإنتاج، تحديث نسخة release، وإعادة إنشاء ملف التثبيت NSIS.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1110549` بايت، و SHA256: `c91fe3b3382a9cf5857c59f4efd7764cf9cefd285f93ede977de5d6e9d84ea18`.

## تحديث المتبقي حسب الأولوية — بعد تقسيم الجهات
### P0
- لا توجد مهام حرجة متبقية في عزل الجهات بعد الاختبار.

### P1
- توسيع فصل النسخ الاحتياطي/الاستعادة بتجربة استعادة عملية لكل جهة على حدة.
- تقسيم `/app/backend/server.py` إلى Routers منفصلة لأن الملف تخطى 3000 سطر.
- تحسين عرض نتيجة CORS/OPTIONS الخارجية إن تطلبت بيئة تشغيل مختلفة عن preview.

### P2
- دعم شعار مطبوع منفصل لكل جهة داخل كل تقرير، بالإضافة إلى أيقونة الاختصار.
- التكامل الرسمي مع مصلحة الضرائب المصرية للفواتير الإلكترونية عند توفر بيانات الاعتماد والتوقيع الرقمي.

## تعديل — 2026-04-29 — تنظيم لوحة الأدمن كأقسام واختيارات
- تحويل لوحة الأدمن إلى واجهة أقسام تشبه الصفحة الرئيسية، بدل عرض كل الأدوات مرة واحدة.
- الأقسام الجديدة: الإعدادات العامة، المستخدمون، تغيير كلمة مرور الأدمن، إضافة بنك، الأرصدة الافتتاحية، تعريفة الخدمات المصرفية، المراجعة الأمنية، وسجل التدقيق.
- قسم "المستخدمون" يحتوي على: إضافة مستخدم لإدخال البيانات + المستخدمون المسجلون.
- قسم "تغيير كلمة مرور الأدمن" يحتوي على: تغيير كلمة مرور الأدمن + Google Authenticator.
- قسم "إضافة بنك" يحتوي على: إضافة بنك جديد.
- قسم "الأرصدة الافتتاحية" يحتوي على: رصيد افتتاحي لكل بنك.
- قسم "تعريفة الخدمات المصرفية" يحتوي على: تحديث تعريفة كل بنك - شركات.
- قسم "المراجعة الأمنية" يحتوي على: ضوابط الاقتراب من الاعتماد، إقفال/فتح الفترات، اعتماد التقارير، النسخ الاحتياطي، ومؤشرات الأمان.
- فصل "سجل التدقيق Audit Log" كقسم مستقل مع فلترة بالسنة والشهر والساعة، وعرض التاريخ والوقت والساعة واسم المستخدم والإجراء وكود الحالة لكل سجل.
- تحديث endpoint `/api/admin/security/audit-logs` ليدعم معاملات `year` و`month` و`hour` بجانب `limit`.
- الاختبارات: فحص lint للواجهة والخلفية، اختبار API لفلترة سجل التدقيق بالسنة/الشهر/الساعة، واختبار واجهة Playwright للتنقل بين كل أقسام الأدمن والتأكد من ظهور مكونات كل قسم وفلاتر سجل التدقيق.
- إعادة بناء واجهة الإنتاج، تحديث نسخة release، وإعادة إنشاء ملف التثبيت NSIS.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1113790` بايت، و SHA256: `7abb499e69463cd148d9e752a3876dbfb38bfb836a00feac4b5ed1c77de2242c`.

## تحديث المتبقي حسب الأولوية — بعد تنظيم لوحة الأدمن
### P0
- لا توجد مهام حرجة متبقية حالياً في لوحة الأدمن.

### P1
- تقسيم `/app/backend/server.py` إلى Routers منفصلة لتسهيل صيانة الإعدادات والأمان والجهات.
- تحسين عرض سجل التدقيق بجدول متقدم يدعم البحث النصي بجانب فلاتر السنة/الشهر/الساعة.

### P2
- دعم شعار مطبوع مستقل لكل جهة داخل كل تقرير.
- التكامل الرسمي مع مصلحة الضرائب المصرية للفواتير الإلكترونية عند توفر بيانات الاعتماد والتوقيع الرقمي.

## تعديل — 2026-04-29 — فصل صفحة إضافة مستخدم داخل لوحة الأدمن
- إضافة قسم مستقل داخل لوحة الأدمن باسم "إضافة مستخدم".
- القسم الجديد يحتوي فقط على نموذج "إضافة مستخدم لإدخال البيانات".
- تعديل قسم "المستخدمون" ليعرض فقط "المستخدمون المسجلون" بدون نموذج الإضافة.
- جعل قسم "إضافة مستخدم" هو القسم الافتراضي عند فتح لوحة الأدمن.
- الاختبارات: فحص lint للواجهة والخلفية، واختبار واجهة للتأكد من ظهور زر "إضافة مستخدم"، وأن نموذج الإضافة منفصل عن صفحة "المستخدمون".
- إعادة بناء واجهة الإنتاج، تحديث نسخة release، وإعادة إنشاء ملف التثبيت NSIS.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1113842` بايت، و SHA256: `a22ee30aa7077359ae1c42d3beb850f602f39b73dc8c90b039766239713b7d51`.

## تحديث المتبقي حسب الأولوية — بعد فصل إضافة المستخدم
### P0
- لا توجد مهام حرجة متبقية حالياً.

### P1
- إضافة حفظ آخر قسم مفتوح في لوحة الأدمن لكل أدمن.
- تقسيم `/app/backend/server.py` إلى Routers منفصلة.

### P2
- دعم شعار مطبوع مستقل لكل جهة داخل كل تقرير.

## تعديل — 2026-04-29 — التراجع عن تسمية "إضافة المستخدمين"
- بناءً على طلب المستخدم، تم التراجع عن آخر تعديل فقط وإعادة اسم قسم لوحة الأدمن من "إضافة المستخدمين" إلى "إضافة مستخدم".
- محتوى القسم بقي كما هو: "إضافة مستخدم لإدخال البيانات".
- الاختبارات: فحص lint للواجهة والخلفية، واختبار واجهة للتأكد من عودة اسم القسم إلى "إضافة مستخدم".
- إعادة بناء واجهة الإنتاج، تحديث نسخة release، وإعادة إنشاء ملف التثبيت NSIS.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1113841` بايت، و SHA256: `04f76aaae2ef207db30cb2676bc03a8c29fe10241e6219fd92d78f50791ff1ea`.

## تعديل — 2026-04-29 — إلغاء التعريفة التلقائية للمصروفات البنكية
- تعديل صفحة "المصروفات البنكية / تقرير رسوم وعمولات البنك" لتصبح يدوية بالكامل: إدخال "البيان" و"المبلغ" فقط.
- إزالة الحساب التلقائي لرسوم البنك من التقرير، وإلغاء الاعتماد على تعريفة الخدمات المصرفية أو بيانات الإيرادات/المصروفات/الودائع في حساب الرسوم.
- دعم إضافة أكثر من بيان يدوي للشهر، وحذف أي بيان، وحفظ البنود شهرياً. العرض السنوي يجمع البنود اليدوية المحفوظة في الشهور حسب البيان.
- إزالة قسم "تعريفة الخدمات المصرفية" من لوحة الأدمن، وإزالة أزرار التحديث التلقائي من PDF/رابط من الواجهة.
- إزالة صلاحية "إدارة التعريفات البنكية" من واجهة إضافة/تعديل المستخدمين لأنها لم تعد مستخدمة في النظام.
- تحديث API البنود اليدوية للمصروفات البنكية ليخزن قائمة `items` تحتوي على `statement` و`amount`، مع الحفاظ على قراءة القيم القديمة إن وجدت.
- الاختبارات: فحص lint للواجهة والخلفية، اختبار API لحفظ/قراءة/تنظيف بنود يدوية، واختبار واجهة للتأكد من اختفاء قسم التعريفة من الأدمن وحفظ بيانين يدويين وإظهار إجماليهما في التقرير.
- إعادة بناء واجهة الإنتاج، تحديث نسخة release، وإعادة إنشاء ملف التثبيت NSIS.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1103610` بايت، و SHA256: `0b6732a19af146698f92e6170ba79baa52a29ffa63ca0b0698d5667760130725`.

## تحديث المتبقي حسب الأولوية — بعد إلغاء التعريفة التلقائية
### P0
- لا توجد مهام حرجة متبقية حالياً.

### P1
- تنظيف endpoints القديمة الخاصة بالتعريفة البنكية من الخلفية نهائياً بعد التأكد من عدم الحاجة للأرشيف السابق.
- تقسيم `/app/backend/server.py` إلى Routers منفصلة.

### P2
- دعم استيراد بنود المصروفات البنكية اليدوية من ملف Excel شهرياً.

## تعديل — 2026-04-29 — إضافة العدد والإجمالي لتقرير رسوم وعمولات البنك
- تعديل إدخال المصروفات البنكية اليدوية ليصبح: البيان، العدد، المبلغ، الإجمالي.
- الإجمالي يُحسب تلقائياً في الواجهة والخلفية حسب المعادلة: `العدد × المبلغ`.
- جدول التقرير أصبح يعرض الأعمدة: البيان، العدد، المبلغ، الإجمالي، مع إجمالي عام في آخر الجدول.
- العرض السنوي يجمع البنود اليدوية حسب البيان ويجمع العدد والإجمالي، ويحسب متوسط المبلغ من الإجمالي/العدد عند التجميع.
- تحديث API البنود اليدوية ليحفظ `count` و`amount` و`total`، مع دعم قراءة البنود القديمة بدون كسر.
- الاختبارات: فحص lint للواجهة والخلفية، اختبار API لحفظ بند بعدد 3 ومبلغ 25.5 والتحقق من إجمالي 76.5، واختبار واجهة لإدخال عدد 4 ومبلغ 30.25 والتحقق من إجمالي 121.00 في الصف والتقرير.
- إعادة بناء واجهة الإنتاج، تحديث نسخة release، وإعادة إنشاء ملف التثبيت NSIS.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1105003` بايت، و SHA256: `9f832dd452437a54c31b01e1ca22d56255cd3f8d48f4fad6a416f92862030b2b`.

## تحديث المتبقي حسب الأولوية — بعد إضافة العدد والإجمالي
### P0
- لا توجد مهام حرجة متبقية حالياً.

### P1
- دعم استيراد بنود رسوم وعمولات البنك من Excel بصيغة: البيان، العدد، المبلغ.
- تقسيم `/app/backend/server.py` إلى Routers منفصلة.

### P2
- دعم قوالب بنود متكررة لكل بنك لتسريع الإدخال اليدوي الشهري.

## تعديل — 2026-04-29 — إلغاء الفاتورة الإلكترونية من مشروع التكافل فقط
- إلغاء ظهور كارت "الفاتورة الإلكترونية" من الصفحة الرئيسية عند تسجيل الدخول على جهة `social-solidarity` = مشروع التكافل الاجتماعي.
- منع الوصول المباشر لمسار `/electronic-invoice` لمستخدمي مشروع التكافل وتحويلهم للصفحة الرئيسية.
- منع جميع API الخاصة بالفاتورة الإلكترونية لمشروع التكافل الاجتماعي بإرجاع 404، مع إبقائها تعمل للنقابة العامة `general-union`.
- إخفاء صلاحية "إدارة الفاتورة الإلكترونية" من شاشة إضافة/تعديل المستخدمين داخل أدمن مشروع التكافل فقط، مع استمرارها متاحة للنقابة العامة.
- الفاتورة الإلكترونية ما زالت موجودة للنقابة العامة فقط، وما زالت **MOCKED** داخلياً وليست تكاملاً رسمياً مع مصلحة الضرائب.
- الاختبارات: فحص lint للواجهة والخلفية، اختبار API يؤكد أن مشروع التكافل يحصل على 404 وأن النقابة تحصل على 200، واختبار واجهة يؤكد اختفاء الكارت من مشروع التكافل وظهوره وفتح الصفحة للنقابة العامة.
- إعادة بناء واجهة الإنتاج، تحديث نسخة release، وإعادة إنشاء ملف التثبيت NSIS.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1105696` بايت، و SHA256: `b0a728d6598d6313652b6da7ce646f8e749e9930bb49b0a7c1bbf49d4d8160d9`.

## تحديث المتبقي حسب الأولوية — بعد إلغاء الفاتورة للتكافل
### P0
- لا توجد مهام حرجة متبقية حالياً.

### P1
- تنظيف بيانات الفاتورة الإلكترونية القديمة الخاصة بمشروع التكافل من قاعدة البيانات إذا قرر المستخدم حذف الأرشيف نهائياً وليس فقط تعطيل الوصول.
- تقسيم `/app/backend/server.py` إلى Routers منفصلة.

### P2
- جعل تفعيل/تعطيل الوحدات يتم من شاشة إعدادات كل جهة بدلاً من كود ثابت.

## تعديل — 2026-04-29 — إعدادات الخواص لتفعيل/تعطيل وحدات كل جهة
- إضافة صفحة جديدة داخل لوحة الأدمن باسم "إعدادات الخواص".
- الصفحة تتيح للأدمن تفعيل أو تعطيل وحدات الجهة الحالية بدون تعديل الكود.
- الوحدات المدعومة حالياً: فوائد الودائع، التسويات البنكية، الإيرادات، المصروفات، تحليل المصروفات، المصروفات البنكية، دفتر الأستاذ، الفاتورة الإلكترونية.
- الإعدادات محفوظة لكل جهة بشكل مستقل داخل `organizations.modules`، وتُرجع ضمن بيانات المستخدم `organization_modules` حتى تختفي/تظهر الوحدات فوراً بعد الحفظ.
- مشروع التكافل الاجتماعي يبدأ افتراضياً والفاتورة الإلكترونية مغلقة، والنقابة العامة تبدأ والفاتورة الإلكترونية مفعلة.
- الصفحة الرئيسية ومسارات الوحدات في الواجهة أصبحت تعتمد على إعدادات الخواص؛ عند تعطيل وحدة تختفي من الصفحة الرئيسية، ومحاولة فتح مسارها مباشرة تعيد المستخدم للصفحة الرئيسية.
- صلاحية "إدارة الفاتورة الإلكترونية" في إضافة/تعديل المستخدمين تختفي تلقائياً إذا كانت وحدة الفاتورة الإلكترونية معطلة للجهة.
- endpoints الفاتورة الإلكترونية أصبحت تعتمد على إعدادات الخواص بدلاً من شرط ثابت، لذلك يمكن تفعيلها/تعطيلها لكل جهة من الشاشة الجديدة.
- الاختبارات: فحص lint للواجهة والخلفية، اختبار API لجلب وحفظ إعدادات الخواص والتأكد من تحديث `auth/me`، واختبار واجهة لتعطيل دفتر الأستاذ وإخفائه ثم إعادته بنجاح.
- إعادة بناء واجهة الإنتاج، تحديث نسخة release، وإعادة إنشاء ملف التثبيت NSIS.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1109584` بايت، و SHA256: `bb5bc2496b6187e7083c305714fb30870aa4b9d26a9a200d1b08d10a1037e100`.

## تحديث المتبقي حسب الأولوية — بعد إعدادات الخواص
### P0
- لا توجد مهام حرجة متبقية حالياً.

### P1
- توسيع حماية backend لجميع وحدات النظام بحيث يتم منع API للوحدة المعطلة وليس فقط إخفاء الواجهة/المسار.
- تقسيم `/app/backend/server.py` إلى Routers منفصلة.

### P2
- إضافة وصف مختصر لكل خاصية داخل شاشة إعدادات الخواص لتوضيح تأثير تعطيلها قبل الحفظ.

## تعديل — 2026-04-29 — الاسم الكامل والوصف العربي في سجل التدقيق
- إضافة حقل إجباري عند إنشاء مستخدم جديد: "الاسم بالكامل (باللغة العربية)".
- إضافة حقل للأدمن داخل قسم "تغيير كلمة مرور الأدمن": "الاسم بالكامل (باللغة العربية)" مع زر حفظ مستقل.
- إضافة إمكانية تعديل الاسم الكامل للمستخدمين المسجلين من قائمة المستخدمين.
- تحديث نموذج المستخدم في الخلفية ليخزن `full_name` ويمنع إنشاء/تعديل اسم كامل لا يحتوي على حروف عربية.
- تحديث سجل التدقيق ليحفظ ويعرض:
  - `actor_full_name`: اسم الشخص الذي نفذ الإجراء، من حقل الاسم الكامل للمستخدم/الأدمن.
  - `arabic_description`: وصف عربي شامل لما حدث، مثل نوع الإجراء، القسم، المسار، نتيجة التنفيذ، كود الحالة، وبعض التفاصيل المهمة.
- تحديث واجهة سجل التدقيق Audit Log لعرض اسم منفذ الإجراء باللغة العربية والوصف العربي الكامل بدلاً من الاعتماد على اسم المستخدم التقني فقط.
- تحديث مستندات الاختبار وبيانات الاعتماد لتوضيح أن `full_name` إجباري وأن سجل التدقيق يعرض `actor_full_name` و`arabic_description`.
- الاختبارات: فحص lint للواجهة والخلفية، اختبار API لإنشاء مستخدم باسم عربي ورفض الاسم اللاتيني، تحديث اسم المستخدم، والتحقق من ظهور `actor_full_name` و`arabic_description` في سجل التدقيق؛ واختبار واجهة لحفظ اسم الأدمن، إنشاء مستخدم باسم عربي، وعرض الاسم والوصف العربي في Audit Log.
- إعادة بناء واجهة الإنتاج، تحديث نسخة release، وإعادة إنشاء ملف التثبيت NSIS.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1112172` بايت، و SHA256: `ab159015732d5a97519c686e3630ce74596b381165ffcdcffa2dba3fa43c5781`.

## تحديث المتبقي حسب الأولوية — بعد تحسين سجل التدقيق
### P0
- لا توجد مهام حرجة متبقية حالياً.

### P1
- إضافة قاموس أوضح لتحويل أسماء الحقول التقنية في وصف Audit Log إلى تسميات عربية بالكامل.
- تقسيم `/app/backend/server.py` إلى Routers منفصلة.

### P2
- إضافة تصدير Audit Log إلى Excel/Word مع الوصف العربي واسم الشخص.

## تعديل — 2026-04-29 — القيود اليومية التلقائية واليدوية
- إضافة وحدة جديدة باسم "القيود اليومية" ضمن الصفحة الرئيسية وإعدادات الخواص لكل جهة.
- رقم القيد أصبح تسلسلياً لكل جهة بشكل مستقل، يبدأ من رقم 1 في كل جهة عند أول قيد.
- إضافة صفحة `/journal-entries` لعرض دفتر القيود اليومية، مع فلاتر التاريخ ونوع المصدر.
- إضافة نموذج قيد يدوي للأدمن فقط، مع سطور متعددة للحسابات، مدين، دائن، ومنع الحفظ إذا كان إجمالي المدين لا يساوي إجمالي الدائن.
- كل قيد يُحفظ مباشرة بحالة "معتمد".
- إضافة توليد تلقائي للقيود من العمليات التالية:
  - الإيرادات: مدين البنك أو شيكات تحت التحصيل، دائن الإيرادات.
  - المصروفات: مدين المصروفات، دائن البنك أو شيكات صادرة.
  - المصروفات البنكية اليدوية: مدين المصروفات البنكية، دائن البنك.
  - إثبات عوائد الودائع: مدين عوائد ودائع مستحقة، دائن الإيرادات.
  - فروق التسويات البنكية عند وجود فرق.
- عند تعديل أو حذف العملية الأصلية، يتم تحديث أو حذف القيد التلقائي المرتبط بنفس المصدر.
- الحسابات الافتراضية المستخدمة كما طلب المستخدم: البنك، الإيرادات، المصروفات، المصروفات البنكية، شيكات تحت التحصيل، شيكات صادرة، عوائد ودائع مستحقة.
- إضافة حماية إعدادات الخواص للقيود اليومية: عند تعطيل `journal_entries` تختفي من الصفحة الرئيسية ويمنع فتح المسار المباشر في الواجهة.
- الاختبارات الذاتية: إنشاء قيد يدوي اجتماعي برقم 1، توليد قيد تلقائي للمصروفات البنكية برقم 2، إنشاء قيد للنقابة برقم 1 مستقل، واختبار واجهة لإضافة قيد يدوي متوازن وظهوره في الجدول، ثم تنظيف بيانات الاختبار.
- اختبار مستقل `iteration_22`: أكد نجاح تدفقات القيود اليومية الأساسية، رفض القيد غير المتوازن، توليد القيد التلقائي للمصروفات البنكية، فصل التسلسل بين الجهتين، وتعطيل/إعادة وحدة القيود من إعدادات الخواص.
- ملاحظة فنية: public OPTIONS CORS في بيئة preview ما زالت تُدار من طبقة ingress/Cloudflare وترجع wildcard قبل الوصول للتطبيق؛ داخلياً `localhost:8001` يرجع explicit origin وcredentials بشكل صحيح. تدفق الواجهة وتسجيل الدخول يعملان في الاختبارات.
- إعادة بناء واجهة الإنتاج، تحديث نسخة release، وإعادة إنشاء ملف التثبيت NSIS.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1122618` بايت، و SHA256: `ddefbd766f1d728e717f13fac70ceee2e6ca05806af18446467ad0f8262b2ced`.

## تحديث المتبقي حسب الأولوية — بعد القيود اليومية
### P0
- لا توجد مهام حرجة متبقية في القيود اليومية بعد اختبار التدفقات الأساسية.

### P1
- إضافة صفحة شجرة الحسابات الرسمية وربط كل شاشة بحساباتها بدل الاعتماد على أسماء الحسابات الافتراضية فقط.
- إضافة إمكانية تعديل القيود المعتمدة من الأدمن مع سبب تعديل وحفظ سجل تغييرات لكل قيد.
- توسيع حماية backend لجميع الوحدات المعطلة من إعدادات الخواص.
- تقسيم `/app/backend/server.py` إلى Routers منفصلة.

### P2
- إضافة ميزان المراجعة اعتماداً على القيود اليومية.
- إضافة تصدير القيود اليومية إلى Excel/Word/PDF.

## تعديل — 2026-04-29 — بداية شجرة الحسابات التلقائية
- إضافة وحدة جديدة باسم "شجرة الحسابات" تظهر في الصفحة الرئيسية وتدخل ضمن إعدادات الخواص لكل جهة.
- إنشاء شجرة حسابات تلقائية لكل جهة بشكل مستقل، مع مزامنة تلقائية للحسابات الأساسية وحسابات البنوك عند فتح الصفحة أو الضغط على "تحديث تلقائي".
- الحسابات التلقائية الأساسية تشمل: الأصول، البنوك، حساب مستقل لكل بنك، شيكات تحت التحصيل، عوائد ودائع مستحقة، الالتزامات، شيكات صادرة، حقوق الملكية/الفائض، الإيرادات، إيرادات فوائد ودائع، المصروفات، والمصروفات البنكية.
- إضافة صفحة `/chart-accounts` لعرض دليل الحسابات مع فلترة حسب نوع الحساب وإمكانية إضافة حسابات فرعية يدوياً للأدمن.
- ربط القيود اليومية تلقائياً بشجرة الحسابات: كل سطر قيد أصبح يمكنه تخزين `account_id` و`account_code` و`account_type` بجانب اسم الحساب.
- تحديث توليد القيود التلقائية للمصروفات البنكية والإيرادات والمصروفات وعوائد الودائع والتسويات لاستخدام الحساب الرسمي من الشجرة كلما أمكن، وخاصة حساب البنك الفعلي بدلاً من كلمة "البنك" العامة.
- تحديث صفحة القيود اليومية لعرض كود الحساب بجانب اسم الحساب داخل سطور القيد.
- إضافة endpoints: `/api/chart-accounts`, `/api/chart-accounts/sync`, `/api/chart-accounts`, `/api/chart-accounts/{account_id}`.
- إضافة `chart_accounts` إلى النسخ الاحتياطي، وربطها بعزل الجهات.
- الاختبارات: فحص lint للواجهة والخلفية، اختبار API لمزامنة الشجرة ووجود الحسابات التلقائية وربط قيد مصروف بنكي تلقائي بأكواد الحسابات، واختبار واجهة لظهور الشجرة وإضافة حساب من الواجهة ثم تنظيف حساب الاختبار.
- إعادة بناء واجهة الإنتاج، تحديث نسخة release، وإعادة إنشاء ملف التثبيت NSIS.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1130817` بايت، و SHA256: `262e3645d905a09cceaa3669a4f6084a1ef82db7a28f25feae99132a7fc38c34`.

## تحديث المتبقي حسب الأولوية — بعد بداية شجرة الحسابات
### P0
- لا توجد مهام حرجة متبقية في أول إصدار من شجرة الحسابات بعد الاختبار.

### P1
- إضافة تعديل كامل للحسابات من الواجهة مع منع تعطيل/حذف حساب عليه قيود.
- إضافة اختيار الحساب من قائمة داخل إضافة القيد اليدوي بدلاً من الكتابة الحرة.
- إضافة ميزان المراجعة اعتماداً على شجرة الحسابات والقيود اليومية.
- تقسيم `/app/backend/server.py` إلى Routers منفصلة.

### P2
- إضافة مراكز تكلفة وربطها بسطر القيد.
- إضافة استيراد شجرة الحسابات من Excel.

## تعديل — 2026-04-29 — ميزان المراجعة التلقائي
- إضافة وحدة جديدة باسم "ميزان المراجعة" داخل الصفحة الرئيسية وإعدادات الخواص.
- إضافة endpoint `/api/trial-balance` يحسب التقرير تلقائياً من القيود اليومية المعتمدة وشجرة الحسابات لكل جهة بشكل مستقل.
- يدعم التقرير فلاتر: من تاريخ، إلى تاريخ، نوع الحساب، وإظهار الحسابات ذات الحركة/الرصيد فقط.
- التقرير يعرض: كود الحساب، اسم الحساب، نوع الحساب، إجمالي مدين، إجمالي دائن، رصيد مدين، رصيد دائن.
- إضافة KPIs: حالة الميزان، إجمالي المدين، إجمالي الدائن، إجمالي الأرصدة المدينة، إجمالي الأرصدة الدائنة.
- إضافة طباعة وتصدير Excel/Word لميزان المراجعة.
- إضافة صفحة `/trial-balance` بواجهة عربية كاملة.
- اختبار API: إنشاء حركة مصروفات بنكية تولد قيداً تلقائياً، ثم تأكيد أن ميزان المراجعة للفترة متوازن بإجمالي مدين 100 ودائن 100، وأن فلتر نوع الحساب `expense` يعرض حسابات المصروفات فقط ويظهر غير متوازن طبيعياً لأنه فلتر جزئي.
- اختبار واجهة: ظهور كارت ميزان المراجعة، فتح الصفحة، ظهور الجدول، تشغيل فلتر نوع الحساب.
- اختبار مستقل `iteration_23`: أكد تدفقات ميزان المراجعة الأساسية، ظهور الصفحة، التصدير/الطباعة، وانعكاس قيد المصروفات البنكية في الميزان، وتعطيل الوحدة من إعدادات الخواص. كما كشف خلل فلتر نوع الحساب وتم إصلاحه والتحقق منه ذاتياً.
- ملاحظة فنية متبقية: public OPTIONS CORS في بيئة preview ما زالت تُدار من طبقة ingress/Cloudflare وترجع wildcard قبل الوصول للتطبيق؛ داخلياً `localhost:8001` يرجع explicit origin وcredentials، وتدفق الواجهة يعمل.
- إعادة بناء واجهة الإنتاج، تحديث نسخة release، وإعادة إنشاء ملف التثبيت NSIS.
- التحقق من رابط تنزيل المثبت `/api/download/setup`: ملف EXE صالح يبدأ بـ `MZ`، الحجم `1136033` بايت، و SHA256: `6d16e29562035b7fae6b30b819c60407a150dfe62677924b0fd0d49b430c764e`.

## تحديث المتبقي حسب الأولوية — بعد ميزان المراجعة
### P0
- لا توجد مهام حرجة متبقية في ميزان المراجعة بعد إصلاح فلتر نوع الحساب.

### P1
- إضافة اختيار الحساب من قائمة داخل القيد اليدوي بدلاً من الكتابة الحرة.
- إضافة تقارير القوائم المالية اعتماداً على ميزان المراجعة: قائمة الإيرادات والمصروفات، وقائمة المركز المالي.
- توسيع حماية backend لجميع الوحدات المعطلة من إعدادات الخواص.
- تقسيم `/app/backend/server.py` إلى Routers منفصلة.

### P2
- إضافة تصدير ميزان المراجعة بصيغ مخصصة مع شعار الجهة.
