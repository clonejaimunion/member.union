# PRD — نظام محاسبي متكامل للنقابة العامة ومشروع التكافل الاجتماعي

## بيان المنتج الأصلي
تطبيق محلي يعمل على Windows بنظام واجهة عربية كاملة لإدارة المحاسبة والودائع البنكية لجهتين منفصلتين بالكامل:
- النقابة العامة
- مشروع التكافل الاجتماعي

يجب أن يضمن النظام عزلاً كاملاً للبيانات بين الجهتين، وأن يعتمد على محرك قواعد محاسبية آلي بحيث يُدخل المستخدم أحداث العمل فقط، بينما يقوم النظام بتوليد القيود اليومية المتوازنة وتحديث دفتر الأستاذ دون إدخال يدوي للمدين والدائن. التطبيق موجه للعمل دون إنترنت داخل بيئة Windows محلية مع إمكانية الوصول عبر الشبكة المحلية LAN وحزمة تثبيت NSIS.

## المبادئ الحاكمة
- الواجهة والرسائل والتقارير باللغة العربية وباتجاه RTL.
- لا حذف محاسبي للقيود المرحلة؛ يتم استخدام القيود العكسية للحفاظ على الأثر الرقابي.
- دفتر الأستاذ والميزان والتسويات يجب أن تعتمد على journal_entries كمصدر الحقيقة.
- كل منظمة لها بياناتها ومستخدميها وقواعدها وقيودها بصورة مستقلة.
- محرك القواعد يحوّل الأحداث مثل الإيراد، المصروف، الرصيد الافتتاحي، إذن عضوية جماعي، ربط وديعة، وإهلاك أصل إلى قيود تلقائية متوازنة.
- تقارير PDF وExcel/Word يجب أن تكون عربية قابلة للطباعة والتصدير.

## المعمارية الحالية
- Backend: FastAPI + Motor/MongoDB في `/app/backend/server.py`.
- Frontend: React + Tailwind + Shadcn UI في `/app/frontend/src`.
- قاعدة البيانات: MongoDB عبر `MONGO_URL` و`DB_NAME` فقط.
- التغليف المحلي: NSIS عبر `/app/local_install/BankDepositSystem.nsi`.
- ملفات الإصدار المحلي داخل `/app/release/BankDepositSystem`، وملف التثبيت الناتج `/app/dist/BankDepositSystemSetup.exe`.

## أهم النماذج والكيانات
- `journal_entries`: مصدر الحقيقة للقيود ودفتر الأستاذ.
- `chart_accounts`: دليل الحسابات التلقائي لكل منظمة.
- `accounting_rules`: قواعد الترحيل الآلي.
- `banks` و`bank_settings`: البنوك وأرصدة الإدارة.
- `reconciliations`: التسويات البنكية.
- `memberships`: عضويات مشروع التكافل.
- `membership_batch_payments`: أذون التحصيل الجماعية وتوزيعات FIFO.
- `fixed_assets` و`fixed_asset_depreciations`: الأصول والإهلاكات.

## ما تم تنفيذه سابقاً
- محرك قواعد محاسبية آلي مع محاكاة Fail-Safe.
- دفتر أستاذ وميزان مراجعة من القيود اليومية.
- قيود عكسية بدلاً من حذف القيود المحاسبية.
- سجل تدقيق يتضمن قبل/بعد التعديل.
- تقارير PDF عربية محسّنة.
- إهلاك سنوي تلقائي للأصول الثابتة.
- تحسين تشغيل LAN وملفات التشغيل المحلية.

## تحديثات منفذة بتاريخ 2026-05-04
- تم تنفيذ محرك قيود الأرصدة الافتتاحية للبنوك: إدخال رصيد افتتاحي يولد قيداً تلقائياً من حساب البنك إلى حساب رصيد افتتاحي.
- تم جعل الرصيد الدفتري في التسوية البنكية مقروءاً من دفتر الأستاذ/القيود اليومية فقط عبر endpoint `/api/banks/{bank_id}/book-balance`، مع تجاهل أي رقم يدوي مرسل كرصيد دفتري في التسوية.
- تم إضافة حالات العضوية الأربع: فعال، معاش، متوفي، مستقيل، مع إيقاف الاشتراكات الجديدة لغير الفعالين والحفاظ على المديونيات القديمة.
- تم إضافة الاشتراك الشهري الافتراضي 3 جنيه، وحساب المستحق والمحصل والمتبقي لكل عضو.
- تم إضافة إذن جماعي للجان عبر `/api/memberships/batch-payments` يوزع المبلغ FIFO على أقدم المديونيات ويولد قيد يومية تلقائي.
- تم إضافة تقرير تحصيل العضويات حسب اللجنة أو المحافظة عبر `/api/memberships/collection-report`.
- تم توسيع التقرير السنوي للعضوية ليشمل المستحق والمحصل والمتبقي.
- تم تحديث صفحة العضوية لإضافة حالة العضو، الإذن الجماعي، تقرير المديونيات والتحصيل، وسجل الأذون.
- تم توحيد عرض التواريخ بدون وقت في الواجهة، وتعديل صفحة الودائع لاستخدام تاريخ فقط لإنشاء/استحقاق الوديعة.
- تم تحديث محرك القواعد ليدعم `OpeningBalance` و`MembershipBatchPayment` في القواعد والمحاكاة والواجهة.
- تم بناء نسخة الإنتاج للواجهة وتحديث حزمة Windows: `/app/dist/BankDepositSystemSetup.exe`.

## حالة الاختبارات
- اختبار ذاتي API ناجح لتسجيل الدخول، الرصيد الافتتاحي، الرصيد الدفتري من GL، العضوية، الإذن الجماعي، وتقرير العضوية.
- اختبار واجهة smoke ناجح: ظهور أقسام إذن جماعي وتقرير المديونيات في صفحة العضوية.
- اختبار مستقل iteration_57: نجحت تدفقات الأرصدة الافتتاحية، التسوية من GL، العضوية، الإذن الجماعي، تنسيق تاريخ الوديعة. كشف خطأ في Literals لمحرك القواعد وتم إصلاحه.
- إعادة اختبار ذاتي بعد الإصلاح: `/api/rules-engine/rules` يعمل، ومحاكاة `OpeningBalance` و`MembershipBatchPayment` تعمل بنجاح.
- فحص lint ناجح للباك إند والملفات الأمامية المعدلة.
- بناء `yarn craco build` ناجح، و`makensis` أنتج ملف التثبيت بنجاح.

## نقاط معروفة
- تكامل الفاتورة الإلكترونية ETA ما زال MOCKED ولم يتم ربطه فعلياً بسبب انتظار بيانات/SDK التكامل.
- ملف `/app/backend/server.py` ما زال كبيراً ويحتاج تفكيكاً لاحقاً إلى APIRouter modules لتقليل مخاطر الصيانة.

## الأولويات الحالية
### P0 — مكتمل
- الأرصدة الافتتاحية بقيود تلقائية وربطها بدفتر الأستاذ.
- التسوية البنكية من الرصيد الدفتري المحسوب من GL.
- حالات العضوية وإيقاف الاشتراكات الجديدة لغير الفعالين.
- الإذن الجماعي للجان وتوزيع FIFO وقيد اليومية.

### P1 — مكتمل
- تنسيق التاريخ بدون وقت في تدفقات الودائع والعرض العام.
- تقارير العضوية حسب اللجنة والمحافظة للمستحق/المحصل/المتبقي.

### P2 — متبقٍ
- تفكيك `/app/backend/server.py` إلى ملفات Routers وخدمات أصغر.
- استكمال تكامل ETA الحقيقي عند توفر مفاتيح/SDK من المستخدم.
- تحسين محرر محرك القواعد بخصائص بحث/نسخ/تعطيل جماعي.

## بيانات اختبار معروفة
- username: `admin`
- password: `Admin@123`
- organizations: `general-union`, `social-solidarity`


## تحديث إصلاح حرج بتاريخ 2026-05-04
- تم حل مشكلة سطور القيود غير المرتبطة بحسابات في القيود: 122، 123، 128، 129، 130، 131، 132، 133، 138، 139، 144، 145، 146، 147، 152، 153.
- تم إضافة حساب `الخزينة` إلى شجرة الحسابات كحساب أصل قابل للترحيل بكود `1150` و`system_key=cash_box`.
- تم ربط الاسم القديم `إيرادات الاشتراكات` تلقائياً بحساب `إيرادات اشتراكات العضوية` كود `4103` و`system_key=membership_subscription_revenue`.
- تم تشغيل مزامنة شجرة الحسابات وترميم روابط القيود الحالية؛ في منظمة مشروع التكافل تم ترميم 32 سطر/قيد مرتبط بهذه المشكلة.
- تم تشغيل فحص التدفق الكامل: `journal_entries → ledger → trial_balance → income_statement & cash_in_out → balance_sheet`.
- نتائج الفحص الذاتي: لا توجد سطور بلا حساب، ميزان المراجعة متوازن، دفتر الأستاذ مطابق للميزان، وفرق اتزان الميزانية = 0.
- نتائج Testing Agent iteration_58: جميع الفحوص المطلوبة نجحت 7/7، ولا توجد APIs/flows معطلة ضمن نطاق الاختبار.
- تم تحديث حزمة Windows النهائية بعد الإصلاح: `/app/dist/BankDepositSystemSetup.exe`.


## تحديث تقارير العضوية والطباعة بتاريخ 2026-05-04
- تم جعل تقارير العضوية الخمسة قابلة للطباعة كصفحات مستقلة: تقرير المديونيات والتحصيل، التقرير السنوي، الأذون الجماعية المحفوظة، بحث الاسم، وخروج المعاش.
- تم إضافة أزرار طباعة/تصدير لقسمي أذون جماعية محفوظة وبحث بالاسم، واستبدال طباعة خروج المعاش بطباعة تقرير مستقلة.
- تم إخفاء قسم العضويات المسجلة من عرض الصفحة مع استمرار تحميل بيانات العضويات لاستخدامها في القوائم والتقارير والبحث.
- تم إضافة حالة العضويات المستوردة وتاريخ الحالة إلى استيراد البيانات الخارجية، وربطها بالباك إند بحيث تحفظ الحالة وتاريخ الإيقاف تلقائياً لغير الفعالين.
- تم إضافة إعداد ESLint حديث للواجهة وسكربت `yarn lint` بعد ملاحظة الاختبار المستقل.
- اختبارات ذاتية: API preview/commit للاستيراد بحالة معاش، Screenshot للواجهة، Print media = 5 صفحات تقارير، lint/build ناجحين.
- Testing Agent iteration_59: كل اختبارات العضوية والطباعة والاستيراد نجحت؛ تمت معالجة ملاحظة lint بعدها بنجاح.
- تم تحديث حزمة Windows بعد تغييرات الواجهة والباك إند: `/app/dist/BankDepositSystemSetup.exe`.


## تحديث القيود العكسية وتدفقات البيانات بتاريخ 2026-05-04
- تم إلغاء عمود/ميزة `مقارن` نهائياً من صفحة الميزانية والحسابات الختامية، ولم تعد الصفحة تطلب تقرير السنة السابقة.
- تم تثبيت علم القيود العكسية `is_reversal=true/false` داخل نموذج القيود اليومية واستجابة API.
- القيود العكسية تبقى محفوظة في قاعدة البيانات لأغراض التدقيق، لكنها مخفية من دفتر اليومية، دفتر الأستاذ، ميزان المراجعة، القوائم المالية، وتقارير الفحص.
- تم إضافة endpoint شامل: `/api/admin/data-flow-validation` يعرض تدفقات البيانات المحاسبية والعضوية لكل جهة ويفحص الاتزان وعدم وجود missing/orphan entries.
- تم إضافة صفحة `تدفقات البيانات` داخل لوحة التحكم الآمنة لعرض مسار: journal_entries → ledger → trial_balance → income_statement/cash_in_out → balance_sheet، مع فحص العضوية والحالات والتحصيل.
- تم إضافة صفحة `تفريغ البيانات` داخل لوحة التحكم الآمنة للسوبر أدمن فقط، مع عبارة تأكيد إجبارية `تفريغ البيانات نهائيا` قبل أي حذف. لم يتم تنفيذ التفريغ على بيانات التطوير.
- تم تشغيل الفحص الكامل بنجاح: كل الجهات سليمة، ميزان المراجعة متوازن، فرق الميزانية = 0، ولا توجد قيود يتيمة أو سطور بلا حساب ضمن النطاق المطلوب.
- تم تشغيل فحص العضوية: الحالات والتحصيل والتوزيع سليمة وبدون issues.
- Testing Agent iteration_60: نجح 9/9، ونجحت واجهة القوائم ولوحة الأدمن وقيود التفريغ، ولا توجد تدفقات مكسورة ضمن النطاق.
- `yarn lint` و`yarn craco build` نجحا، وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe`.


## تحديث Data Flow & Rules Manager بتاريخ 2026-05-04
- تم إضافة صفحة جديدة داخل لوحة السوبر أدمن باسم `Data Flow & Rules Manager`، وهي خاصة بالسوبر أدمن فقط.
- تم إضافة Flow Monitor يعرض مراحل التدفق: إدخال العملية، الفحص الآلي، إنشاء القيد، دفتر اليومية، الأستاذ العام، ميزان المراجعة، القوائم المالية، ومحرك القواعد، مع الحالة وعدد العمليات وآخر تشغيل وعدد الأخطاء.
- تم إضافة Rules Engine إداري داخل الصفحة لإدارة القواعد فقط: الحدث، النوع، الحساب المدين، الحساب الدائن، وتشغيل/إيقاف القاعدة، بدون السماح بتعديل القيود المحاسبية نفسها.
- تم إضافة Validation سريع داخل الصفحة لفحص: اتزان القيود، الأستاذ العام، ميزان المراجعة، الميزانية، والعضوية.
- تم إضافة endpoints محمية بالسوبر أدمن: `/api/admin/data-flow-rules-manager` و`/api/admin/data-flow-rules-manager/rules/{rule_id}`.
- تم ربط تعديلات القواعد بسجل التدقيق مع `before_document` و`after_document`، ويظهر من قام بالتعديل ووقت التعديل والقيمة القديمة والجديدة.
- تم التحقق أن تعديل القاعدة لا يغير `journal_entries` ولا يسمح بتعديل القيود نفسها.
- الاختبار الذاتي نجح: GET للمدير، تعديل قاعدة ثم إرجاعها، وجود audit before/after، وظهور الواجهة.
- Testing Agent iteration_61 نجح بالكامل: لا توجد APIs أو تدفقات مكسورة ضمن النطاق.
- نجح `yarn lint` و`yarn craco build`، وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe`.


## تحديث تاريخ الرصيد الافتتاحي والتسوية البنكية بتاريخ 2026-05-04
- تم إضافة `opening_balance_date` لكل بنك، وإلزام السوبر أدمن بإدخال تاريخ الرصيد الافتتاحي بصيغة `YYYY/MM/DD` في إضافة البنك وتعديل الأرصدة الافتتاحية.
- تم منع أي عملية مالية مرتبطة ببنك قبل تاريخ الرصيد الافتتاحي برسالة: `No financial transaction is allowed before the Opening Balance Date.`
- تم جعل قيد الرصيد الافتتاحي يُنشأ بتاريخ الرصيد الافتتاحي نفسه، مع الحفاظ على تدفق القيود إلى اليومية والأستاذ والميزان والقوائم.
- تم تطبيق حماية التاريخ على الودائع، الإيرادات، المصروفات، الأصول الثابتة، العهد، مصروفات البنك اليدوية، أذون العضوية الجماعية، والتسويات البنكية.
- تم تعديل حقل `الإدارة` في التسوية البنكية ليعرض خياراً واحداً فقط حسب الجهة الحالية: مشروع التكافل عند الدخول عليه، والنقابة العامة عند الدخول عليها.
- تم تشغيل فحص ذاتي: رفض عملية قبل التاريخ، قبول عملية في نفس التاريخ، `/api/admin/data-flow-validation` سليم، واجهة التسوية تعرض خيار إدارة واحد للجهتين.
- Testing Agent iteration_62 نجح في نطاق الطلب، وتمت معالجة ملاحظتي الرسالة وصيغة التاريخ بعده ذاتياً.
- نجح `yarn lint` و`yarn craco build`، وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe`.


## تحديث معادلة رصيد التسوية البنكية بتاريخ 2026-05-06
- تم تعديل رصيد التسوية البنكية ليحسب أوتوماتيكياً من بنود تفصيلية، وليس من إدخال يدوي أو من رصيد الأستاذ الخام فقط.
- المعادلة المعتمدة: الرصيد الافتتاحي + إجمالي الإيرادات الشهرية المحصلة للبنك + ترصيد الودائع المستحقة في الفترة + شيكات تحت التحصيل - المصروفات المدفوعة - شيكات لم تقدم للصرف - المصروفات البنكية.
- تم إضافة endpoint: `/api/banks/{bank_id}/reconciliation-balance` يعيد breakdown كامل: opening_balance, monthly_revenues, deposit_settlements, checks_under_collection, gross_total, monthly_expenses, checks_not_presented, bank_expenses, reconciliation_balance.
- حفظ/تعديل التسوية الآن يتجاهل أي `book_balance` مرسل من الواجهة ويستخدم الرصيد المحسوب تلقائياً، ويحفظ `balance_breakdown` داخل مذكرة التسوية.
- تم منع الجمع/الطرح المزدوج داخل التسوية بجعل `calculated_balance = book_balance` المحسوب من المعادلة.
- واجهة التسوية تعرض تفصيل رصيد التسوية البنكية في بطاقات واضحة، وتعرض التفصيل داخل الطباعة/المعاينة.
- تم التحقق ذاتياً من المعادلة والحفظ وتدفق البيانات، ثم نجح Testing Agent iteration_63 بالكامل بدون تدفقات مكسورة.
- نجح `yarn lint` و`yarn craco build` وتم تحديث `/app/dist/BankDepositSystemSetup.exe`.


## تحديث الودائع القائمة عند بداية الفترة بتاريخ 2026-05-06
- تم إضافة خيار `وديعة قائمة عند بداية الفترة` عند تسجيل الودائع.
- إذا كانت الوديعة قائمة أول الفترة، يسمح بأن يكون تاريخ الربط أقدم من تاريخ الرصيد الافتتاحي للبنك.
- يتم احتساب الوديعة من تاريخ بداية الفترة/تاريخ الرصيد الافتتاحي عبر `accounting_start_datetime`، وليس من تاريخ الربط القديم.
- لا يتم إنشاء حركة بنك تاريخية قبل تاريخ الرصيد الافتتاحي؛ قيد الوديعة القائمة يكون: مدين `ودائع لأجل` ودائن `رصيد افتتاحي` بتاريخ الرصيد الافتتاحي.
- الودائع الموسومة `is_opening_balance_deposit=true` لا تمنع تعديل تاريخ الرصيد الافتتاحي بسبب تاريخ ربطها القديم.
- تقارير عوائد الودائع تبدأ من `accounting_start_datetime` للودائع القائمة أول الفترة.
- تم إضافة عرض `بداية الاحتساب` في ملخص الوديعة.
- الاختبارات الذاتية نجحت، ثم Testing Agent iteration_65 نفّذ اختبارات API/UI فعلية ونجحت 2/2 backend مع UI toggle، ولا توجد تدفقات مكسورة.
- ملف التثبيت `/app/dist/BankDepositSystemSetup.exe` موجود ورابط `/api/download/setup` يعمل.


## تحديث دفتر الأستاذ — عرض الكل داخل الفترة بتاريخ 2026-05-06
- تم إضافة خيار `عرض الكل ضمن الفترة المحددة` في قائمة الحساب داخل صفحة دفتر الأستاذ.
- endpoint `/api/ledger` يدعم الآن `account_id=all` ويعيد كل حركات الحسابات القابلة للترحيل داخل الفترة فقط.
- تم إضافة `account_scope=all/single` وحقول الحساب داخل كل صف: `account_id`, `account_code`, `account_name`.
- في وضع حساب واحد، لم يعد دفتر الأستاذ يضيف حركات قبل `from_date` إلى الرصيد الجاري؛ يتم عرض واحتساب الحركات داخل الفترة المحددة فقط.
- واجهة دفتر الأستاذ تعرض عمود `الحساب` عند اختيار عرض الكل، وتوضح أن التقرير للحركات المالية داخل الفترة فقط.
- تم اختبار API ذاتياً ثم Testing Agent iteration_66: backend pytest 3/3 ناجح، UI contract ناجح، data-flow valid=true، ولا توجد APIs مكسورة.
- نجح `yarn lint` و`yarn build` وتم تحديث `/app/dist/BankDepositSystemSetup.exe`.


## تحديث منطق الفترة المالية الشامل بتاريخ 2026-05-06
- تم ضبط منطق الفترة المالية مركزياً بحيث تعتمد اليوميات والأستاذ والميزان والتسويات والتقارير على الحركات داخل الفترة فقط.
- دفتر اليومية: يدعم `from_date/to_date` ويعرض القيود المرحلة داخل الفترة فقط.
- دفتر الأستاذ: رصيد أول المدة = صافي الحركات السابقة، حركة الفترة = داخل الفترة فقط، رصيد آخر المدة = أول المدة + صافي حركة الفترة، مع دعم عرض الكل.
- ميزان المراجعة: تم إصلاح endpoint `/api/trial-balance` ليستخدم الدالة المركزية، ويرحل السابق إلى opening_balance مع إبقاء total_debit/total_credit لحركة الفترة فقط.
- التسوية البنكية: الرصيد الدفتري = رصيد أول المدة للبنك + إيرادات الفترة + ترصيد ودائع الفترة - مصروفات الفترة - المصروفات البنكية، ورصيد التسوية = الرصيد الدفتري + شيكات لم تقدم - شيكات تحت التحصيل.
- تقارير الودائع تستخدم بداية الاحتساب وتستبعد ما انتهى قبل الفترة، وتحتسب العوائد داخل الفترة فقط.
- تم إصلاح خلل كشفه Testing Agent iteration_67 في ميزان المراجعة، ثم نجح pytest الخاص بالفترة 6/6 محلياً.
- Testing Agent iteration_68 نجح بالكامل: pytest 6/6، واجهة Ledger والتسوية تعمل، data-flow valid=true، ولا توجد تدفقات مكسورة.
- نجح `yarn lint` و`yarn build` وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe`.

## تحديث منطق فوائد الحساب الجاري واستحقاق/ربط الودائع بتاريخ 2026-05-06
- تم إيقاف الترحيل الآلي لفوائد الودائع المجدولة كقيود تشغيلية؛ الوديعة تظل أصلاً يظهر في الميزانية ولا تتكرر في تقارير التشغيل.
- تم إضافة طريقتي تحصيل في الإيرادات: `فوائد الحساب الجاري` و`استحقاق وديعة`، وكلاهما يرحل فوراً كمدين بنك ودائن إيراد مناسب داخل نفس الفترة.
- تم إضافة نوع مصروف/خروج: `ربط وديعة`، ويجبر الحركة على حالة مدفوعة فوراً، يصفر الاستقطاعات، ويرحل كمدين `ودائع لأجل` ودائن بنك حتى لا يظهر كExpense تشغيلي في حساب الإيرادات والمصروفات.
- تم توحيد فلترة التقارير وفحص تدفق البيانات لاستبعاد قيود `deposit_interest` القديمة من التقارير التشغيلية والميزان/الفحص الافتراضي، مع بقاء البيانات محفوظة رقابياً.
- تم تحديث واجهتي الإيرادات والمصروفات بخيارات عربية واضحة و`data-testid` للخيارات الجديدة.
- الاختبار الذاتي نجح لإنشاء فوائد حساب جاري وربط وديعة والتحقق من القيود والقوائم المالية ثم تنظيف بيانات الاختبار.
- Testing Agent iteration_69 كشف أولاً خللاً في `data-flow-validation` بسبب احتساب قيود فوائد الودائع القديمة في جانب الفحص فقط؛ تم إصلاحه ثم نجح إعادة اختبار pytest: 7/7، وأصبح `/api/admin/data-flow-validation` يعيد `is_valid=true` للجهتين.
- نجح `yarn craco build` وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe` بعد تثبيت NSIS داخل الحاوية.

## تحديث دفتر الأستاذ — الحسابات ذات الحركة الفعلية فقط بتاريخ 2026-05-06
- تم تعديل `/api/ledger` في وضع `عرض الكل ضمن الفترة المحددة` بحيث لا يظهر أي حساب لمجرد وجود رصيد سابق أو أصل قائم قبل الفترة.
- يتم تحديد الحسابات الظاهرة من سطور القيود التي لها حركة فعلية داخل `from_date/to_date` فقط، ثم يُحسب رصيد أول المدة لهذه الحسابات وحدها من الحركات السابقة.
- الحسابات التي لها رصيد سابق فقط دون حركة داخل الفترة لا تظهر في صفوف دفتر الأستاذ ولا تدخل ضمن رصيد أول المدة المجمع في عرض الكل.
- تم الحفاظ على وضع الحساب الواحد: عند اختيار حساب محدد يظل رصيد أول المدة محسوباً لهذا الحساب، مع عرض حركات الفترة فقط.
- تم استبعاد قيود `deposit_interest` القديمة من حسابات دفتر الأستاذ الافتراضية مثل باقي التقارير التشغيلية.
- تم إضافة اختبار regression: `/app/backend/tests/test_iteration70_ledger_actual_movement_scope.py`.
- الاختبارات نجحت: iteration70 + اختبارات الفترة المالية iteration67 = 7/7، وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe`.

## تحديث دفتر الأستاذ — قيود الودائع الافتتاحية وفوائد الفترة بتاريخ 2026-05-06
- تم تعديل دفتر الأستاذ بحيث لا تظهر قيود `وديعة قائمة عند بداية الفترة` كسطر حركة داخل الفترة حتى لو كان تاريخ قيدها داخل يناير أو داخل الفترة المعروضة.
- قيد الوديعة الافتتاحية الصحيح محاسبياً يظل محفوظاً في دفتر اليومية، لكنه عند عرض دفتر الأستاذ يُرحّل إلى `رصيد أول المدة` فقط ولا يظهر ضمن حركات الفترة.
- عند اختيار حساب واحد مثل `1250 - ودائع لأجل`، يتم احتساب قيد الوديعة الافتتاحية ضمن رصيد أول المدة للحساب ولا يظهر كسطر حركة.
- عند عرض `كل الحسابات`، لا يظهر حساب الوديعة لمجرد وجود قيد افتتاحي فقط؛ يظهر فقط إذا حدثت عليه حركة فعلية داخل الفترة.
- تم إضافة حركة دفتر أستاذ مجمعة لفوائد الودائع داخل الفترة فقط: `إجمالي فوائد الودائع عن الفترة من ... إلى ...`، وتظهر بتاريخ نهاية الفترة كسطر مجمع بدلاً من إظهار أصل الوديعة أو فوائد خارج الفترة.
- الفائدة المجمعة تُحسب يومياً من تداخل فترة الوديعة مع الفترة المختارة، وتتجاهل الودائع غير النشطة خارج الفترة.
- تم إضافة اختبار regression: `/app/backend/tests/test_iteration71_ledger_opening_deposit_interest.py`.
- الاختبارات نجحت: iteration71 + iteration70 + اختبارات الفترة المالية iteration67 = 9/9، وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe`.

## إصلاح تحميل دفتر الأستاذ عند تاريخ فارغ بتاريخ 2026-05-06
- سبب رسالة `تعذر تحميل دفتر الأستاذ من القيود اليومية` كان أن واجهة `LedgerPage` ترسل `to_date=` أو `from_date=` فارغة عند مسح حقل تاريخ، فيرفضها FastAPI كقيمة تاريخ غير صالحة 422.
- تم تعديل الواجهة بحيث لا تُرسل أي معلمة تاريخ إلا إذا كانت ممتلئة فعلاً، مع منع تحميل الفترة إذا كان تاريخ البداية بعد تاريخ النهاية.
- تم التحقق من `/api/ledger` بثلاث حالات: تاريخ بداية فقط، تاريخ نهاية فقط، وبدون تواريخ — كلها تعيد 200.
- تم اختبار الصفحة بالمتصفح بعد مسح تاريخ النهاية، ولم تظهر رسالة الخطأ.
- تم تنظيف آثار بيانات الاختبارات السابقة من دفتر الأستاذ حتى لا تظهر للمستخدم.
- نجح `yarn craco build` واختبارات دفتر الأستاذ iteration70/71 = 3/3، وتم تحديث ملف التثبيت `/app/dist/BankDepositSystemSetup.exe`.
